{
  "name": "rs-dispatch-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test/crypto.test.mjs && node test/flow.test.mjs && node test/abuse.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
