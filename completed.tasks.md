# Relational Sovereignty — Completed Tasks

Append-only log. When a task moves out of `tasks.md`, add an entry here in the site's own changelog register — `~~was:~~` / `now:` — plus the task ID, the date, and the commit or PR it shipped in. Don't delete or rewrite past entries; if a completed task is later found wanting, note that as a new entry pointing back at this one, the same way `Colophon.dc.html`'s change log handles corrections.

This file is a working log, not the public-facing changelog — but entries here should be written so they could be trimmed straight into `Colophon.dc.html`'s `log` array with minimal editing, since that's where the finished, user-facing version of this record belongs.

## Format

```
### RS-0xx — short title
**Shipped:** YYYY-MM-DD · **Commit/PR:** <ref>

~~was: <prior state, in one line>~~
now: <what shipped, in one line>

Notes: anything a future maintainer needs — deviations from the original draft copy,
follow-up items spun out, verification results, etc.
```

---

### RS-002 + RS-031 (partial) — Resources.dc.html
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: no Resources page existed; nothing on the site pointed anywhere for a reader in danger~~
now: `Resources.dc.html` ships as a plain static HTML page (no runtime, no script, per RS-004's "static from the start" requirement) with twelve entries across Tier 0 (international) and Tier 1 (Canada national), each checked against the organisation's own site or corroborating independent sources on 2026-08-07: 9-8-8 Suicide Crisis Helpline, Hope for Wellness Helpline, Kids Help Phone, ShelterSafe (Women's Shelters Canada), 211 Canada, Canadian Council for Refugees, ACORN Canada, Council of Canadians with Disabilities, Egale Canada, Native Women's Association of Canada, Find A Helpline, Befrienders Worldwide. Linked from the primary nav and footer nav on every page, the RS-001 safety-stage interstitial, and the tool's exit copy. Added to `robots.txt` (`Allow` for every agent, including the training crawlers otherwise disallowed site-wide — RS-025's requirement, done as part of this task) and `sitemap.xml`, and wired into `scripts/prerender.mjs` (`COPY_AS_IS`) and `scripts/check-pages.mjs` so it's validated (HTML + axe-core) without being run through the dc-runtime prerender check it doesn't use.

Notes:
- **Partial completion, flagged honestly on the page itself.** Tier 2 (local/regional) is not built — still blocked on FLAG-01 (jurisdiction), which was asked about but went unanswered before this work started. Shipping Tier 0+1 now was a judgment call, made explicit in `tasks.md`'s resolved-decisions log: a smaller, honest page beats leaving a safety-critical page at zero indefinitely, but Tier 2 genuinely needs the answer and wasn't guessed.
- **Legal aid and immigration categories are thin, and say so.** No clean national legal-aid directory exists to link (legal aid is province-administered, 13 separate plans, no single consumer-facing index) — 211 Canada is used as the pointer instead, with an explicit note that this is a known gap. The Canadian Council for Refugees is a coalition/advocacy body, not a direct-service provider — described that way rather than implied otherwise.
- **Mutual aid networks category is empty, not padded.** No stable, professionally-staffed national mutual-aid directory was found; small individual groups were deliberately excluded per the maintenance-risk reasoning in D12/RS-031 (a dead contact is worse than no listing), even though that reasoning was written for the Solidarity Finder specifically. Marked "not yet built" on the page and in the fault list, matching the site's own honesty convention rather than silently omitting it.
- RS-031's original scope (a searchable, schema-driven Solidarity Finder widget) was **not** built as an interactive tool — the categories are small enough that a plain static list serves the same function without adding a JS dependency to a page RS-004 requires to be static from the start. If a future maintainer wants the searchable version, that's a separate, additive task, not a gap in what shipped.
- The five RS-001 safety questions are published standalone on this page per D7, ahead of the crisis-lines section, independent of whether someone arrived via the Practise gate.
- `npm run check` passes on the new page (HTML validation + axe-core, zero violations); `npm run build` correctly copies it as-is rather than attempting to prerender a page with no `<x-dc>` root.

### RS-025 — robots.txt / sitemap.xml for Resources
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: no rule existed for a page that didn't exist yet~~
now: folded into RS-002+031 above rather than done separately — `Allow: /Resources.dc.html` added under every `User-agent` block in `robots.txt`, including the training-crawler blocks that otherwise `Disallow: /` site-wide, with a dated ("last reviewed 2026-08-07") comment on the agent list. `sitemap.xml` carries the new URL at priority 0.9.

### RS-001 + RS-032 — safety interstitial in Practise.dc.html
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: a single small safety-notice paragraph sat beside the Consent Domains Map; the tool itself was reachable with no gate at all~~
now: the tool is preceded by a true, blocking three-stage sequence held in component state (`stage: "safety" | "grounding" | "tool"`, default `"safety"`) — five non-diagnostic safety questions with no score, then optional rewritten grounding (no prescribed breathing, no self-diagnosis instruction, explicitly optional), then the tool. A Resources link is present at both interstitial stages and in the closing copy. Every stage transition is one click, and every stage offers a way to skip forward without completing the one before it (no gate can only be passed one way). Nothing about the stage or any answer is stored, scored, or transmitted — it's ephemeral component state only, verified empty in `localStorage`/`sessionStorage`/cookies at every stage via a live Playwright click-through, not just asserted. The closing "you have reached the end" section was rewritten to carry RS-032's aftercare framing ("You have finished, or stopped. Either is complete... Nothing has been saved.") instead of only the original print/close-tab copy. `<meta name="robots" content="noindex">` added to the page head (SUGGEST-02, done alongside this since it directly serves the same intent).

Notes:
- **Verified by actually clicking through it**, not just by reading the prerendered HTML: a Playwright script loaded the page via a local static server, confirmed the five questions render and the domains tool does not at the default stage, confirmed the domains tool still doesn't render after advancing to grounding, confirmed it does render only after the second transition, and confirmed zero console errors and zero storage-API usage throughout. Command used a temporary, uncommitted script — not left in the repo.
- **A useful side effect, not separately engineered:** because the domains list (17 domains × 5 options, previously ~140KB of the page's prerendered weight) is now behind `sc-if atTool` and the default stage is `"safety"`, the *prerendered* snapshot a no-JS reader or crawler receives no longer contains the tool content at all — confirmed by inspecting `_site/Practise.dc.html`'s actual `#dc-root` output, not the hidden raw template. This wasn't a stated acceptance criterion but directly serves the same "not bypassable without JS" intent RS-001 asked for, given the whole page still requires JS to interact with at all (Practise keeps the runtime — see the Phase 1 sequencing note).
- **Deviation from the literal draft flow:** RS-001's original draft offered "[Open the tool] [Go to resources instead]" from the safety stage. Since RS-032 merges in a second stage (grounding) ahead of the tool, "Open the tool" was split into two buttons instead — "Continue" (→ grounding, the default path) and "Skip straight to the tool" (→ tool directly) — so that the "no forced completion" requirement holds at the first gate too, not only after grounding. Judgment call, not drawn from either draft verbatim.
- **Not done in this pass:** RS-026's reduced-language/non-verbal variant of the five questions (explicitly Phase 3 scope) was not built. RS-023's full accessibility acceptance pass (screen-reader pass, print stylesheet test on the new stages) has not run yet — `npm run check`'s axe-core pass is clean, but that's automated coverage only, not the manual SR pass the QA checklist calls for.

### RS-030 — Continuity of care clause
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: no clause governed the conflict between principle 06 (relational accountability) and principle 07 (refusal) — sovereignty language was available, unopposed, to justify abandoning a dependant~~
now: a new "Continuity of care" section on `Learn.dc.html`, placed directly after the principles grid, using the author-approved draft from `docs/spec/addendum-a.md` RS-030 verbatim (not the earlier, rejected Non-Abandonment Clause draft, which created a duty to stay and hold a community process before leaving — a sentence that would have been quotable by a controlling partner against someone trying to leave). Distinguishes dependants from partners, states plainly that sudden departure is sometimes the only safe departure, and closes with the clause's own built-in defense against being weaponized: *"it is not available to anyone who wants to use it to keep you."* Links to Resources for anyone unsure whether leaving is safe.

Notes:
- **Shipped in the current JS-array template format, ahead of RS-004.** The Phase 1 sequencing note in `tasks.md` originally grouped RS-030 with RS-028+005 as "after RS-004." Revised that note: RS-030 is one self-contained clause (a single new section, no existing array touched), so the authored-twice risk the sequencing note exists to avoid doesn't really apply here the way it does to RS-028+005, which edits the Practise tool's domain array and the Learn principles array both.
- Placement (`Learn.dc.html`, directly after the principles grid) was a judgment call — the draft said "Manifesto or Learn" without specifying where on either page. Chosen because the clause explicitly functions as a tiebreaker between two of the principles just above it, and burying it lower on the page (e.g. in the closing "cautions" section) seemed likely to bury a P0-priority safety clause with everything else.
- `npm run check` passes clean on the updated page.

### RS-004 (partial: Manifesto) — Hugo migration begun
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: Manifesto.dc.html needed the dc-runtime to draw its content; scripting off showed an empty shell~~
now: `hugo/` toolchain stood up (Hugo v0.164.0, verified against both `github.com/gohugoio/hugo/releases` and `gohugo.io`'s own install docs, then actually installed and run in this environment via `go install github.com/gohugoio/hugo@v0.164.0` — three independent confirmations of the same version, not a guess). `Manifesto.dc.html` is now generated by Hugo from `hugo/layouts/manifesto.html` (plain Go template, no data file needed — the fifteen theses were already hand-written HTML in the old template, not a JS array, so this page's migration is a faithful "strip the runtime wrapper" port, confirmed by diffing old vs. new: the only changes are removing `<x-dc>`/`<helmet>`/`support.js`/the trailing `<script type="text/x-dc">` block, and replacing `onClick="{{ print }}"` with a plain `onclick="window.print()"`). Verified rendering fully with JavaScript disabled via a real headless-browser check (not just inspecting the source), not merely assumed from the absence of `{{ }}` placeholders.

New `npm run build:hugo` (`scripts/build-hugo.mjs`) runs Hugo and copies its output over the matching root-level file(s) — currently just `Manifesto.dc.html`, tracked in `build-hugo.mjs`'s `HUGO_PAGES` list, which is the single place to add the next page as it migrates. `npm run check` gained a `checkHugoPagesInSync` step (mirrors the existing `index.html`-matches-`Home.dc.html` check): regenerates without writing and diffs against the committed file, so a hand-edit to `Manifesto.dc.html` or a `hugo/` change that never got rebuilt shows up as a CI failure instead of silent drift. `.github/workflows/check-pages.yml` installs the pinned Hugo version via the Go toolchain so this check runs for real in CI, not just locally. `prerender.mjs` moved `Manifesto.dc.html` from `PAGES` (dc-runtime prerendering) to `COPY_AS_IS`, same treatment as `Resources.dc.html`.

`BehindTheScenes.dc.html` updated to describe this accurately as **partial**: fault 4's body, the `Build` and `Durability` substrate rows, and a new changelog entry all say Manifesto is done and Learn/Archive/this page are not — fault 4 itself stays open, per D2's own instruction to retire it only "when this lands" (all four pages, not one).

Notes:
- **Deploy-time behavior is unchanged.** Per D2, Hugo's output is committed and *is* the shipped artifact — `deploy.yml` doesn't run Hugo and doesn't need to; it already copies whatever `Manifesto.dc.html` is currently sitting at the repo root, same as any other file.
- Chose the Go-toolchain install method (`go install`) over a prebuilt-binary download because direct `github.com` fetches are blocked in this session's network policy (proxy 403), while the Go module proxy was reachable — documented as one of several valid install methods in `hugo/README.md`, with an explicit warning against using Ubuntu's `apt` package (0.123.7 at time of writing, several major versions behind).
- Remaining pages (`Learn`, `Archive`, `BehindTheScenes` itself) are real migrations, not ports — they're genuinely `sc-for`-driven from JS arrays today and need actual Hugo data files + range/if templates, which is why this shipped as its own checkpoint rather than waiting to land all four at once.

### RS-004 (partial: Learn) — Hugo migration continues
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: Learn.dc.html's twelve principles, opacity table, and topics list lived in a JS array; scripting off showed an empty shell~~
now: `Learn.dc.html` generated by Hugo from three new data files — `hugo/data/principles.yaml` (12 entries), `hugo/data/opacity.yaml` (4), `hugo/data/topics.yaml` (19, `drafted: true/false` computed into label + colour via the template's `cond` calls rather than stored twice) — through `hugo/layouts/learn.html`, using `{{ range }}` in place of `sc-for`. Added to `build-hugo.mjs`'s `HUGO_PAGES` and moved out of `prerender.mjs`'s `PAGES` into `COPY_AS_IS`, same as Manifesto.

Verification, not assertion: after the build, counted list items in the generated output against the source data (12 principle `<li>`s, 4 opacity `<div>`s, 19 topic `<div>`s — all matched), spot-checked first/last entries in each list landed in the right place with the right drafted/not-drafted styling, then loaded the page with JavaScript disabled in a real headless browser and confirmed the principle text, the Continuity of care clause, an opacity-table entry, and a topics-table entry are all in `document.body.innerText`.

`BehindTheScenes.dc.html` fault 4, `Build`/`Durability` substrate rows, and the changelog updated again: now says Manifesto *and* Learn are migrated, Archive and itself are not. Also fixed a deprecation Hugo warned about — `site.Data` is deprecated as of Hugo v0.156; both `manifesto.html` and `learn.html` use `hugo.Data`.

Notes:
- Two of four reading pages done. Archive (~30 entries across groups, plus the dual-axis diagram noted in fault 05) and BehindTheScenes itself (substrate/glyph/crawler/fault/changelog tables) remain — both larger and more structurally involved than Manifesto or Learn, since neither is a simple "strip the wrapper" port.

### RS-004 (partial: BehindTheScenes) — Hugo migration, three of four pages
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: BehindTheScenes.dc.html's substrate, glyph, crawler, terms, fault, and change-log tables lived in JS arrays; scripting off showed an empty shell~~
now: generated by Hugo from six new data files — `substrate.yaml` (8), `glyphs.yaml` (7), `crawler.yaml` (5), `terms.yaml` (7 plain strings), `faults.yaml` (6), and `changelog.yaml` (2 version blocks, 15 entries total, structured as nested YAML rather than the original array-of-two-tuples so the "was"/"now" fields are named instead of positional). The changelog file's own header comment states the append-only rule explicitly: past entries are not edited even when a later entry supersedes what they say, matching the page's own stated practice of showing corrections rather than overwriting them.

Verification: counted every list's rendered items against its source count (8/7/5/7/6/2-with-15 — all matched), spot-checked boundary entries (first and last changelog blocks, the fault-06 entry, the "no jurisdiction" closing term), then confirmed full rendering with JavaScript disabled in a real headless browser.

One rendering quirk investigated and confirmed harmless: Go's `html/template` (which Hugo's templates compile to) renders the literal `+` in "U+0302" as the numeric entity `&#43;` in the HTML source rather than a literal character. Verified with a minimal Playwright page that this displays and reads identically to a literal `+` — not a bug, just a different valid encoding of the same character.

Fault 4, the `Build`/`Durability` substrate rows, and a new changelog entry all updated in the same pass to say three of four reading pages are done — this page describing its own migration in its own body text, which is on-brand for a page whose entire premise is "a site arguing against domination cannot keep its own workings private."

Notes:
- Archive is the only remaining reading page — the largest, with ~30 entries across groups and the dual-axis "shared jurisdiction" diagram fault 05 already flags as unfinished. Practise stays on the runtime permanently (it's a tool, not a reading page) — see the Phase 1 sequencing note in `tasks.md`.
- Once Archive lands, fault 4 retires per D2's original instruction ("retire fault 04 when this lands") — that's the next and last piece of RS-004's core scope.

### RS-004 (complete) — Hugo migration finished, fault 04 retired
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: Archive.dc.html's ~30 entries, the client-side tag filter, the "Held in common" shared-jurisdiction diagram, the top-ten reading sequence, and the access-states list all lived in JS arrays; scripting off showed an empty shell~~
now: generated by Hugo from `hugo/data/archive.yaml` — the largest single data file in the migration (6 groups, 31 entries, every field transcribed and independently recounted against the original JS array: 31 items via three different grep patterns, 6 groups, 5+5 trackA/trackB, 4 shared, 10 sequence, 7 access states — all matched before this was trusted).

**The interesting design decision on this page:** Archive is the one migrated page with genuine client-side interactivity (a tag filter — click "toolkits" and everything else hides). Rather than drop the filter or leave it dead, it ships as a small vanilla-JS progressive enhancement (~25 lines, no framework, in the generated page itself) layered on top of a page that is already 100% complete without it: every one of the 31 entries renders, unfiltered, by default. Verified both halves separately in a real headless browser: with JavaScript disabled, all 31 `<article>` elements are present and the first/last groups and the "Held in common" diagram all read correctly; with JavaScript enabled, clicking the "toolkits" filter narrows 31 visible entries down to exactly 1 (the Creative Interventions toolkit) and clicking "everything" restores all 31, with zero console errors either way. This is the pattern D2 actually asks for — "renders without JS" doesn't mean "no JS ever," it means content isn't gated behind JS, and enhancement is layered on top rather than load-bearing.

**Fault 4 retired**, per D2's own instruction to do so only once this landed. Removed from `hugo/data/faults.yaml` (not kept-and-marked-resolved — the fault list's job is current problems, the changelog's job is history, and it has the full record) and the remaining faults renumbered 01–05. Caught and fixed a cross-reference this renumbering broke: Archive's own "Two Row Wampum, as layout" note pointed to "fault 05" by number, which shifted to fault 04 — would have been a live wrong citation on the page itself if missed. The substrate `Build` and `Durability` rows now state completion plainly (green, not amber) rather than partial. A final changelog entry uses close to D2's own suggested wording, adapted to name what actually shipped (the filter enhancement, the verification method) rather than the generic draft.

Notes:
- **This closes RS-004's core scope.** `Practise.dc.html` deliberately keeps the dc-runtime permanently — it's an interactive tool, not a reading page, and the Phase 1 sequencing note in `tasks.md` never proposed migrating it.
- `prerender.mjs`'s `PAGES` array (dc-runtime prerendering) now holds only `index.html`, `Home.dc.html`, `Practise.dc.html`, `Contribute.dc.html` — down from eight pages at the start of this session. Its top-of-file comment updated to stop citing stale "79%/59%/52% of text missing" figures for pages that no longer have that problem.
- RS-028+005 (Access Intimacy domain + principle 03/13 revision) was explicitly sequenced in `tasks.md` to land *after* RS-004, specifically so its Learn-page content wouldn't need authoring twice (once in the old JS array, once again during Hugo migration). That condition is now met — RS-028+005 can proceed directly against `hugo/data/principles.yaml`.

### RS-028 + RS-005 (+ D3) — Access Intimacy domain, thirteen principles
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: twelve principles written for a person who can choose, refuse, and exit a relationship, in a framework already citing Kittay, Mingus, Piepzna-Samarasinha, and Sins Invalid on dependency and care; the Consent Domains Map had a single generic "Caregiving" domain and no principle occupied the carer's own position~~
now: **Learn** carries thirteen principles — principle 03 revised per the exact `decision-record-d1-d15.md` D3 draft (title drops "chosen," body names guardianship, congregate care, and dependency that cannot be left; the question is unchanged, since it already worked for both cases), and new principle 13, "The carer is inside the relationship," closing the gap thesis 07 ("care must not become a caste system") already named but no principle carried. **Practise** gains an Access Intimacy & Body Support section on the Consent Domains Map: three sub-domains (Body support; Administrative & logistical care; Emotional & relational care) per the table in `docs/spec/addendum-a.md` RS-028, using the tool's existing five-option scale and free-text condition field rather than inventing a parallel input system — the "both positions must be representable" requirement is satisfied by hint text naming both the cared-for's and the carer's concerns (e.g. "who relieves me" is exactly what the free-text field is for), not by a second UI. Domain-level intro copy used verbatim from the spec's draft. Global find-replace: "twelve principles" → "thirteen principles" (Learn's title/og:title/intro, Home's nav card and feature list, `index.html` kept byte-identical).

Notes:
- **Data model, not UI, changed for Practise.** The three new sub-domains are appended to the same flat `domainList` array (indices 17–19), sharing one `picks`/`notes` state object with the original seventeen — `renderVals()` now computes the full twenty-item render once and slices it into `domains` (0–16, unchanged section) and `accessDomains` (17–19, new section), so "Print this map" and "Clear everything" apply uniformly across both without new state-management code. Verified interactively in a real browser: clicked a radio in the new "Body support" domain, confirmed the `marked:` state label updated, filled the condition field, clicked print (no error), then clicked "Clear everything" and confirmed both the pick and the note field reset — not just read from the source.
- **Heading precision, not just find-replace.** The original "The seventeen domains" heading was deliberately *not* renamed — it still introduces exactly seventeen items — while the page's overview text (meta description, og:description, intro paragraph) was updated to "twenty domains" since that's the tool's total scope. The new section gets its own heading, "Access Intimacy & Body Support," rather than being folded silently into the "seventeen" count. Each heading stays literally accurate to what immediately follows it.
- `npm run build:hugo` + full `npm run build && npm run check` pass clean; `BehindTheScenes.dc.html`'s changelog carries the was/now entry.
- This was the item Phase 1's sequencing note held back until RS-004 landed, specifically so its Learn-page content wouldn't be authored twice (once in the old JS array, once during Hugo migration). Authored directly against `hugo/data/principles.yaml` — no duplication occurred.

### RS-020 — CSP + security headers, machine-enforced
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: no Content-Security-Policy anywhere on the site, and two inline `<script>` blocks (Archive's tag filter, glyph-check's font test) that a strict policy would have had to carve an exception for~~
now: every page carries a `Content-Security-Policy` `<meta>` tag, scoped to what that specific page actually does rather than one blanket policy copy-pasted everywhere — four tiers: `Resources.dc.html` (`script-src 'none'`, it has zero JavaScript), the four Hugo-generated reading pages plus `glyph-check.html` (`script-src 'self'`, same-origin script only, no `unsafe-eval` needed since nothing on them calls `eval`/`new Function`), `Practise.dc.html` (`script-src 'self' 'unsafe-eval'`, needed for the `dc-runtime`'s `new Function()` template evaluation), and `Home.dc.html`/`index.html`/`Contribute.dc.html` (same as Practise, plus `connect-src` naming the dispatch Worker's origin specifically, since those two pages' signup forms are the one legitimate cross-origin `fetch` on the site). New `scripts/check-origins.mjs`, wired into `npm run check`, fails the build if any page loads a script, stylesheet, or CSS `@import`/`url()` from off `relationalsovereignty.com` — outbound `<a href>` prose links are explicitly exempt, since Archive and Resources exist specifically to link away.

**Both of the site's remaining inline `<script>` blocks were externalized first**, specifically so no page's `script-src` ever needed `'unsafe-inline'`: Archive's filter became `archive-filter.js`, glyph-check's font-measurement code became `glyph-check.js`. Verified each still works after the move (Archive's filter click-tested again: 31 visible → 1 after the "toolkit" filter → 31 after "everything," zero errors; glyph-check still populates its output).

**Verification method matters here more than most tasks**: rather than just asserting the policies are correct, `npm run check` was run with every CSP live — since Chromium reports CSP violations as console errors, and `check-pages.mjs` already fails on any console error on page load, a wrong policy (missing `unsafe-eval` where needed, a typo in the Worker origin, etc.) would have shown up as a real, automatic failure. All ten pages passed clean, which is real evidence the policies are neither too strict (breaking real functionality) nor too loose (allowing something CSP should catch) — not just confidence that they look right on paper.

**One real, disclosed gap: no clickjacking protection.** `frame-ancestors` is one of a small number of CSP directives the specification itself refuses to honor when delivered via `<meta>` rather than a real HTTP response header — this isn't a choice or an oversight, it's a hard limit of the delivery mechanism GitHub Pages (the current host) forces. New fault 06 in `BehindTheScenes.dc.html` names this plainly rather than shipping a CSP that implies more protection than it delivers. Closing it requires the RS-022 hosting decision (FLAG-02, still unconfirmed) to land on a host that can set response headers.

Notes:
- Shipped **without waiting for RS-022/FLAG-02** — scoped explicitly to the current host (GitHub Pages, meta-tag delivery) per the task's own original framing in `tasks.md`. If/when hosting moves to something that can set real headers, this becomes a header-delivery change plus closing fault 06, not a from-scratch redesign — the actual policy content carries over.
- SUGGEST-05 (connect-src carve-out for the dispatch Worker) is resolved by this work, struck in `tasks.md`. SUGGEST-03 (dormant Babel/unpkg CDN path in `support.js`) is now directly enforced rather than just theoretical: that code path isn't allow-listed anywhere, so triggering it would hit a CSP violation instead of silently working — the dead-code question stays open, but the CSP no longer has to carry an exception for it.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-023 (partial) — accessibility acceptance pass, the automatable half
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: the site's own 44px touch-target standard (cited in its own changelog: "34-pixel colour-only swatches... now: labelled radio controls, 44-pixel targets") held for the Consent Domains Map but not for the primary or footer navigation, whose links were ~20px tall everywhere, nor for Archive's 31 per-entry citation links — the page's actual purpose, styled identically to the undersized nav; the Held in Common diagram's three lists had no programmatic connection to their visual column headers~~
now: every nav link (primary + footer, all pages) and every Archive citation link meets 44px, fixed with a single consistent pattern (`display:inline-flex;align-items:center;min-height:44px;padding:...`) applied via `sed` across every `.dc.html`/`.html` file and Hugo layout for the four distinct link-color variants used site-wide, then verified — not assumed — by re-running `responsive-audit.mjs` at 320/375/768/1280/1920px and confirming the flagged nav/citation links dropped out of the report entirely. What's left flagged (`Mukurtu CMS`, `crawler policy`, `fault 04`, etc.) is genuinely inline-in-a-sentence prose links, which both WCAG 2.5.8 and the audit script's own documented exception treat as legitimate — left alone rather than padded into looking like buttons they aren't.

Archive's Held in Common diagram: the two outer lists (`trackA`/`trackB`) gained `aria-labelledby` pointing at their existing visible column-header text; the middle ("shared") list had no visible heading to point to, so it gets a new visually-hidden `<p>` labeled "Held in common by both traditions" plus `aria-labelledby` referencing it. **First attempt was wrong and the error was real**: `aria-label` directly on the middle `<ul>` failed `html-validate`'s `aria-label-misuse` check; a follow-up guess (`role="list"` to "fix" it) produced a *different* real error, `no-redundant-role` (`<ul>` already has an implicit list role); the actual fix — a hidden label element plus `aria-labelledby`, matching what already worked on the other two lists — was verified two ways: `npm run check` passing clean, and a separate script that resolves each list's `aria-labelledby` in a live DOM and confirms the right text and item count (5/4/5) comes back.

Keyboard focus order verified end to end on the Practise safety→grounding→tool sequence with real `Tab` key presses in a live browser: focus-visible outline present (2px solid, not suppressed), and — the one that actually matters — activating the skip link puts the very next `Tab` on the "Continue" button, not somewhere in the seven-item header nav a keyboard user would otherwise have to traverse first. Honeypot field (Home + Contribute dispatch forms) checked and already correct: `aria-hidden` on the wrapping label, `tabindex="-1"`, `autocomplete="off"`, positioned off-screen rather than `display:none` (deliberately, so it still functions as bot bait) — no change needed.

Notes — **what this is not**:
- **No screen reader was run.** NVDA, JAWS, VoiceOver, and TalkBack all require real assistive technology this session has no access to. Everything above is automatable (axe-core, html-validate, a real headless browser exercising real keyboard events and DOM state) — genuine, verified engineering, but it is not a substitute for a person listening to the page. `tasks.md`'s residual RS-023 row says so and stays open until someone does that pass.
- Print stylesheet: `@media print` rules were spot-checked as applying correctly, but "does the printed page actually read well" needs a human looking at real paper or a real PDF, not just confirming a CSS rule matched.
- RS-026 (non-verbal/reduced-language state-control standard) doesn't exist yet — deliberately deferred to Phase 3, not silently dropped.
- `npm run build:hugo && npm run build && npm run check` and `node responsive-audit.mjs` all pass clean on the final state.

### RS-003 (+ D4) — Drop "survivance" from thesis 14
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: Manifesto thesis 14 titled "Survivance, not 'no future'," transposing Gerald Vizenor's Nation-specific concept of Native active presence onto the site's own claim about relational futures, with the Archive's Vizenor entry captioning the book itself as "the answer to punk's no future"~~
now: thesis 14 retitled "Regeneration, not endurance," body revised to match — "We are not here only to outlast damaged forms of connection. We are here to make new ones" replaces the earlier "survive... regenerate it" language that leaned on the borrowed term without saying so. Pull-line ("There is a future, and it will be relational.") is unchanged, since it never used the term. Both the heading and body match `decision-record-d1-d15.md` D4's draft text verbatim, not a paraphrase.

**D4 resolves this in favor of Option B (remove), not Option A (argue the transposition)** — the base work order called the two options "either is defensible," but D4's reasoning is that arguing the transposition would require the site to make a public claim about the proper extension of a Nation-specific concept, which is exactly the category of claim fault 03 already names as unreviewed and awaiting paid Indigenous review. Removal costs nothing if the replacement holds on its own terms, and it does — the new thesis argues the same structural point (colonial relationships reproduce by claiming there's no alternative) without needing anyone else's specific vocabulary to make it.

Vizenor stays in the Archive on his own terms: the entry's `why` field drops "The answer to punk's no future" (the line doing the actual borrowing) and is replaced with `base-work-order.md`'s own suggested text, "Read for what it argues, not for what it evokes" — same citation, same book, no longer captioned as a metaphor for this site's argument.

**One item beyond the two files D4 and the base work order name directly:** the not-yet-drafted Learn topic "Survivance and relational futurity" (`hugo/data/topics.yaml`) used the same borrowed term for the same undecided claim. The base work order says to update it "if its status changes" (i.e. when it moves from undrafted to drafted) — but leaving the placeholder title itself carrying the term seemed like leaving the borrowing in place under a different heading, just deferred rather than resolved. Renamed to "Regeneration and relational futurity" to match the thesis's own replacement language. No content exists yet at this topic, so nothing else needed to change.

**Logged in the changelog per D4's own suggested wording** (adapted only to match the site's established was/now format rather than the inline strikethrough D4 drafted it in): "was: thesis 14 titled 'Survivance, not "no future"', using a Nation-specific concept for injuries it was not written about" / "now: the term removed until there is paid review that could say whether the extension holds. The archive still carries Vizenor. The debt was real; the borrowing was not ours to make."

Verified with a real headless browser, JavaScript disabled, reading both regenerated pages directly: `Manifesto.dc.html` contains "Regeneration, not endurance" and "outlast damaged forms" and does not contain the string "Survivance" anywhere; `Archive.dc.html` contains "Read for what it argues" and does not contain "punk's no future" — not just a text diff against the source data files, but confirmation of what a no-JS reader actually receives.

Notes:
- This does not touch `docs/spec/base-work-order.md` or `docs/spec/decision-record-d1-d15.md` — both are historical source documents recording the original problem and its resolution, and stay as written, same as every other spec reference this session.
- Fault 03 (unreviewed claims about Indigenous concepts) is not retired by this change — it's the reason this change exists. The fault stays open until the paid review D4 anticipates actually happens; if it does, D4 explicitly leaves Option A revisitable.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-021 (rescoped, per D5 amended) — Disclose the dispatch Worker architecture
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: the dispatch signup forms on Home and Contribute told readers an address is "never sold, traded, or given to a third party" and "no third parties," full stop — while a live Cloudflare Worker, a Resend transactional-email account, and a private GitHub repository were already the real infrastructure behind every signup, none of it named anywhere on the site~~
now: four new rows in the substrate table on `BehindTheScenes.dc.html` name the whole chain plainly: **Mailing-list processor** (a self-operated Cloudflare Worker, not a third-party platform, pointing to `worker/` in this repo as the source), **Transactional email** (Resend sends the confirmation and unsubscribe messages only — plain text, no tracking pixel, no click-tracking redirect — and never receives the interests picked at signup or the list itself), **Where the list lives** (a private, operator-controlled GitHub repository, separate from this public one, holding one file encrypted with AES-256-GCM; the key exists only as a Worker secret and is never written to any repository), and **What's actually stored** (per subscriber: the confirmed address and confirmation time; interests are recorded only as a site-wide count, never attached to an address). The existing **Third-party requests** row is amended too — it claimed "zero... nothing on this site phones anywhere," which stopped being true the moment the dispatch endpoint existed; it now names the one exception explicitly rather than leaving a blanket claim standing next to its own contradiction three rows down.

**This is disclosure, not a rebuild.** Per the D5 amendment already logged in `tasks.md`'s resolved-decisions log (2026-08-07, explicit author instruction to keep the existing processor rather than replace it with RSS+mailto), the underlying system doesn't change — it was already closer to D5's own "self-hosted list + own SMTP" option than to the email-service-provider pattern D5 was originally written against. Reading `worker/src/store.js` and `worker/src/email.js` directly (not just the architecture description in `tasks.md`) confirmed the claims made here are literally true of the code that runs: `store.js`'s own comment states interests are "never stored next to their email... a leaked or subpoenaed copy of this file should say '43 people are interested in X' and nothing about who," and `email.js` sends via Resend's bare HTTP API with no HTML, no tracking parameters.

Home's dispatch section and Contribute's dispatch form both replace their absolute "never... third party" claims with a shorter, accurate statement plus a pointer to the colophon for the full picture, rather than trying to compress four paragraphs of real architecture into a footer line.

Verified with a real headless browser, JavaScript disabled: `Home.dc.html` no longer contains the string "never sold, traded, or given to a third party" and does contain the replacement sentence naming the transactional email service; `BehindTheScenes.dc.html` contains all four new row headings and the string "AES-256-GCM" — not just a source-file diff, confirmation of what actually reaches a no-JS reader.

Notes:
- `index.html` was re-synced byte-for-byte from `Home.dc.html` after the edit (the established pattern for every change touching Home) — `npm run check`'s "index.html matches Home.dc.html" comparison confirms it.
- The Worker's operator Cloudflare-account email and other credential-adjacent operational detail from `worker/README.md` were deliberately left out of the public substrate copy — the disclosure obligation D5 identifies is about what happens to a reader's data and who can see it, not about publishing operational secrets.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-015 — Archive link-quality pass
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: the change log claimed "every archive entry links to the author, publisher, or open-access source" — true of the domain, not the page. Roughly twenty of the Archive's 31 entries linked to a publisher's generic homepage (`global.oup.com`, `dukeupress.edu`, `upress.umn.edu`, `akpress.org`, `nyupress.org`, `routledge.com`, `arsenalpulp.com`, `press.umich.edu`, `nebraskapress.unl.edu`, `sinsinvalid.org`, `criticalpolyamorist.com`, `schneier.com/academic/`, `leavingevidence.wordpress.com`) rather than the specific book or article, and two entries — Wildcat & Voth, and Garneau — had no link at all~~
now: every one of the 31 entries was checked individually against `docs/spec/base-work-order.md` §4's verification procedure — resolve the work on the publisher's own site or via DOI, never construct a URL from a pattern, keep the no-link honesty if nothing stable exists — and every candidate URL was actually fetched (via `WebFetch`/`WebSearch`, not assumed) before it went into `hugo/data/archive.yaml`. Twenty-one links were repointed to the specific work's own page; three real findings came out of the process rather than just tidying:

- **Consent's Revenge (Audra Simpson)** was tagged "free"/"open access" but linked to a Wiley AnthroSource mirror that returns **402 Payment Required** on a live fetch — a false "free" claim that predates this session. Repointed to Cultural Anthropology's own journal site (`journal.culanth.org`), which is genuinely open access and is the article's authoritative home.
- **Indigenous Relationality (Wildcat & Voth)** had no link recorded because none was believed to exist. OpenAlex's structured metadata (cross-checked directly, not just cited by a subagent) shows it is in fact open access under a CC-BY-NC license, with a real, live PDF at Sage. A real link exists now where the honest answer used to be "none." Tagged `free` to match.
- **Imaginary Spaces of Conciliation and Reconciliation (Garneau)** likewise had no recorded link; resolved to Wilfrid Laurier University Press's page for the *Arts of Engagement* anthology, with the table of contents fetched directly to confirm Garneau's chapter (pp. 21–42) is actually in it.

Three additional accuracy fixes the source doc's §4 flagged alongside the link work, each independently verified before applying: **Wildcat & Voth**'s `use` field now carries the actual citation (*AlterNative* 19(2): 475–483, 2023) instead of the generic "peer-reviewed"; **Glissant**'s `by` field now credits translator Betsy Wing, confirmed on the University of Michigan Press page itself; **Wildcat**'s *Replacing Exclusive Sovereignty* was repointed past both the current author's-page link and the source doc's optional JSTOR suggestion (paywalled) to a genuinely free DOI copy (`doi.org/10.21307/borderlands-2020-014`) — a better outcome than either option on file, found because the entry's own `free` tag was taken as a constraint to satisfy, not just a label to leave alone. One suggestion from the source doc was deliberately **not** taken: noting Vizenor's 1994 Wesleyan first edition on *Manifest Manners* — checked, and that edition actually carries a different subtitle ("Postindian *Warriors* of Survivance" vs. the 1999 Nebraska reprint's "Postindian *Narratives* of Survivance," which is the exact title already in this entry) — noting it risked implying they're a more interchangeable printing history than they are, so the entry links cleanly to the edition it actually names.

**Research was split across two parallel background agents** (one per roughly half the entries) given the sheer number of independent web lookups involved — but every fetch a subagent reported was either spot-checked directly before being trusted (the three findings above, plus the Nebraska Press and Duke UP pages that blocked bot fetches directly and were corroborated via WebSearch/Wayback Machine before being used) or came back from a page render the agent itself had already visually confirmed matched title and author. Two entries in the source doc's own table — Brake's *Minimizing Marriage* and Stark's *Coercive Control* — were missed by both agents' assigned batches and caught only by re-reading the full entry list against `archive.yaml` afterward; both were then verified directly.

Also fixed, per the source doc's own instruction: the change-log overstatement itself. Rather than editing the original v0.1-era changelog entry (this site's changelog is append-only, per its own header comment — "an entry noting a page 'has not migrated yet' stays as written once a later entry documents that migration"), a new entry documents the correction in the same was/now voice.

Verified with `npm run check` (all 31 citation links confirmed absolute, none relative to site root — the template bug the source doc asked to rule out doesn't exist, `{{ if .link }}` already emits a plain `<a href="{{ .link }}">`) and a real headless browser with JavaScript disabled reading `Archive.dc.html` directly: all 31 citation links render, and a sample of the newly-added URLs (Nebraska Press, AK Press, Sins Invalid, Duke UP, Temple University Press, kimtallbear.com, WLU Press) are present in the no-JS DOM.

Notes:
- Every entry that previously had an empty `it.link` (Wildcat & Voth, TallBear's *Caretaking Relations*, Garneau) now has a real, verified one — there are no `it.link: ""` entries left anywhere in the file, confirmed by scanning for empty or bare-domain links after the edit. The site's "no-link honesty" practice (name the holder rather than guess a URL) stays intact as a rule; it just turned out to have zero live instances left once each entry was actually checked rather than assumed unfindable.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-024 (partial) — glyph check methodology fix
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: `glyph-check.html` claimed each character was "measured against the notdef box... checked not assumed," using `canvas.measureText().width` to compare a character's advance width against the .notdef glyph's. That claim was false for the `mono` stack specifically: a true monospace font gives every character — present or missing — the exact same fixed advance width by design, so width comparison can never tell a real glyph from a tofu box there. Running the original check in this session's own sandbox proved it: every single sample character in the `mono` stack, including plain ASCII `A` and a bare space, measured as "MISSING," identical to the .notdef box. The flagged-at-risk `x̂` (Unangax̂ combining circumflex) — which renders live in that exact `mono` stack on Archive's by-lines — was never actually resolved one way or the other. The check just couldn't answer~~
now: `glyph-check.js` renders each character and the .notdef glyph to an offscreen canvas and compares actual pixel data, not advance width — a method that works regardless of whether the font is proportional or monospace. Re-run in this session against the one platform actually available (Chrome / Linux): every character across all three font stacks, `x̂` included, is confirmed present, not assumed. `glyphs.yaml`'s `x̂` row changes from "at-risk" (`ok: false`) to confirmed (`ok: true`), with the entry's own text explaining why: the earlier flag was the check being unable to answer, not evidence of an actual problem.

**This was not the task as scoped.** RS-024 asked for the coverage matrix to extend across real Safari/macOS+iOS, Firefox, Chrome/Android, and Edge/Windows, with published per-platform results. None of that happened — this sandbox has exactly one browser engine (Chromium) on exactly one OS (Linux), and no amount of Playwright configuration substitutes for real ClearType font rendering on Windows or CoreText on macOS. What shipped instead is upstream of that gap: the check itself had a real, previously undiscovered defect that would have produced false data on every platform, monospace-stack included, no matter who ran it or where. Fixing the check makes any future platform pass actually trustworthy; running the check on a platform that was never going to give a truthful answer would not have been.

Notes:
- **`x̂`'s new "confirmed" status is scoped to what was actually tested.** It is not a claim that Windows or macOS font stacks render it correctly — those remain genuinely unverified, same as before. The colophon copy says this explicitly rather than letting a green checkmark imply more than one platform's worth of evidence.
- The residual — real Safari/macOS+iOS, Windows ClearType, Chrome/Android, Edge testing — stays open in `tasks.md`, framed the same way as RS-023's screen-reader residual: it needs a person with access to that hardware, not more work in this environment.
- `npm run build:hugo && npm run build && npm run check` all pass clean, including `glyph-check.html` itself (console-error capture confirms the rewritten pixel-comparison script runs without error).

### RS-027 (+ D14) — async mode and export for the Consent Domains Map
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: the Consent Domains Map had to be completed in one browser sitting — no way to save progress and pick it back up, a stamina requirement addendum-a.md's RS-027 draft argues the framework has no standing to impose~~
now: an explicit, file-based save and resume, built to the safety condition addendum-a.md says the original spec omitted — export is a discoverable artifact, and the feature has to say so before it creates one, not after. "Save to a file" sits below Print (D14: print offered before file download) and is never triggered automatically; a non-dismissible warning in the site's rust warning color sits directly above the button, using the spec's own draft copy verbatim (*"Saving this makes it findable..."*), not a dismissible tooltip a person could close and forget. The exported file is plain JSON — `who`, `picks`, `notes` — downloaded via a `Blob`/`URL.createObjectURL`, never sent anywhere. Resuming works only by re-importing that file; there is no `localStorage` anywhere in `Practise.dc.html`, so the same risk the export warning names never exists silently, without the warning attached to it.

**Filenames are neutral by default and user-editable**: `notes-YYYY-MM-DD.json`, computed from the real date, containing none of "relationship," "consent," or the site name, per the spec's explicit requirement — and a missing `.json` extension is appended automatically if the person retypes the field. **Print title is blanked around the print call** (`document.title` cleared before `window.print()`, restored on the `afterprint` event with a 4-second timeout fallback for browsers that don't fire it reliably) — the one part of a browser's print header page code can actually influence. The one part it genuinely can't (the URL/date a browser may add to a print footer) is disclosed on the page itself rather than silently left as an implied guarantee: *"Your browser may add its own header or footer to a printed page, including this page's address. If that matters, check your print dialog's header and footer setting before printing."*

**"Crip time" is attributed, not used as ambient vocabulary**, per the spec's explicit instruction. Both citations were verified before use, the same way as RS-015's archive pass: Alison Kafer's *Feminist, Queer, Crip* (Indiana University Press, confirmed via the press's own page) and Ellen Samuels' "Six Ways of Looking at Crip Time" (*Disability Studies Quarterly* 37(3), 2017 — the live journal page is bot-walled against automated fetches, so the citation was cross-checked against Crossref's DOI registry directly rather than assumed from a search snippet). Both are new Archive entries, grouped with Kittay/Piepzna-Samarasinha/Sins Invalid. The on-page copy names Kafer inline and links to the archive rather than treating "crip time" as if it needed no source.

**Every interactive path was actually exercised, not just read off the markup** — real Playwright sessions against a local HTTP server (this dc-runtime app doesn't hydrate correctly from a bare `file://` URL, unlike the fully-static Hugo pages):
- **Export**: confirmed the non-dismissible warning renders, the default filename is neutral, a real download fires with the exact JSON content the tool state holds, and a filename typed without `.json` gets it appended.
- **Keyboard reachability** (the specific check addendum-a.md's RS-023 cross-reference calls for): tabbed from the "who" field through Print → Clear everything → the archive link → the filename field → "Save to a file," and pressing Enter on the button triggers a real download — not simulated, an actual `download` event captured by Playwright.
- **Import**: a valid exported file round-trips correctly (who/picks/notes all restored); a deliberately malformed file produces the on-page error message without crashing or silently wiping the state already in the tool.
- **Reset**: "Clear everything" also regenerates a fresh dated filename and clears any import error, not just the domain picks.
- **Print title**: stubbed `window.print` to observe `document.title` mid-call — confirmed blank during the call, confirmed restored once `afterprint` fires.

One mouse-click attempt against the export button hit a Playwright actionability timeout on the first try; a keyboard-driven activation of the same button worked immediately and a forced mouse click also worked and produced a correct download, which points to a Playwright scroll/timing quirk in this specific harness rather than a real defect — recorded here rather than silently retried into looking clean.

Notes:
- Full screen-reader listening (does AT actually announce the warning before the button, not just "is it in DOM order before it") has not been done by a person — same residual as RS-023, not claimed as verified here.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-007 — trans studies archive entry + principle 08 revision
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: principle 08 (Opacity) stated "intimacy does not require complete access or total legibility" with no distinction between a person declining to be known and a person being compelled to disclose themselves — to a clinician, a court, an employer, an insurer, a border agent — in order to receive care, money, status, or documents. The archive held no trans scholarship at all, despite trans care webs being, per the source doc's own framing, the most developed live practice of the relational-care argument this site makes~~
now: principle 08's body states the limit explicitly — opacity is a right against people who want more of you than they are owed, not against institutions that compel disclosure to release care, money, status, or documents; refusing to be read is a privilege unevenly distributed, and the framework should not mistake one for the other. A second question, appended to the existing one: "And where is this person already required to be legible to someone with power over them?" Both are `base-work-order.md`'s own draft text, used verbatim rather than paraphrased. Hil Malatino's *Trans Care* joins the archive in the "Relation, opacity, refusal, survivance" group — the placement, not just the citation, is the point: this is filed as the strongest correction to principle 08, not as a separate trans-studies afterthought bolted onto the end of an unrelated list.

**Re-verified rather than trusted on the source doc's own checkmark.** `base-work-order.md` §5 marks this entry "✓ verified," but this session's practice all along has been not to carry forward an unverified — or previously-verified-but-unrechecked — claim without checking it again. Fetched the University of Minnesota Press page directly: confirmed title, author, and that it's genuinely open access — the book is published in UMN Press's "Forerunners: Ideas First" series, whose whole model is a free Manifold digital edition alongside a paid print one, not just a marketing claim of openness. Tagged `free` in `archive.yaml` accordingly.

Notes:
- `question` in `principles.yaml`/the Learn layout is a single string field, not a list — the second question was appended to the first as a second sentence rather than dropped in as a separate list item the template doesn't support, since the spec offered "second question, or replacement" as the author's choice and appending preserves the original question's content rather than discarding it.
- This does not touch the `sequence` (reading-ten) list in `archive.yaml` — RS-008 is where Malatino's addition to that specific list is scoped, and that task hasn't shipped yet.
- Verified with a real headless browser, JavaScript disabled: `Learn.dc.html` contains the revised principle 08 body and the new second question; `Archive.dc.html` contains the Trans Care entry and Malatino's name — not just confirmed in the source YAML.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-006 (partial) — the second genealogy of possession
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: thesis 01 derived possession from a single road — settler colonialism enclosing land, then bodies, then the home — for a manifesto whose opening thesis is specifically about the grammar of possession, with no Black feminist theory of kinship and property anywhere in the archive~~
now: thesis 01 names the second road, inserted exactly where `base-work-order.md`'s draft places it — directly after "The grammar was never innocent," before the thesis's closing line: "There is more than one road into that grammar. Settler sovereignty made land into property and then made people into holdings. Chattel slavery made the person into property directly, and made kinship legally void — a different mechanism, a different afterlife, and one that still shapes which households are permitted a closed door. Both are here. Neither stands in for the other." A new archive group, "Possession, the person, and kinship made void," carries four entries: Hortense Spillers' "Mama's Baby, Papa's Maybe," Saidiya Hartman's *Scenes of Subjection*, Dorothy Roberts' *Torn Apart*, and Khiara M. Bridges' *The Poverty of Privacy Rights* — placed between "Decolonization, possession, land, kinship, intimacy" and "Relation, opacity, refusal, survivance," exactly as the spec directs.

**Every link was independently found and verified, not copied from the source doc's `[VERIFY]` placeholders** (all four entries in `base-work-order.md` §5 were literally unfilled — `link: "[VERIFY]"` or `link: "[VERIFY: ...]"`, deliberately left for implementation to resolve, not drafted guesses). Spillers' JSTOR stable link (`jstor.org/stable/464747`) was corroborated via a WebSearch result whose title snippet matches the exact citation (journal, volume, issue, pages) — JSTOR itself returns 403 to automated fetches, the same bot-blocking pattern hit repeatedly this session on academic-publisher sites, so this is cross-referenced confirmation rather than a direct render, disclosed as such rather than presented as equally certain to a directly-fetched page. Hartman's Oxford University Press page and Bridges' Stanford University Press page were confirmed the same way (both blocked automated fetches directly; both corroborated via independent search-result title matches against the exact URL). Roberts' Hachette/Basic Books page was fetched directly and confirmed clean.

**Patterson and Collins are not in this group, and stay out.** The source doc names both (`base-work-order.md` §5's entry list: "Spillers, Hartman, Patterson, Collins, Roberts, Bridges") but supplies zero metadata for either — no title, no `why`, no link, nothing to verify against — a gap the base-work-order.md's own familiarization note already flagged (tasks.md FLAG-03) as likely referring to Orlando Patterson's *Slavery and Social Death* and Patricia Hill Collins, but explicitly unconfirmed. Guessing which specific books belong here would repeat the exact error this site's own accountability practice exists to avoid — publishing an attribution nobody actually checked. The group ships with four verified names instead of a placeholder for six.

**Resolves the overlap this created with RS-016's P0 batch list** (which named Spillers/Hartman/Roberts/Bridges again, plus Malatino, already shipped via RS-007) — `tasks.md`'s RS-016 row is struck down to what's actually still unshipped from that set: Coulthard and Cohen.

Verified with a real headless browser, JavaScript disabled: `Manifesto.dc.html` contains the second-genealogy insertion; `Archive.dc.html` contains the new group heading and all four author names.

Notes:
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.
- FLAG-03 itself is not resolved by this — it's the reason this group ships partial rather than complete, and stays open in `tasks.md`'s Phase 0 flags until the author confirms the two missing works.

### RS-008 (+ D8, D9) — disambiguating "sovereignty," and the objection the site hasn't answered
**Shipped:** 2026-08-07 · **Commit/PR:** (pending)

~~was: the site used "sovereignty" in at least four distinct senses — political jurisdiction, jurisdictional questions about data and law, the site's own relational sense, and a loose rhetorical "not owned" — without ever marking which sense was doing the work in a given sentence, and the strongest objection to using the word at all (Taiaiake Alfred's argument that sovereignty is inseparable from the frameworks of colonisation) was never engaged anywhere on the site~~
now: a new Learn section, "Four things this word is doing," states all four senses plainly and names where the site's own persuasiveness depends on two of them (political and relational) rhyming without being the same kind of claim — using `base-work-order.md`'s exact draft text for all four definitions. A closing paragraph, sourced verbatim from `decision-record-d1-d15.md`'s D9, names Alfred's critique as "an objection this site has not answered" rather than a settled question, and states plainly why the word stays for now (Wildcat rebuilt it; the jurisdictional question is one "autonomy" cannot ask) while committing to drop it if paid review says otherwise.

**"That argument is on the shelf" is now literally true, not just a turn of phrase.** The paragraph links to the Archive, and Joanne Barker's edited collection *Sovereignty Matters* — which carries Alfred's own chapter making exactly this argument — joined the archive alongside it. Verified independently: the Nebraska Press page blocks automated fetches directly (the same bot-wall pattern hit repeatedly this session on academic-publisher sites), so the citation was corroborated via multiple independent sources (Amazon, Open Library, Project MUSE) all agreeing on title, editor, publisher, ISBN, and that Alfred's chapter — titled "Sovereignty," pp. 33–50 — is genuinely in the book. This also fulfills the "Barker" entry the RS-016 bulk-addition list was still carrying; struck there rather than left to duplicate.

**The reading-ten list changes for the first time since v0.1**, per D8's exact resolution: Mackenzie & Stoljar's edited collection steps aside for the Stanford Encyclopedia's free entry on the same content, already on the shelf but not previously in the top ten; Brake and Nordgren step aside — not off the shelf, still full archive entries — because amatonormativity and relationship anarchy are, in D8's own words, "the part of this framework people get wrong the least." In: Spillers (the second genealogy of possession, shipped this session via RS-006), the Stanford Encyclopedia entry, and Malatino (compelled legibility, shipped via RS-007) — meaning this task specifically depended on both of those landing first, which is why it shipped third rather than earlier in the phase. The standfirst copy states outright that six of the ten are now free to read, per D8's explicit instruction not to leave that as something a reader has to notice for themselves.

Verified with a real headless browser, JavaScript disabled: `Learn.dc.html` contains the four-senses section and the Alfred paragraph; `Archive.dc.html` contains the *Sovereignty Matters* entry and Barker's name; and — checked by extracting the rendered `<ol>` list itself, not just searching page text, since the old entries' titles legitimately still appear elsewhere on the page as regular archive items — the reading-ten sequence reads exactly as D8 specifies, in order.

Notes:
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-042 — Practise domain-label correction
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the Consent Domains Map's two section headings — "The seventeen domains" and "Access Intimacy & Body Support" — never stated that they add to twenty, leaving a reader to do the arithmetic against the page's own meta description ("mapping consent across twenty domains") instead of being told directly on the page~~
now: "Seventeen general consent domains" and "Three care and access domains: Access Intimacy & Body Support" — the split and the total are both explicit in the headings themselves. The Access Intimacy name (Mia Mingus's term, tied to principle 13 via RS-028) stays rather than being replaced outright by a purely numeric label — the second heading leads with the count, matching the first heading's structure, and keeps the named concept after it rather than losing it.

Verified with a real Playwright session against a local server (this dc-runtime page doesn't hydrate from a bare `file://` URL): navigated to the tool stage, confirmed both exact heading strings render, zero console errors.

Notes:
- First of Phase 2.5's priority items (implementation order 1 of the author's own sequence: RS-042 → RS-043 → RS-044 → RS-045 → RS-046 → RS-047/RS-048).
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-043 — Resources contents index
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Resources listed eleven categories in strict page order with no way to jump directly to the one a reader needed — crisis lines came first, Indigenous-led services sat near the bottom, and everything between was unnamed until a reader scrolled to it~~
now: a "Choose what you need" list of eleven ordinary same-page anchor links (`#crisis-lines`, `#coercive-control`, `#legal-aid`, `#immigration`, `#housing`, `#income`, `#disability`, `#lgbtq`, `#indigenous`, `#mutual-aid`, `#international`), placed **after** the immediate-safety questions rather than before them — a reader in danger sees those first, on their own, exactly as the page already presented them, not gated behind a menu. Every category on the actual page got its own anchor, not a condensed subset — the task's own gloss grouped "legal/immigration" and "housing/income/food" together, but the page already headed them as four separate sections, so the index mirrors the page's real structure rather than inventing a coarser one.

No geolocation, no questionnaire, no filtering, no storage of what anyone clicked — plain `<a href="#id">` links, nothing else. This page has zero script by design (`script-src 'none'` in its CSP) and stayed that way; the index needed none.

Verified with a real headless browser, JavaScript disabled (this page has no JS to disable, but the check confirms nothing here silently depends on any): all eleven anchor hrefs resolve to a real `id` on the page — checked programmatically, not eyeballed — and the safety-questions section still precedes the new index, which still precedes "Immediate safety and crisis lines."

Notes:
- Second of Phase 2.5's priority items (implementation order 2).
- `Resources.dc.html` is hand-authored, not Hugo-generated (no `hugo/layouts/resources.html` exists) — edited directly, consistent with how the page has always been maintained.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-044 — simplify the end of Learn
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Learn ended with a nineteen-card grid naming every planned topic, none linked, most not even drafted — visually the single largest block on the page, outweighing the thirteen principles, the four senses of sovereignty, and everything else actually written above it~~
now: two sentences ("Nineteen further topics are named but not one is linked... The full list, and why nothing on it is dated, lives on the roadmap") replace the grid entirely. The full nineteen-topic list didn't get deleted — it moved to a new **Roadmap** section on `BehindTheScenes.dc.html`, reusing the same `hugo/data/topics.yaml` (six drafted, thirteen not yet written) and the same drafted/not-yet-written color coding, so nothing about what's disclosed changed, only where the bulk of it sits. The new section also names why nothing on the list carries a date, echoing Practise's own "no dates, because a date we couldn't keep would be its own small domination" — same reasoning, now stated in both places it applies rather than once.

Verified with a real headless browser, JavaScript disabled: `Learn.dc.html` no longer contains the old grid's "not yet written" labels and does contain the new short explanation and a working link to `BehindTheScenes.dc.html#roadmap`; `BehindTheScenes.dc.html` has the new `#roadmap` anchor and, checked by querying the actual grid element rather than a whole-page text search (which produced a false negative on a first pass — the phrase "not yet written" also appears elsewhere on the long page, unrelated to the topics grid), renders exactly nineteen topic cards with the correct names.

Notes:
- Third of Phase 2.5's priority items (implementation order 3).
- RS-045 (Behind the Scenes contents menu, next in this phase) now needs to cover this new `#roadmap` section too — not in the author's original anchor list, since it didn't exist yet when that list was written. Noted on RS-045's row rather than silently expanding scope without saying so.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-045 — Behind the Scenes contents menu
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Behind the Scenes carried eight sections — substrate, type, crawler policy, reuse terms, faults, roadmap, change log, labour and money — as one unbroken scroll with no way to jump to any of them directly~~
now: a contents menu at the top of the page, eight ordinary `<a href="#id">` anchor links, one per section. `#roadmap` is included even though it wasn't in the author's original seven-item list — it didn't exist yet when that list was written, and shipped earlier this session via RS-044, landing on this exact page.

**The fault list stays exactly as visible as it always was.** The menu is pure navigation — no `<details>`, no collapse, nothing that could fold current faults out of sight by default. This was the one explicit constraint on this task and it was checked programmatically, not assumed: confirmed the `#faults` section has no collapsed `<details>` ancestor.

Verified with a real headless browser, JavaScript disabled: all eight menu hrefs (`#substrate`, `#type`, `#crawler`, `#reuse`, `#faults`, `#roadmap`, `#changelog`, `#labour`) resolve to a real `id` on the page.

Notes:
- Fourth of Phase 2.5's priority items (implementation order 4). Only RS-046 (priority) and RS-047/RS-048 (optional follow-ups) remain in this phase.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-046 — Practise export/import into a native disclosure
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the Consent Domains Map's save/resume controls — the findability warning, the filename field, the Save button, and the resume-from-file control — sat permanently visible beneath Print and Clear, adding four elements' worth of visual weight to a toolbar most visits never touch~~
now: those controls live inside a native `<details>`/`<summary>` disclosure labelled "Save or resume this map" — collapsed by default, no script needed to open it. **The full findability warning stays in exactly the same words, directly above the export button, every time the section is open** — verified by DOM order, not just visual placement, so it can't drift out of position on a future edit. Print and Clear everything stay exactly where they were, outside the disclosure, reachable with zero extra interaction — confirmed both remained clickable and visible with the details still collapsed. The print-header caveat, previously nested inside the (now-hidden-by-default) warning box, moved to the always-visible explanatory paragraph above the disclosure, since it's actually about the Print button, which never moved.

**No change to what gets saved, how, or the no-persistence guarantee.** `doExport`, `importFile`, `setFilename`, and `reset` are untouched — this is a markup change, not a logic change, and the completed-tasks entry for RS-027 still accurately describes the underlying model.

**Print CSS needed no changes.** The entire toolbar section (including the new `<details>`) is already wrapped in `class="noprint"` and excluded from printed output entirely — Phase 2.5's "optional sections must expand for printing" constraint doesn't apply here, since nothing in this section prints regardless of its `open` state on screen.

Verified with real Playwright interaction against a local server: confirmed the details element is closed by default; confirmed Print and Clear are visible and clickable without opening it; confirmed the warning text is genuinely not visible while collapsed (not just off-screen); opened the disclosure both by mouse click on the summary and by real keyboard (Tab to the native `<summary>`, then `Enter`) and confirmed both work; re-ran the full export flow (keyboard-triggered download, correct filename) and the full import flow (valid-file round-trip) through the new wrapper and confirmed both still work exactly as RS-027 shipped them.

Notes:
- Fifth and last of Phase 2.5's priority items (implementation order 5). Only RS-047 and RS-048 — the two items the author explicitly called optional follow-ups — remain in this phase.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-047 — Archive starter-route reframe
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the Archive's filter for its most accessible entries was labelled "★ start here," implying an order to the shelf the page's own kicker already called "nothing ranked" — the filter's own label disagreed with the framing copy sitting a few lines above it~~
now: "★ a first route," with a new line at the filter bar stating plainly what that means: a suggested way in, not a ranking — everything else on the shelf carries the same weight, not runner-up status. The per-entry caption on each "first route" archive card changed to match. Nothing about the underlying mechanism changed: `archive-filter.js` filters on the `data-filter`/`data-tags` attribute value `"start"`, untouched by the label-text rename, and every entry's `tags: ["start", ...]` in `archive.yaml` is unchanged.

Verified two ways, matching the rigor RS-004/D2 established for this exact page: with JavaScript disabled, all 39 archive entries (this count grew from 31 across RS-006/007/008's additions earlier this session) still render unfiltered, the new label text is present, and the old "start here" text is gone entirely — not coexisting, actually replaced. With JavaScript enabled, clicked the renamed filter button and confirmed it still narrows the visible set correctly, then clicked "everything" and confirmed the full 39 restore — the filter mechanism itself was never touched, but re-tested anyway rather than assumed safe from a label-only change.

Notes:
- Last of Phase 2.5's items shipped before RS-048 (the other author-flagged optional follow-up).
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-048 (rescoped) — clarify the Contribute form
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the Contribute form's "What you might offer" field, and the plan to add copy stating that subscribing and contributing are independent actions~~
now: the field is relabelled "Optional contribution note," the stranger-reads-this warning is preserved verbatim, and instead of the originally-planned independence claim, the field's own hint text says plainly what's actually true — the note isn't wired to anywhere it gets acted on yet — linking to a new fault (07) that names the gap precisely.

**The literal ask couldn't be shipped honestly, and here's why.** Before touching any copy, I traced what actually happens to this form's data: the "offer" textarea's value was captured in component state but never included in the `fetch()` call to the dispatch Worker — dropped before it even left the browser. Reading `worker/src/index.js` end to end confirmed the deployed Worker has zero code path for an "offer" field regardless; its entire surface is the mailing-list double-opt-in flow (subscribe/confirm/unsubscribe). Submitting the form for any reason — even leaving a note with every dispatch-interest box unchecked — requires a valid email and unconditionally triggers a "confirm your subscription" email, since there's no branch in the Worker that distinguishes "wants to subscribe" from "wants to leave a note." Separately, both this form's and Home's error message pointed a failed submission to "the address in the colophon" — a destination that does not exist; no email address is published anywhere on this site.

Given this, writing copy that told a reader "subscribing and contributing are independent actions" would have been the exact kind of claim this site's whole practice exists to prevent — asserting a capability the code doesn't have, the same category of problem RS-003, RS-021, and RS-015 all corrected earlier this session. **The user's own instruction — "a full multi-mode form redesign can wait" — also ruled out building the fix**, and building it for real requires infrastructure (a genuine second delivery channel) this session can't respons­ibly invent: no maintainer contact address exists to route note-only submissions to, and even a Worker-side code change can't take effect without a live `wrangler deploy` this sandboxed session has no credentials for. A clarifying question was put to the user about how to handle the gap (disclose honestly / provide a real address / ship the literal ask anyway / drop the field) and went unanswered; proceeded on the first, most conservative option — the one that ships the safe, real parts of the ask and discloses the rest, rather than either lying or blocking indefinitely.

**What shipped, concretely:**
- Field relabelled "Optional contribution note"; stranger-warning preserved word for word.
- The kicker line changed from "Join the dispatch, offer something, or both" (implies real independence) to "Join the dispatch, leave a note, or both," with a new sentence stating plainly that an email is required to submit this form at all today, and that leaving every interest box unchecked results in exactly one confirmation email and nothing further unless it's clicked — true of the code as it stands, not aspirational.
- `offer` added to the JSON body actually POSTed to the Worker — a small, safe, forward-compatible change (the deployed Worker currently ignores the extra key harmlessly; wiring up real handling later becomes a Worker-side change only, not a client one too).
- New fault 07 on `BehindTheScenes.dc.html`, named precisely: the note isn't read or stored anywhere, and why (no second channel exists behind this form).
- The dead "write to the address in the colophon" reference removed from both Contribute's and Home's submission-failure error messages (neither page nor colophon has ever published such an address) — `index.html` re-synced byte-for-byte from `Home.dc.html` per the established pattern.
- A residual task (RS-048, Phase 4) tracks the actual fix — a real contact destination the author would need to supply, plus a live deploy this session cannot perform — rather than leaving the gap undocumented once this task's row closed.

Verified with real Playwright interaction: intercepted the actual `fetch()` call to confirm the `offer` field is now present in the outgoing JSON body; confirmed the relabelled field, preserved warning text, and new fault-list link all render; confirmed with JavaScript disabled that the old field label, old kicker line, and dead colophon-address text are all gone from both affected pages and the colophon's `#faults` anchor (added by RS-045 earlier this session) resolves correctly.

Notes:
- This is the more consequential of Phase 2.5's two "optional follow-up" items precisely because investigating it surfaced two real, previously-undisclosed defects (silently-dropped user input; a dead contact reference) that the literal one-line task description gave no reason to expect were there.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.
- **This closes Phase 2.5** — RS-042 through RS-048 have all shipped, six as directly specified and RS-048 rescoped, with the reasoning for that deviation documented above rather than silently absorbed into a "done" checkmark.

### RS-034 (+ D11) — thesis 16, "The house is not a rehearsal"
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: fifteen theses, all about relationships, with no thesis linking the dyad to collective struggle — the base review's own finding that the manifesto has no political economy and no bridge from private practice to public movement~~
now: thesis 16, using `addendum-a.md`'s exact draft text (author-approved via `decision-record-d1-d15.md` D11, which resolves the thesis count at sixteen and names this exact draft as what ships): "We will not build a world without police, borders, and extraction while running small versions of all three at home. Not because the private is where politics begins — it is not, and treating it that way has kept a great many people busy and harmless. Because they are the same fight at different scales, and a movement that cannot survive its own kitchen will not survive anything else." Closing line: "Do both. Neither one is waiting for the other to finish." — deliberately load-bearing, per the source doc's own note, foreclosing the deferral reading ("fix yourself first, then organize") the original rejected draft invited.

**This is the salvage, not the original.** The base spec's draft for this thesis used "picking up your bundle" — an unsourced, nation-specific ceremonial term repurposed as a general metaphor for settler self-work — and was rejected outright (see the Rejected section of this file). RS-034 does the same conceptual work (linking interpersonal practice to collective struggle) without the metaphor, without deferral, and without borrowing.

**"A movement that cannot survive its own kitchen" links to the Archive**, per the source doc's explicit instruction ("link it from the thesis") — pointing to *The Revolution Starts at Home*, already on the shelf, rather than asserting the connection with nothing to back it.

**Global count update**: "fifteen theses and a refusal" → "sixteen theses and a refusal" everywhere the count is stated — both Manifesto meta tags (description, og:description), Home's nav card, and `index.html` (re-synced byte-for-byte from `Home.dc.html`, the established pattern). **Not touched**: `BehindTheScenes.dc.html`'s v0.1 changelog entry, which still says "fifteen theses" — correctly, since it's a struck historical record of what shipped at v0.1, and this site's own practice is that past changelog entries are never edited even once superseded.

Verified with a real headless browser, JavaScript disabled: thesis 16's heading and closing line render on `Manifesto.dc.html`; the meta description reads "Sixteen theses"; the Archive link is present; both `Home.dc.html` and `index.html` show the updated count.

Notes:
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-033 (partial, + D13) — the somatic archive shelf, Menakem
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the framework asked readers to notice what happens in a relationship and offered no vocabulary for what happens in a body while they do it — no somatic register anywhere in the archive, and the archive's thin Black theoretical presence sat at a conceptual distance from the felt register thesis 07's own line about who absorbs the anger is written in~~
now: a new archive group, "Bodies, regulation, and what happens before words," opens with Resmaa Menakem's *My Grandmother's Hands* — racialised trauma carried somatically, explicitly read alongside thesis 07, using `addendum-a.md`'s own `why` text verbatim. Verified directly against Central Recovery Press's own product page (title, author, publisher, ISBN all confirmed) rather than trusted from the source doc's unfilled `[VERIFY]` placeholder.

**Polyvagal theory is omitted entirely, not shelved with a caveat** — `addendum-a.md`'s own draft offered two options (shelve the contested theory with the dispute named, or omit it), and flagged that "option (b) is more interesting and more honest." `decision-record-d1-d15.md`'s D13 overrides that instinct: omit entirely. Nothing about polyvagal theory appears anywhere in the shipped group — verified by checking the rendered page contains no mention of the term at all, not just that no entry cites it.

**Prentis Hemphill's entry (the other half of this task as originally scoped) is not in this group — moved to RS-040 instead, decided rather than left flagged.** `warm-register-review-v2.md` §8 argued Hemphill belongs with Lorde/hooks/brown/Thom's new "Love, eros, and why any of this is worth doing" group rather than paired alone with Menakem, on the reasoning that five entries in one live group beats two thin groups. That review flagged the question rather than deciding it; since RS-040 (which creates the group Hemphill would move to) hasn't shipped yet in this same session, shipping Hemphill here and then relocating it later would mean editing an already-published entry for no reason other than sequencing — so the call was made now, before either group existed, rather than shipped provisionally and moved. `tasks.md`'s RS-040 row is updated to carry Hemphill forward; this group ships with exactly the one entry it can defend on its own terms.

Verified with a real headless browser, JavaScript disabled: the new group title and Menakem's name both render on `Archive.dc.html`; a case-insensitive search for "polyvagal" across the full rendered page text returns nothing.

Notes:
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-026 (scoped) — non-verbal and reduced-language check-in mode
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the framework's one safety check — the five screening questions asked before the Consent Domains Map, and republished standalone on Resources — existed in only one register: full sentences with embedded clauses ("Have you thought about leaving? If you have said so out loud, what was the response?"), excluding non-speaking, intermittently verbal, alexithymic, and cognitively fatigued readers from the exact check meant to protect them~~
now: the same five questions, in shorter sentences, sit behind a "Read these in shorter sentences" disclosure on both `Practise.dc.html` and `Resources.dc.html` — plain `<details>/<summary>`, no script, so the two pages that carry these questions behave identically whether or not JavaScript runs (Resources has none by design, `script-src 'none'`, and this needed to match rather than force a JS-only Practise version and a permanently-collapsed Resources one). The Consent Domains Map also gains a line naming that consent is not always spoken — use the labels, or agree on your own signals with the other person, a tap, a card, a gesture — stating plainly that the tool holds none of those signals itself.

**Scoped deliberately narrower than the source spec's full proposal, and here's the reasoning.** `addendum-a.md`'s RS-026 draft describes three things: (1) a reduced-language form of the safety questions — shipped, above; (2) documenting tap-count signalling as a between-people practice rather than building it as an input method — shipped, in the same new paragraph; (3) a "proposed state set" — four labelled, shaped, coloured states (Yes/fine, Not sure, No/not this, Skip) as a general non-verbal answering pattern. That third piece was **not built as a parallel answer system for the Consent Domains Map's existing seventeen-plus-three domains**, because the map's current five-option system (yes freely / yes with conditions / not yet ask me / no / mine to withhold) already satisfies the spec's own binding requirements — labelled, colour never the primary or only signal, 44px targets, no emoji — verified and shipped as part of RS-023 earlier this session. Replacing a more expressive, already-accessible five-state system with a coarser four-state one to match a spec table literally would be a regression dressed as compliance, not an improvement, and the spec's own draft copy ("use the labels... or agree on your own signals") reads as compatible with the existing options rather than demanding a second, redundant selector.

Verified two ways, matching what each page can support: with JavaScript disabled entirely, confirmed the `<details>` element and its reduced-language content exist in the DOM on both pages (not just present-but-inert — actual text content, checked directly). With a real headless browser and real interaction on Practise: confirmed the disclosure is closed by default, opens on a mouse click, and — separately — opens via pure keyboard (`Tab` to the native `<summary>`, then `Enter`), matching the keyboard-access rigor this session has applied everywhere a new disclosure has shipped.

Notes:
- RS-023's residual row previously named "RS-026's non-verbal state-control standard" as something it was waiting on; updated now that RS-026 has shipped, since that dependency no longer exists in the form it was written against.
- `npm run build:hugo && npm run build && npm run check` all pass clean on the final state.

### RS-016 — bulk archive additions
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: eighteen works the site's own arguments already leaned on had no shelf entry — Coulthard's *Red Skin, White Masks* was named in the Archive's own "Held in common" diagram since v0.2 and never actually added to a group; sovereignty's own critics (the 2025 sequel to the site's foundational Kuokkanen citation, Povinelli, Rifkin, Lugones); a second genealogy of settler kinship regulating sexuality; the clinical literature the safety gate is built on (Kelly & Johnson, Feeney); the material and abolitionist practice thesis 10 and thesis 12 assert (Freeman, Spade, brown, Kaba); and non-monogamy's own critics (Cohen, Noël, Rambukkana), plus Indigenous law as law rather than metaphor (Borrows) — all sat as `[VERIFY]` drafts in `base-work-order.md` §5, never independently checked or published~~
now: all nineteen entries — Coulthard, Cohen, Kuokkanen/Lightfoot/Starblanket/Wildcat (2025), Povinelli, Rifkin, Lugones, Kelly & Johnson, Feeney, Freeman, Spade, adrienne maree brown (*We Will Not Cancel Us*), Kaba, Noël, Rambukkana, and Borrows — are shelved in `hugo/data/archive.yaml`, each placed in the existing thematic group its subject matter actually belongs to rather than lumped together as one flat batch. `Archive.dc.html` regenerated via `npm run build:hugo`.

**Every link independently verified before use, none copied from the draft's own "✓ verified" marks or `[VERIFY]` placeholders.** Where the publisher's own page could be fetched directly (UMN Press, OUP, Duke UP's main site, UBC Press, AK Press, Haymarket, Verso, the author's own site for Freeman), it was fetched and the title/author confirmed. Where the publisher blocks automated fetches — Duke UP's `read.dukeupress.edu` journal platform (403, same as it blocked RS-006/007/033's fetches earlier this session), Wiley (402, paywall gate not a broken link), Sage Journals (403), University of Toronto Press's `utppublishing.com` (403 after a confirmed 301 redirect from the live `utorontopress.com` catalogue page) — the DOI was resolved through Crossref's own API instead, or the claim was cross-checked against multiple independent secondary sources (library reviews, other university press citations, bookseller listings) agreeing on title, author, and publisher. No link in this batch rests on a single unverified source.

**Cohen's "Punks, Bulldaggers, and Welfare Queens" is not tagged free, despite the source draft's `tag: "peer-reviewed · free"` claim.** Searches surfaced only unofficial third-party PDF mirrors (a course-reading upload, an activist blog repost) — not a publisher-sanctioned open-access copy. Per this site's established practice of never asserting a claim it can't back up, the entry links to Duke UP's own paywalled article page and carries no "free" tag; the draft's tag was aspirational, not something this session could confirm.

**Three entries initially tagged `peer-reviewed` (Kelly & Johnson, Feeney, Noël) were corrected to `article` before shipping.** The source draft used `peer-reviewed` as a tag value, but the Archive's filter (`archive-filter.js`) hides any entry whose `data-tags` doesn't literally contain the active filter's value, and `peer-reviewed` isn't one of the filter bar's buttons (`all` / `start` / `free` / `book` / `article` / `toolkit`). Shipping the draft's tag as written would have made all three vanish under the "articles" filter — a real, user-facing bug caught only by testing the filter after writing the entries, not by reading the draft. Fixed to `article`, matching every other journal-article entry already in the file, then re-verified with a real headless browser: clicking "books" shows Coulthard and hides Cohen; clicking "articles" shows Cohen, Feeney, Kelly & Johnson, and Noël; clicking "all" restores all 55 entries; with JavaScript disabled entirely, all 55 render with none hidden.

**No new filter facets added.** `base-work-order.md` §5 suggested considering `clinical` and `process` alongside the existing `book` / `article` / `toolkit` values. Once the `peer-reviewed` mistagging above was fixed, every new entry fit the existing three-value taxonomy without strain — a "clinical" facet would only ever separate two or three entries from the rest of "article," and the site's actual organizing structure is already its eleven thematic groups, not the flat filter bar. Adding facets that fragment a working, simple filter for a marginal gain was judged not worth it; documented here as a deliberate decision, not an oversight.

**A factual imprecision in `tasks.md`'s own verification-queue note, caught while integrating this task, was corrected separately**: a note added when Phase 3.5 was filed had called RS-016's and RS-040's "brown" entries "the same author, likely the same book." They are the same author (adrienne maree brown) but two different books — *We Will Not Cancel Us* (shipped here) and *Pleasure Activism* (RS-040, not yet shipped) — both legitimate, non-duplicate additions. Fixed in `tasks.md` before this entry was written.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. A real headless browser confirmed the filter's behavior above (both with and without JavaScript). Direct text search of the regenerated `Archive.dc.html` confirms all nineteen new titles/authors appear in the rendered output.

Notes:
- Phase 3 of `tasks.md` is now fully shipped except RS-006's residual (Patterson and Collins), which stays blocked on FLAG-03 — an author decision this session cannot make unilaterally, since it depends on which two specific books the source material meant.
- Placement judgment calls, briefly: Coulthard → "Relational sovereignty & Indigenous relational freedom" (matches the trackA diagram's own theming and his book's direct relevance to the site's master term); Cohen/Noël/Rambukkana → "Hierarchy, custom commitments, possession," clustered with Nordgren/Brake as the non-monogamy-critique set the source doc names them as; Povinelli/Rifkin/Lugones/Borrows → "Decolonization, possession, land, kinship, intimacy," alongside Moreton-Robinson and the Simpsons; Kelly & Johnson/Freeman → "Hierarchy, custom commitments, possession" (safety-gate and anti-hierarchy literature respectively); Feeney/Spade → "Relational autonomy, interdependence, care"; brown/Kaba → "Accountability, repair, transformative justice," alongside the existing Creative Interventions/Mingus/Chen-Dulani-Piepzna-Samarasinha/Dixon-Piepzna-Samarasinha entries.

### RS-036 — the Invitation, a second door
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the site had exactly one door and one register — the manifesto's acid, declarative refusal. Nothing on the site said what wanting this feels like, why anyone would choose it, or what's owed when a relationship it helped shape ends. The Archive's "Held in common" Two Row Wampum figure — two vessels on one river, neither steering the other — was used exactly once, in exactly one section, with the rest of the site organized as conventional, exclusive parcels~~
now: a new Hugo-generated page, `Invitation.dc.html`, presenting the warm register as a co-equal door — "Two ways in. Neither is the introduction to the other." — rather than a softer entry ramp toward the manifesto. It carries `warm-register-review-v2.md` §2.1's "An invitation" verbatim (first person, on wanting someone's growth without owning it, ending on the line the review calls structural rather than decorative: *"That last part isn't romantic. It's the floor. Everything soft in this rests on it."*) and §3.4's "On goodbyes" verbatim, closing on the sentence this whole task exists to hold: *"And if going safely means going suddenly — go. Nothing in any of this is a reason to stay."* Both passages are reproduced exactly as drafted, in the order given — the review is explicit that reordering or trimming the material floor from the warmth turns the page into a mood, and this ships the structure intact.

**Home gets exactly one paragraph, nothing more.** Added above the six-door grid (not as a seventh card — the grid still reads "six doors," accurately, since the Invitation isn't one of them), linking to the new page. This was the one documented exception to Phase 2.5's "do not alter `Home.dc.html`" constraint, carried forward from when Phase 3.5 was filed, and nothing else on Home changed.

**Nav integration went beyond RS-036's own stated file list, and here's why.** The task row named only `Invitation.dc.html` and "`Home.dc.html` (one paragraph only)" as files. But every existing page's persistent primary and footer nav already lists Resources — added, per the current file state, when RS-002+031 shipped that page, even though nothing in this session's visibility shows that as an explicit line item either. Precedent read as: shipping a new page means integrating it into the site's nav, not just dropping an orphan file linked from one paragraph. Added an "Invitation" link, positioned immediately after "Manifesto," to both nav blocks (header and footer where present) on all nine other nav-bearing pages: the four Hugo layouts (`manifesto.html`, `archive.html`, `behindthescenes.html`, `learn.html` — regenerated) and the five hand-authored pages (`Home.dc.html`, `index.html` via the usual `cp`, `Practise.dc.html`, `Contribute.dc.html`, `Resources.dc.html`). Also added to `robots.txt`'s `User-agent: *` allow list (alongside the other reading pages) and `sitemap.xml` at the same priority as the manifesto, and registered `Invitation.dc.html` in `scripts/build-hugo.mjs`'s `HUGO_PAGES` and `scripts/check-pages.mjs`'s page list so it's covered by the standard build/check pipeline going forward.

**The Two Row Wampum figure is used a second time, not just described a second time.** The page's opening section names the figure directly and links back to the archive's own use of it; `hugo/data/faults.yaml`'s fault 04 ("The layout still parcels space") is updated to say two places now use shared/relational layout logic rather than one, since that's what changed — not marked resolved, since the rest of the site is still conventional parcels and the fault's own "whether that matters more than legibility" question stays open. **Correction made in the same edit**: the review's own text calls this "fault 05," which was the correct number when `warm-register-review-v2.md` was written, but fault 04 ("The pages need a runtime to draw") was retired earlier this session and the list renumbered — the figure's fault is 04 under current, live numbering. Written correctly as 04 here and in this entry; the review's own text is left as written, since quoting a source accurately isn't the same as the source being current.

**What did not ship, and why.** §2.2's three softened-principle samples (01, 03, 07) are not on the page. No task ID in `tasks.md` ever assigned them anywhere, and inventing a fourth section beyond what RS-036's own row scoped would be adding un-requested content, not completing an under-specified one — flagging the gap here rather than silently either shipping or dropping it. Separately, §3.3's endings-tool cross-reference on the Invitation page currently points to `Practise.dc.html` for a tool that doesn't exist yet (RS-039, not yet shipped) — worded as "once it ships," honestly, rather than implying the tool is live.

**A real color-contrast defect was caught and fixed before shipping, not after.** The closing "You have reached the end of the invitation" label used `#3F7A4E` (this site's standard link-hover green) at 11px on the page's `#E7E5DC` background — axe-core flagged 4.05:1 against the required 4.5:1 for normal text. Manifesto's parallel line uses a different green because it sits on a dark background; copying that choice onto a light background without recomputing contrast was the mistake. Fixed to `#2C5A38` (the same dark green Archive already uses for its active-filter state, independently contrast-safe at 6.34:1) and re-verified clean.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean (11 pages now, up from 10). A real headless browser confirmed — with JavaScript disabled entirely, since this is a static Hugo page like Manifesto/Learn/Archive/BehindTheScenes — that the page's `<h1>`, the invitation's closing floor line, and the "On goodbyes" heading all render. With JavaScript on: clicking Home's new paragraph link lands on `Invitation.dc.html`; pure keyboard tabbing from Manifesto's header reaches the new "Invitation" nav link.

Notes:
- RS-041 was expected to unblock once RS-036 shipped. It didn't, for a reason only visible after actually writing the page: `warm-register-review-v2.md` §7 resolves the *provenance* of "the Taíno terms" but never names them, and the content that would have named them lived in the superseded v1 review, not in this repo. Nothing in §2.1 or §3.4 — the only draft copy v2 actually supplies — reads as Taíno-sourced vocabulary. Rather than guess which words might qualify, RS-041's `tasks.md` row is updated to say plainly that it's still blocked, on a different and more specific problem than "wait for RS-036." No disclosure terms were written for unidentified content.
- `docs/spec/warm-register-review-v2.md` §3.3 has what reads as a typo — "Part 2 — What you owe on the way out. From §2.4 below" — there is no §2.4 in the document; the actual matching section is §3.4, titled "On goodbyes," which is what's excerpted here and what RS-039 will excerpt from again. Treated as a source-doc error and worked around rather than propagated.

### RS-035 — forms and relationship labels, a Learn topic
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: `topics.yaml`'s roadmap named "Relationship anarchy" and "Amatonormativity," both marked `drafted: false`, and Learn never actually defined either term anywhere — a reader needed to already know what relationship anarchy meant to notice the framework leans on it (the archive cites Nordgren and Brake, but nothing on Learn explains why they're there)~~
now: a new Learn section, "Forms and labels, none of them the default" (`#forms-and-labels`), defining both terms in plain language and linking each to its source on the archive shelf, followed by an alphabetical, unranked grid of eleven relationship-form labels (asexual/aromantic partnership through solo polyamory) with an explicit line stating the list's only order is alphabetical — not seriousness, permanence, or virtue — and that a person's own word for what they have governs even where it isn't one of the eleven. `topics.yaml` flips both entries to `drafted: true`, matching the roadmap to what's actually written.

**"The field visualisation embeds in the topic" (the review's own words, `warm-register-review-v2.md` §4) could not be built to spec, because no spec for it exists in this repo.** RS-035 has no entry in `base-work-order.md` or `addendum-a.md` — it originates entirely from the review's own information-architecture table, which names a "field visualisation" as something already decided elsewhere without describing what it is. No earlier document defines it. Rather than skip the instruction or invent an elaborate interactive element to satisfy an unverifiable phrase, built the most literal, defensible reading of "field, not a ladder": a flat, alphabetized, unranked CSS grid of labels, explicitly captioned as unranked, requiring no script (Learn is a static Hugo page) and printing cleanly. This is disclosed as an interpretation, not presented as if it matches an unseen specification.

**Closes with the material-conditions constraint from Phase 3.5's own binding list, not just alongside it.** The section's closing line — "a field is not a floor... legal recognition, immigration status, custody, and inheritance still fall unevenly across them" — exists specifically because §9 item 4 of the review (differential legal/immigration/custody risk across relationship forms) is one of the things that "must not soften," and an unranked grid of labels, alone, could otherwise read as implying the choice between them is consequence-free. It isn't, and the section says so before it ends.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. One real defect caught before shipping: the label grid's first draft used `aria-label` directly on the `<ul>`, which `html-validate`'s `aria-label-misuse` rule rejects; fixed to `aria-labelledby` pointing at the preceding paragraph's `id`, matching the pattern the archive's own "Held in common" diagram already uses for its lists.

Notes:
- Placed as the last substantive Learn section before the existing "What's planned, not what's built" roadmap pointer, since it's new content added under this initiative rather than a rewrite of something already there.
- **Correction, added when RS-010 shipped:** flipping these two `topics.yaml` entries to `drafted: true` should have come with an update to the hardcoded "six...nineteen" topic-count sentences in `hugo/layouts/learn.html` and `hugo/layouts/behindthescenes.html`. It didn't, and both stayed stale until RS-010 caught and fixed it two tasks later. See RS-010's entry below for the fix; noted here so the record shows where the staleness actually originated.

### RS-037 — "Before you bring this up with someone"
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: nothing on the site addressed the moment right before someone tries to introduce this framework to a partner — no guidance on how to raise it without it landing as a test or an indictment, and no warning that the same vocabulary built to name coercion is exactly as usable to apply it~~
now: a `<details>` disclosure on `Practise.dc.html`, titled "Before you bring this up with someone," placed in the always-visible intro area above the safety/grounding stage gate rather than inside it — this content is about a conversation with someone who hasn't engaged with the tool at all, a different moment from the existing pre-tool sequence, so it's offered alongside that flow rather than merged into or gating it. Five concrete suggestions from `warm-register-review-v2.md` §3.1, reproduced verbatim, closing on the one paragraph the review calls the guard and states must ship with it or not at all: raising this to pressure someone who's already declined isn't what the tool is for, and using this vocabulary to do it makes it worse, not better.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. A real headless browser confirmed the guard paragraph's exact text is present in the DOM with JavaScript disabled entirely (native `<details>`, no script required to read it), and that a real mouse click opens the disclosure with JavaScript on.

Notes:
- Placed above, not inside, the existing `atSafety`/`atGrounding` stage gate — someone deciding how to bring this up with a partner is a different reader, at a different moment, from someone already committed to filling in their own map, and gating this behind "Continue" would have hidden it from exactly the person most likely to need it before ever opening the tool.

### RS-038 — the language-games field guide, and a real print-button fix found while building it
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: this site's own vocabulary was published with no acknowledgment that the same words doing the naming — "boundary," "capacity," "sovereignty" itself — are exactly as usable to avoid accountability as to demand it~~
now: a new Learn section, "How this vocabulary gets used as a weapon" (`#field-guide`), reproducing all ten phrase/diagnostic-question pairs from `warm-register-review-v2.md` §3.2 verbatim, including the entry naming this site's own term directly ("I'm honouring my sovereignty" — *What's being honoured, and what's being avoided?*). Linked from three places, as the task specified: Practise's safety-gate card (new sentence, right where a reader is already sitting with hard questions), Resources' coercive-control listing (a sentence naming that coercion often arrives as ordinary-sounding concern), and the Invitation's own closing section (naming plainly that the Invitation's own warm language is exactly this kind of language too — not exempting itself). A parked, unbuilt contribution channel — "what got said to you, in this vocabulary, to avoid answering" — is named on Contribute's governance section rather than built, per the review's own framing of it as the strongest community-contribution candidate on the site with nowhere yet to route it to.

**Printable as its own one-page-ish object, not just as part of printing all of Learn — a genuine build, not a description.** A "Print this field guide" button toggles a `print-isolate` class on `<body>`, and `@media print` rules scoped to that class hide everything on Learn except the `#field-guide` section before the browser's print dialog opens, then the class is removed on `afterprint`. Nothing else on the site does this (Manifesto and Invitation each print their whole page, since each page mostly *is* one printable object already); Learn is the first page with more than one thing on it that might get printed separately, and this is the first genuinely scoped print button as a result.

**Building that button surfaced a real, pre-existing, unrelated defect: every print button on the site was silently broken.** Manifesto's "Print this manifesto" (shipped earlier this session) and Invitation's "Print this invitation" (RS-036, shipped today) both used `onclick="window.print()"` — an inline event handler, which `script-src 'self'` without `'unsafe-inline'` refuses to execute. Confirmed directly: clicking either button fired a CSP violation in the console (`Refused to execute inline event handler...`) and `window.print()` never ran. Neither button did anything, on any page that shipped it, and nothing surfaced that to a reader — no visible error, just a click that did nothing. Caught only because RS-038 needed a *working* print button and testing the pattern first, rather than assuming a shipped pattern was sound, showed it wasn't.

**Fixed with a new shared file, `print.js`, rather than weakening the CSP.** The alternative — adding `'unsafe-inline'` to `script-src` — would have let any injected inline script execute, a real weakening of the same policy RS-020 built specifically to prevent that. Instead, `print.js` (same pattern as the existing `archive-filter.js`: progressive enhancement, external file, same-origin, already permitted by every affected page's current CSP with no policy change needed) attaches real `click` listeners to `[data-print]` (plain print) and `[data-print-isolate]` (scoped print) elements. Manifesto's and Invitation's buttons were converted from the dead `onclick` attribute to `data-print`, and now genuinely call `window.print()` — verified directly, with the CSP violation gone and the mocked `window.print` actually invoked.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. A real headless browser confirmed, for all three buttons (Manifesto, Invitation, Learn's field guide), that clicking fires `window.print()` with zero console errors — a regression this session's usual build/check pipeline would not have caught on its own, since a silently-failing inline handler produces no HTML-validation or build error, only a runtime console message during actual interaction.

Notes:
- `tasks.md`'s Phase 3.5 header now carries a standing caution about the source review's fault numbers, having caught two stale references from it in two consecutive tasks (RS-036, RS-038) — its "fault 06" is this site's current fault 05.
- Corrected in the same edit: Contribute's new note cites "fault 05," not the review's own "fault 06," per that caution.

### RS-039 — an endings tool, and one build instead of three
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: "Exit & Transition Plan" sat on Practise's not-yet-built list — "Safety, housing, finances, children, animals, digital access, confidentiality. Endings deserve the same care as beginnings" — while nothing on the site actually held that care: no logistics checklist, no statement of what's owed on the way out beyond RS-030's Learn-page clause, and no grief content of any kind~~
now: a second, independent tool on `Practise.dc.html`, entered from where the unbuilt placeholder used to sit (now removed from the `tools` array — seven placeholders remain, not eight). Its own safety gate, reproducing the review's exact line, sits before any of the tool's content rather than after: *"If leaving safely means leaving suddenly — close this and go. Nothing on this page is worth staying for."* Past the gate, three parts on one screen: **Part one**, a care-continuity audit — eight logistics items (money, medication, the school run, paperwork, the lease, a visa or immigration status, a pet, a phone plan), each with a free-text "who holds this after" field and a "they know" checkbox, genuinely stateful like the Consent Domains Map's own fields; **Part two**, the Invitation's "On goodbyes" reproduced in full, credited back to that page; **Part three**, three short paragraphs on grief, built exactly as specified — no fields, no prompts, nothing that registers as complete, ending on permission to close the tab.

**One build, not three, actually enforced rather than just claimed.** The task named this as the same object as RS-029's still-outstanding Repair Protocol step 2 (care-continuity audit) and the already-shipped RS-030 clause (Learn's principle 06/07 bridge). Nothing new was built for either of those — RS-029's row in `tasks.md` is rescoped to three remaining steps (impact assessment, "Pods and stewards," restitution-or-responsible-exit) with a note pointing at this tool for the fourth, and its effort estimate dropped from L to M accordingly. RS-030's Learn content is untouched; this tool doesn't duplicate it, it fulfils the practical half RS-030 always deferred.

**Kept as its own state machine, not folded into the Consent Domains Map's.** The map's `stage` field ("safety" → "grounding" → "tool") already gates one flow; this tool uses an entirely separate `endingsStage` field ("closed" → "gate" → "tool"), so opening one tool doesn't disturb the other's state, and a reader who's mid-map when they open the endings tool doesn't lose their place. The cost of that choice, disclosed rather than hidden: both tools' safety gates can be on-screen simultaneously if a reader has the map at its default gate and opens the endings tool too — confirmed directly (two "Continue" buttons exist on the page at once in that state) and left as is, since each gate is self-contained and unambiguous about which tool it belongs to.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. A real headless browser walked the whole flow — clicked "Open this tool," clicked past the gate, confirmed all three parts render, typed into the audit's first row and checked its "they know" box, read the value back out of the DOM (not just visually), and confirmed the print button calls `window.print()` with zero console errors, same fix as RS-038's `print.js`.

Notes:
- The "Practise — tool 02 of 09" kicker above this tool follows the existing convention from the Consent Domains Map's own "tool 01 of 09" label; the total of 09 is unchanged since this was already counted among the nine tools the page described, two built now instead of one.
- `warm-register-review-v2.md` §3.3's Part 3 ("grief content with no fields, no prompts, no completion state") has no draft copy anywhere in the source documents — this session wrote the three closing paragraphs directly, since the instruction specifies constraints on the content, not the content itself.

### RS-040 — a fourteenth archive group, on why any of this is worth doing
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the archive held thirteen groups of critique, revision, and repair, and no shelf for the erotic, for love as practice rather than feeling, or for what makes any of this worth building — a real gap in a site about intimacy, and distinct from thin Black feminist presence even after this session's Spillers/Hartman/Roberts/Bridges (RS-006) and Menakem (RS-033) additions~~
now: a fourteenth and final archive group, "Love, eros, and why any of this is worth doing," with five entries: Audre Lorde's *Uses of the Erotic: The Erotic as Power*, bell hooks' *All About Love: New Visions*, adrienne maree brown's *Pleasure Activism: The Politics of Feeling Good*, Kai Cheng Thom's *I Hope We Choose Love*, and Prentis Hemphill's *What It Takes to Heal*. Placed as the archive's closing group — the shelf ends on why, after thirteen groups of what to refuse and how to repair it.

**All five links independently verified**, same standard as every archive addition this session. Four fetched directly against their publisher's own page (Penguin Random House for Lorde and Hemphill, AK Press for brown, Arsenal Pulp Press for Thom) and confirmed on title, author, and publisher. The fifth, hooks' *All About Love*, could not be fetched from the primary `harpercollins.com` domain (the tool declined the fetch outright, and a guessed URL slug returned nothing usable); confirmed instead via HarperCollins India's own product page (`harpercollins.co.in`), which loaded cleanly and is still a genuine publisher-branded page, cross-checked against Wikipedia, Goodreads' edition list, and multiple booksellers all agreeing on the same title, author, and 1999/2000 William Morrow (HarperCollins) publication. Documented here rather than silently treated as equivalent to a `.com`-domain link, since it's a regional storefront rather than the primary one.

**Hemphill's placement was decided when RS-033 shipped, not revisited here.** `tasks.md`'s RS-033 entry already recorded the reasoning in full: Menakem shipped alone in "Bodies, regulation, and what happens before words" rather than provisionally paired with Hemphill and moved later, specifically so Hemphill's entry would only ever be written once, in the group it actually belongs to. This entry is that write. A `why` line for Hemphill needed to be composed fresh — the source review names Hemphill as belonging in this group without drafting `why` copy for the entry, unlike the other four — built around Hemphill's own best-known formulation (boundaries as the distance at which you can love someone and still love yourself), which also nets a small piece of unplanned cohesion: it echoes Learn's new field guide (RS-038), which has its own entry on "boundary" as a word that gets weaponised.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean.

Notes:
- Placement decision: last group in the archive, deliberately, rather than interleaved earlier — the source review frames this shelf as answering "why" after the rest of the archive spends its groups on refusal and repair, and closing on it reads that argument correctly rather than burying it mid-list.
- `tasks.md`'s verification queue entry for RS-040 is updated to record that all five are done, and to correct itself: it had previously described the RS-016/RS-040 "brown" entries only in a note added elsewhere in the file, not resolved in its own line — now consistent.

### RS-011 — chosen monogamy, named and protected
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Home's "Four limits, stated before anything else" named what relational sovereignty isn't — not radical individualism, not relationship anarchy, not decolonization, not one universal Indigenous philosophy — without ever affirmatively protecting chosen monogamy or naming the coercion that runs toward non-monogamy rather than away from it, an odd gap for a site whose own "not a synonym for relationship anarchy" limit already argues against treating any one relationship form as more evolved~~
now: a new paragraph on `Home.dc.html`, placed beside the four-limit grid rather than folded into it (a fifth item would have muddied a section titled "four limits"): chosen monogamy named directly as protected on the same terms as any other form, and coercive non-monogamy named as what isn't — pressure, guilt, or being worn down into opening a relationship someone didn't want opened, sometimes using this site's own vocabulary to do it.

**Deliberately consistent with, not duplicating, RS-038's field guide.** Learn's "How this vocabulary gets used as a weapon" already carries an entry on "You're being possessive. That's so amatonormative." with the diagnostic *is the objection being answered, or reclassified?* This paragraph is the Home-level, affirmative statement of the same position rather than a repeat of that entry — Home states what's protected, Learn's field guide names how the protection gets talked around.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean; `index.html` re-copied from `Home.dc.html` and confirmed byte-identical.

### RS-012 — thesis 02, tradition isn't unfreedom by default
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: "The colonial relationship arrives preassembled... We will not mistake tradition for consent, or familiarity for freedom... No rule is sacred just because it is old." The closing line made age the test for whether a rule counted as free or colonial default, which — read as a general principle rather than just a jab at "this is just what marriage is" — implicitly treats every inherited, genealogical, or ceremonial form of relationship as suspect by definition, and treats the self-authoring individual as the one free position. That is the autological/genealogical antinomy Elizabeth Povinelli's *The Empire of Love* names directly (added to the archive this session, RS-016): liberal settler discourse's habit of calling genealogically-organized societies unfree for being genealogical~~
now: the thesis keeps its actual target — preassembled defaults, tradition mistaken for consent — and adds the sentence that was missing: "a relationship chosen inside inherited law, kinship, or ceremony is not less free than one built from nothing. The self-authoring individual is not the only shape freedom takes, and insisting it is has its own colonial history — the same one that called genealogical societies unfree for being genealogical." The closing line changes from an age test to the same accountability test the rest of the thesis already runs on: "Not how old the rule is. Whether it can answer for itself."

**The fix was already implicit in the manifesto's own thesis 14, which is why the task named it as the template.** Thesis 14's line — "We do not worship destruction; we destroy what prevents life, then we build" — already refuses the crude "old = bad, new = free" equation in the destruction direction; RS-012 applies the same discernment in the other direction, to what gets kept rather than only to what gets torn down. The two theses now argue the same thing from opposite ends rather than one of them (02) quietly contradicting it.

**Directly reinforced by this session's own archive work**, not just theoretically: Coulthard's *Red Skin, White Masks* (RS-016, "grounded normativity — obligation generated by place-based practice") and Povinelli's *The Empire of Love* (RS-016, cited above) both landed on the shelf this same session, before this thesis was touched — the revision uses vocabulary and an argument the archive can now actually back up with a citation, rather than asserting it unsupported.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean.

### RS-013 — thesis 11, naming who has standing to ask
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: "What happens between two people does not always stay between two people; children, friends, communities, care networks, and land absorb the consequences. We reject the excuse that anything is ethical because consenting adults arranged it behind closed doors." / "No private freedom built on public damage." The thesis argued a relationship must answer for its effects, but never said to whom — left as a general principle, it can license anyone with an opinion (a hostile public, an ideological pile-on, a state agency) to demand that a household explain itself, which is already, in practice, how surveillance of poor, disabled, queer, and Black families gets justified~~
now: the thesis keeps its claim and adds the missing sentence: "standing to demand that account belongs to the people who absorb the consequences — not the state, not a crowd with an opinion. Collapse that distinction and this thesis becomes a rationale for the surveillance already aimed at the families it should be protecting." The closing line changes to match: "No private freedom built on public damage — and only the people who absorb it get to ask."

**"The people who absorb the consequences" reuses the thesis's own list rather than introducing a new category.** Standing is defined by the same group the thesis already names as bearing the costs — children, friends, communities, care networks, land — not by a newly invented category of "affected parties" that would need its own definition. The revision closes the gap the task named without adding vocabulary the thesis doesn't already have a home for.

**Names the failure mode directly rather than just avoiding it.** Rather than quietly adding the standing clause and moving on, the revision states outright what happens if the distinction is dropped — "this thesis becomes a rationale for the surveillance already aimed at the families it should be protecting" — matching thesis 01's own citation of Dorothy Roberts' *Torn Apart* ("the demand that a household account for itself is already, for some families, the state's demand," added to the archive this session under RS-006) rather than leaving that connection implicit.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean.

### RS-009 (residual) — the adjudication question, answered where it can be
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the framework named two real internal conflicts and answered neither in general terms. Learn's existing "Continuity of care" clause already flagged that "principle 06 (relational accountability) and principle 07 (refusal) can pull against each other with nothing here to say which wins," resolving only the dependency-at-leaving case and saying explicitly it wasn't a general answer. The second conflict — principle 08 (opacity) against the manifesto's thesis 11 ("relationships are not private empires," standing to demand an account) — wasn't addressed anywhere at all, despite both principle 08's own text and (as of RS-013, shipped earlier this same session) thesis 11's own text already containing the pieces needed to answer it~~
now: a new Learn section, "Two adjudication questions, two different answers," directly beneath the existing Continuity of care clause. It gives the two conflicts different treatments because they have different shapes, rather than forcing one method onto both: **06 vs. 07 stays open, argued rather than just asserted** — refusal is protected regardless of the disruption it causes; what 06 actually governs is the manner of an ending where real obligations exist, not whether the ending is allowed; past that distinction, the framework declines to supply a formula, and calls that a refusal of false precision rather than an oversight. **08 vs. thesis 11 gets an actual rule**, not a shrug: principle 08's own text already narrows opacity to a right against people who want more than they're owed; thesis 11's revised text (RS-013, shipped earlier today) already narrows standing to the people who absorb the consequences. Put together, the answer is that opacity holds fully against anyone without standing, and even someone with standing is owed only the specific thing they'll absorb, not a full accounting.

**This wasn't answerable until earlier today.** The 08-vs-11 half of this task depended on RS-013's revision to thesis 11 actually existing — before that revision shipped, thesis 11 didn't name who had standing, so there was no "standing" concept on the site for principle 08 to be weighed against. Sequencing this after RS-012/RS-013 rather than before was necessary, not incidental.

**Directly cites its own proof of work.** Rather than assert the 08/11 synthesis as abstract reasoning, the section points at RS-039's endings tool as the rule already in practice: "who holds this after, and do they know" is exactly "owed only what they'll absorb, not a full accounting," built before this section explained why it was built that way.

**A cross-reference callout distinguishes this from the Repair Protocol** (RS-029, still mostly unbuilt, deliberately last per its own row): the Repair Protocol answers what's the process after harm; this section answers which principle governs a standing conflict between two of them. Different questions, and only one of them had a process already planned to resolve it.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean.

### RS-010 — treaty and protocol, promoted from layout gesture to concept
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the Two Row Wampum appeared exactly once on the site, in the archive's "Held in common" section, functioning purely as a layout figure — two vessels on one river, neither steering the other — with no connection drawn to the framework's own central structural claim: two people, each sovereign over the terms of their own relations, sharing one condition, with neither subordinate to the other. That is exactly the shape a treaty relationship is built to hold, and "sovereignty" (the site's own operative word, per the existing "Four things this word is doing" section) names the value without naming the missing form~~
now: a new Learn section (`#treaty`), placed directly after the sovereignty-senses discussion it completes: "Two sovereigns, one shared condition. Sovereignty doesn't have a form for that. Treaty does." Names treaty and protocol as concepts the framework needs and identifies what's actually borrowed — not the fact of a treaty, but its *logic*: a treaty is a standing practice of renewal, not terms set once and assumed to hold, and this framework asserts non-domination (principle 05) as a value while having built no equivalent renewal practice of its own. Points at "The Relational Review" — still on Practise's not-yet-built list, "a seasonal check-in on what feels alive, what feels assumed, where resentment is accumulating, what needs revision" — as that exact logic, unbuilt, rather than inventing a new tool to fill the gap.

**The limit is stated with the same weight as the claim, not appended as a disclaimer.** Given the `[DECISION]` tag on this task and the real risk of analogizing an interpersonal relationship to a nation-to-nation treaty relationship, the section states directly and early: the Two Row Wampum "is not a metaphor invented for that purpose. It's a specific treaty relationship, between specific nations, with its own terms, its own history of being violated, and its own ongoing life independent of anything this site does with it," and that treating a household as equivalent to that relationship "would be exactly the kind of 'lifestyle experimentation' borrowing of Indigenous political thought that Home's own stated limits already rule out" — a direct cross-reference to Home's existing "Not a substitute for decolonization" and "Not one universal Indigenous philosophy" limits, so the new section is bound by constraints the site already committed to rather than inventing new ones for the occasion.

**The archive's own Two Row Wampum note now points back**, closing what was previously a one-way, undeveloped reference: `hugo/layouts/archive.html`'s existing callout gains a sentence linking to the new Learn section, "including the limit on how far that suggestion is allowed to go" — naming the limit even in the one-line cross-reference, not just in the fuller treatment.

**A real, unrelated staleness bug was caught and fixed in the same edit.** `topics.yaml`'s roadmap gained a twentieth entry ("Treaty and protocol," `drafted: true`) for this section, which required recomputing the drafted count — nine of twenty, not six of nineteen. Checking that number surfaced that it had *already* gone stale two tasks earlier: RS-035 (shipped today) flipped two topics from `false` to `true` without updating the hardcoded "six" in either `hugo/layouts/learn.html`'s or `hugo/layouts/behindthescenes.html`'s prose, so both pages had been quietly wrong since that commit. Both fixed here, with `behindthescenes.html`'s claim also tightened from "none are linked or published" to "none are linked or published from this list," since three of the drafted topics now correspond to real, anchored Learn sections even though the Roadmap's own cards don't hyperlink to them.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean.

Notes:
- A general lesson from the staleness bug, worth carrying forward: any task that flips `topics.yaml` entries between `drafted: true`/`false` needs to recompute and update the two hardcoded topic-count sentences (`learn.html`, `behindthescenes.html`) as part of that same task, not as an afterthought caught by the next task that happens to touch the same file.

### RS-014 — seven worked scenarios, failures marked as failures
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the thirteen principles were illustrated only by their own built-in questions — abstract, and abstractly clean. Nothing on the site traced a principle through an actual hard case, and a framework that only ever illustrates itself with easy cases is quietly asking a reader whose situation is a hard one to notice the gap on their own~~
now: a new Learn section (`#scenarios`), placed directly after the thirteen principles and before the continuity-of-care clause: seven situations, each with which principles hold, and — marked distinctly, in the same burgundy the site already uses for safety and limit content, not softened into the "holds" copy — which principle fails and why. Sponsored partner (07 fails: no material floor under refusal, only a pointer to Resources). Disabled person partnered with their funded attendant (01 and 08 both strain: no separate vocabulary for paid labour and chosen intimacy sharing the same hours). Carer of a dependent adult (07 fails honestly, not relieved: naming a constraint isn't the same as fixing it, and a website can't supply respite care). Coercive control (nearly everything fails at once, by design — the reason this framework routes to Resources here instead of offering more principles). Guardianship (02 fails by legal design; no stated position on when that's justified versus a form of the possession thesis 01 refuses). Precarious worker in shared housing (07 again: refusal exists in principle, not in practice, without secure exit). Child in a restructuring household (no principle grants a child standing proportionate to what they absorb — the framework's principles are written for adults exercising refusal and self-determination directly, and a child needs a different, not smaller, theory of voice).

**Reuses the site's own prior work rather than re-deriving it.** Each "holds" and "fails" line cites the actual principle number and, where relevant, the actual revised text shipped earlier this same session — thesis 02's "answer for itself" test (RS-012, precarious housing scenario), thesis 11's standing clause (RS-013, the child scenario), principle 08's institutional-disclosure carve-out (already shipped, the funded-attendant scenario). None of the seven scenarios required inventing a new position; all seven required actually applying positions the site had already taken, honestly, to situations harder than the ones that prompted them.

**A closing note states the section's own limits rather than implying completeness.** "Not exhaustive, and not ranked by severity... the point isn't the list, it's that a list this short already finds seven of them" — seven scenarios are not a claim to have covered every hard case, and the note says so rather than letting the number seven imply a survey.

**A second staleness bug, caught the same way as RS-010's, fixed in the same edit.** Learn's own top-of-page reading-time estimate ("about 6 minutes") predates most of this session's additions to the page — continuity of care, the adjudication section, sovereignty senses' existing content, treaty, opacity, forms and labels, the field guide, and now seven worked scenarios have roughly tripled the page's length since that estimate was set, and nothing had touched it since. Corrected to "about 15 minutes" on Learn itself, and on Home's own door card for Learn (previously "the idea · 6 min"), which pointed at the same now-wrong number.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean; `index.html` re-copied from `Home.dc.html` and confirmed byte-identical after the reading-time fix.

Notes:
- Worth watching for going forward, now caught twice in one session (RS-010, RS-014): any task that substantially lengthens Learn should include a check of the page's own reading-time claim, not just its content accuracy — length claims go stale exactly as easily as content claims, and this site has committed to not making either kind of small lie.

### RS-017 — sexual content, named plainly with the existing vocabulary
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: a site whose stated subject is intimacy said nothing about sex at all — no mention of desire discrepancy, reproductive coercion, sexual trauma, or consensual power exchange anywhere, despite the framework already having exactly the conceptual tools each one needed: principle 04's specific, ongoing, materially possible consent; principle 05's non-domination test (arbitrary, hidden, or impossible to challenge — not "power exists at all")~~
now: a new Learn section (`#sexual-content`), placed directly after the worked scenarios, naming all four using only vocabulary the site already had. **Desire discrepancy**: a yes given to avoid conflict, guilt, or the relationship ending fails principle 04's ongoing-consent test in either direction — the higher-desire partner is equally protected from being treated as a problem to manage. **Reproductive coercion**: named as a specific, documented form of intimate partner violence, not a disagreement about family planning, and routed directly to Resources' coercive-control listing (`#coercive-control`) rather than treated as a standalone concept needing its own tool. **Sexual trauma**: principle 04's "materially possible" applied to a nervous system's capacity to say no in the moment, paired with an explicit statement that this site is not a trauma-therapy resource and doesn't claim to be — a limit stated plainly rather than implied by silence. **Consensual power exchange**: distinguished from what principle 05 refuses using principle 05's own test, not a new standard — named, bounded, revocable power exchange is closer to what non-domination actually asks for than a relationship that claims to have no power dynamics and therefore never examines the ones it has.

**Every one of the four required applying an existing position, not inventing a new one.** This matches the source document's own framing exactly: "thesis 05's 'named, limited, contestable' is already a serviceable description of negotiated asymmetry; the framework has the vocabulary and does not use it." No new principle, no new archive citation, no new register — the gap was that the site had the tools and had simply never pointed them at this material.

**`topics.yaml` gains four new entries** (Desire discrepancy, Reproductive coercion, Sexual trauma, Consensual power exchange), all `drafted: true` since real content ships with this task rather than being promised for later. Topic and reading-time counts recomputed and corrected everywhere they're stated — twenty-four topics, thirteen drafted (`learn.html`, `behindthescenes.html`), and Learn's own reading-time estimate moved from 15 to about 17 minutes to account for this section's length, on both Learn itself and Home's door card. This time the count was kept in sync in the same commit that changed it, rather than caught two tasks later as happened with RS-035 and again partially with RS-014 — the lesson from those two actually applied rather than just written down.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean; `index.html` re-copied from `Home.dc.html` and confirmed byte-identical.

**This closes out Phase 4's actionable scope.** RS-009, RS-010, RS-011, RS-012, RS-013, RS-014, and RS-017 all shipped today. What remains in `tasks.md`'s Phase 4 is exactly two rows, both intentionally not actioned rather than overlooked: RS-048 (residual), blocked on the author supplying a real contact destination this session cannot invent; and RS-029 (residual), whose own row says to build it "when there are people" — not a gap, a sequencing decision the task itself states.

### SUGGEST-01 — colophon accuracy audit
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: `hugo/data/substrate.yaml`'s "Server location" and "Watershed" rows both said "Not yet known — the site is unhosted at v0.2," while the same table's "Security headers" row, three rows down, correctly named "GitHub Pages — this site's current host." `hugo/data/faults.yaml`'s fault 01 repeated the same unhosted claim. "Page weight" said "Under 60 KB per page" against pages that had grown, over the course of this session's own content additions, well past that — `Archive.dc.html` alone is now ~118 KB of uncompressed HTML, nearly double the stated ceiling, with no images or video involved at all~~
now: "Server location" and "Watershed" corrected to state what checking the repo directly confirms — a live `CNAME` file for `relationalsovereignty.com` and a `.github/workflows/deploy.yml` that ships every push to GitHub Pages — the site *is* hosted, and the reason the specific city, operator, and territory still can't be named is that GitHub itself doesn't publish which physical facility serves a given Pages deployment. That's a different, more precise problem than "nothing exists yet," and the rows now say so. "Page weight" corrected to the real current range — about 15 KB (the Invitation) to about 118 KB (the Archive) per page, uncompressed, with the shared ~70 KB `support.js` three pages load noted separately since it's cached once per session rather than re-downloaded per page view. "What is logged" now states plainly that this project deployed to GitHub Pages before auditing what GitHub logs by default for it — a real sequencing gap, named rather than smoothed over. `faults.yaml`'s fault 01 rewritten to match all of the above.

**Verified against the actual repository state, not assumed from memory.** Confirmed live hosting via `CNAME` (contains `relationalsovereignty.com`) and `.github/workflows/deploy.yml` (pushes to `main` trigger a GitHub Pages deploy) rather than trusting the task's own description of what was stale. Confirmed page-weight figures with `wc -c` against every shipped `.dc.html` file and `support.js`/`print.js`/`archive-filter.js`, rather than estimating.

**One claim from the audit's own description was checked and found already fixed.** The task as raised (during this session's earlier codebase familiarization) also flagged "no build step" as stale, citing `prerender.mjs`. Checking the current `hugo/data/substrate.yaml` "Build" row shows it already accurately describes Hugo generating the reading pages and Practise keeping its runtime — that fix predates this session's visible window (likely part of the RS-004 Hugo migration). Not re-fixed, since it wasn't broken; noted here so the audit's own completeness is accurate rather than claiming credit for work already done elsewhere.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean.

### D15 / RS-031 residual — the territory-identification outbound link
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Decision Record D15 resolved a real open question — should the Solidarity Finder embed a territory lookup, or only link out to one — with "outbound link only, no embedded lookup, no reproduced dataset." That decision was never implemented; the verification queue carried "Native Land Digital's terms/disclaimer, for the outbound-link-only territory reference" as still open through every phase of this session until now, and Home's "A website returns no land" section — which already names \"whose territory the servers sit\" as a real jurisdictional question — pointed nowhere a reader could actually go to ask that question about their own location~~
now: a new paragraph on Home, immediately after the existing jurisdictional-questions paragraph, linking to native-land.ca and stating the organisation's own caveat about its map rather than presenting it as authoritative: "a starting point, not a legal record... its map stays a work in progress rather than a finished boundary line." The paragraph also states plainly that this is a link out, not a lookup built in — matching D15's "no embedded lookup, no reproduced dataset" restriction exactly, since it would have been easy to read D15 as merely a suggestion once the original Solidarity Finder shrank to a static directory list rather than the interactive tool it was written against.

**Verified rather than assumed, on both fronts D15 actually cared about.** Fetched native-land.ca's own FAQ directly: confirmed no restriction exists on linking to the site (their attribution requirement applies only to reusing their map data, which this site doesn't do), and confirmed the exact caveat language to use — "not a legal resource... educational and reference purposes only," "a work in progress" — rather than writing a caveat from general knowledge of what such tools usually say.

**Placement chosen over the task's own original context, and why.** D15's title ties this to RS-031 (the Solidarity Finder), which shipped this session as a static directory list rather than the interactive, location-aware tool the original territory-lookup idea was written for — see RS-002+031's own completed entry. Rather than force the link into a Resources section it no longer fits (a static list has no "your location" moment to attach a lookup to), it's placed on Home, where the site already asks "whose territory the servers sit" as one of jurisdictional sovereignty's real questions — the natural home for a tool answering exactly that question about a reader's own location, once the original interactive-Finder context no longer existed to house it.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean; `index.html` re-copied from `Home.dc.html` and confirmed byte-identical. No CSP change needed — this is a plain outbound `<a href>`, exempt from `script-src`/`connect-src` the same way every other outbound citation link on the site already is.

### Direct instruction — Resources' categories made collapsible
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: all eleven Resources categories — crisis lines, coercive control, legal aid, immigration, housing, income, disability, 2SLGBTQ+, Indigenous-led services, mutual aid, international — rendered fully expanded, one after another. The "Choose what you need" contents index (RS-043) let a reader jump to a category, but jumping still landed on a page where every other category was already open too, so the page read as long regardless~~
now: each category is a native `<details>/<summary>` disclosure — the same zero-script pattern used everywhere else on this site (RS-046, RS-026, RS-038) — closed by default. **One exception, deliberate**: "Immediate safety and crisis lines" stays open (`<details open>`), since this site's own binding practice, established across every disclosure shipped this session, is to never hide safety-critical information behind a click. The `<summary>` for each keeps the category's actual heading as a real nested `<h2>` (not just styled text), so a screen-reader user navigating by heading list still finds all eleven categories — collapsing them didn't remove them from the page's heading outline.

**A genuine, previously-unaddressed print gap, caught and fixed in the same edit.** Nothing on this site had ever forced a closed `<details>` open for printing — RS-046's and RS-026's existing disclosures on Practise happened not to need it (RS-046 is export/import controls, meaningless on paper; RS-026's reduced-language text duplicates content already on the page). Resources is the first case where genuinely necessary information — a phone number, a URL — would have been silently missing from a printout. Added `details:not([open])>*{display:block!important}` to the page's `@media print` block. Verified directly against raw DOM layout (`getBoundingClientRect()`), not Playwright's `isVisible()`/`boundingBox()` helpers, which — discovered while testing this — report closed `<details>` content as not-visible even when forced open via CSS and genuinely rendered with real dimensions; a real quirk of Playwright's own actionability heuristics, not of the page.

**A boolean-attribute validator false-positive, found and fixed via a targeted rule change.** `<details open>` (correct, spec-preferred bare-boolean style) fails `html-validate`'s `attribute-boolean-style` rule specifically when checked against this pipeline's *rendered* DOM — Chromium's `page.content()` always serializes boolean attributes as `open=""`, regardless of source style, which is exactly what that rule flags. This is the first time this site has shipped a boolean attribute present at page load (RS-046/RS-026's `<details>` are closed by default, so `open` never appears in their initial markup), so the mismatch never surfaced before. Disabled `attribute-boolean-style` in `.htmlvalidate.json`, alongside the other rules already turned off there for similar not-applicable-to-this-pipeline reasons, rather than write worse source HTML to satisfy a check that's validating a browser-normalized copy rather than the authored file.

Verified: `npm run build && npm run check` pass clean. Real headless-browser testing, JavaScript disabled entirely (this page is `script-src 'none'` regardless): confirmed only crisis-lines starts open; confirmed a real mouse click and a real keyboard `Enter` on a focused `<summary>` both toggle a closed section; confirmed print media forces every section's content to genuine non-zero rendered dimensions; confirmed the existing `#anchor` contents-index links still scroll each collapsed section into view (they don't auto-expand it — the HTML spec's fragment-triggered auto-open only fires when the id is on a descendant of the `<details>`, not on the `<details>` element itself — landing on a collapsed, but visible and clickable, section header is consistent with what was asked for, not a bug).

### Direct instruction — BehindTheScenes' substrate made collapsible
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the substrate's thirteen fields (server location, watershed, page weight, third-party requests, mailing-list processor, transactional email, where the list lives, what's stored, cookies, security headers, what's logged, build, durability) rendered as one long grid under "The substrate" heading, all visible at once~~
now: the field-by-field grid sits inside a `<details>`, summary reading "Show the 13 substrate fields." The section heading and its framing sentence stay outside the disclosure and visible either way — only the grid itself collapses. The count in the summary comes from `{{ len hugo.Data.substrate }}` in the Hugo template, not a typed number, specifically so it can't join the two count-drift bugs this session already found and fixed elsewhere on this same page (the topic count and reading-time estimate, both caught during RS-010 and RS-014).

Same print-open fix applied as Resources: `details:not([open])>*{display:block!important}` added to this page's `@media print` block, so a printed colophon still carries every substrate fact rather than silently dropping them because the section happened to be collapsed on screen.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. A real headless browser confirmed, with JavaScript disabled: the disclosure starts closed, all thirteen `<dt>` fields are present in the DOM regardless, a real click opens it, and print media forces the grid to genuine non-zero rendered dimensions.

### Direct instruction — "What this is not" and the fifth limit moved from Home to BehindTheScenes
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Home carried its own four stated limits (not radical individualism, not relationship anarchy, not a substitute for decolonization, not one universal Indigenous philosophy) plus the chosen-monogamy clarification and the safety callout, and directly beneath that, a fifth limit about the site itself ("A website returns no land") including the native-land.ca outbound link — all of it on the page meant to be the site's short, warm entry point, ahead of the six-doors grid a first-time reader is supposed to actually land on~~
now: both sections live on `BehindTheScenes.dc.html`, under a new `#limits` section titled "Four limits, stated before anything else," placed first in the page's contents menu and first in the page body — ahead of the substrate, since a reader arriving at the colophon by choice is exactly the reader ready for this material, unlike a reader landing on Home for the first time. Every sentence carried over unchanged except one: the fifth-limit block's closing reference to "the colophon" (which made sense pointing off Home, at a different page) now reads "the substrate below" and links to `#substrate` on the same page, since it would otherwise have pointed at itself. Home now runs straight from the six-doors grid into the roadmap section with nothing in between — no orphaned heading, no gap in the visual rhythm.

**One stale cross-reference found and fixed as part of the move, not left for later.** Learn's treaty section had a sentence ending "...exactly the kind of 'lifestyle experimentation' borrowing of Indigenous political thought that Home's own stated limits already rule out," linking to `Home.dc.html`. Since the limits no longer live there, this would have silently become a dead pointer — a link that still resolves (to Home) but no longer supports the sentence's claim once a reader clicks through and finds nothing about limits there. Repointed to `BehindTheScenes.dc.html#limits`, reworded from "Home's own" to "this site's own" since the limits are no longer Home's to claim. Grepped the rest of the repo for other references to the moved content (`"fifth limit"`, `"four limits"`, `"returns no land"`) and confirmed no other live page pointed at it.

**A third reading-time drift, found while editing this exact page and fixed in the same pass.** `BehindTheScenes.dc.html` still said "about 6 minutes" — the same placeholder Learn carried before RS-010/RS-014 corrected it — against a page that measured at just under 10,000 words in `<main>` even before this move added another full section to it. That's the same drift bug this session has now caught three times (Learn twice, this page once), always for the same reason: an estimate set early is never revisited when later tasks lengthen the page. Corrected to "about 39 minutes," computed from the actual current word count rather than estimated.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. Real headless-browser check, JavaScript disabled: confirmed `Home.dc.html` no longer contains "Four limits," "Not radical individualism," or "A website returns no land"; confirmed `BehindTheScenes.dc.html` contains all three, that `#limits` renders as a real, non-zero-height section, that the new contents-menu link's visible text reads "What this is not," and that the adapted closing sentence now reads "The substrate below" rather than the old self-referential phrasing. `index.html` re-copied from `Home.dc.html` and confirmed byte-identical.

### Direct instruction — Resources.dc.html migrated into the Hugo pattern
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Resources.dc.html was one of only two reading pages (the other, by design, being Home/Practise/Contribute's runtime-dependent group) never brought into the Hugo migration RS-004 completed for Manifesto, Invitation, Learn, Archive, and BehindTheScenes. Every one of its eleven categories and roughly thirty individual entries — name, URL, description, contact line — lived as raw HTML with inline styles on nearly every tag, meaning a routine edit (a changed phone number, a new crisis line, a corrected URL) required opening dense, style-interleaved markup with no separation between the two~~
now: `hugo/data/resources.yaml` holds all eleven categories as plain data — `id`, `title`, `scope`, `open`, and an `entries` list of `name`/`url`/`linkLabel`/`description`/`contact` — no markup anywhere in the file. `hugo/layouts/resources.html` renders it with a `{{ range }}` loop, identical in shape to how Archive's citation groups and Learn's principles already work. The handful of one-off connective sentences that cross-reference another category or page (the ShelterSafe pointer from housing, the field-guide pointer from coercive-control, the two fault-list pointers from legal-aid and mutual-aid) stay hardcoded in the layout template rather than the data file, matching this site's existing convention: data holds the repeated, list-shaped content; one-off editorial prose with cross-page links stays in the template. `Resources.dc.html` joins `HUGO_PAGES` in `build-hugo.mjs` and is generated the same way as the other five migrated pages from here on — hand-editing the committed file directly is no longer the workflow for it.

**Zero visible or behavioral change — verified as a refactor, not a rewrite.** Diffed the generated output against the pre-migration file: differences are limited to Go template whitespace (blank lines from `{{ range }}`/`{{ end }}`, harmless) and standard Go `html/template` entity-escaping of data-driven text (`'` → `&#39;`, `+` → `&#43;`) — the same escaping already present in 34 places on `Archive.dc.html` and 178+6 places on `BehindTheScenes.dc.html`, not a new inconsistency this migration introduces. One real difference caught and fixed before shipping: a naive `{{ range }}` applied the same `margin:0 0 2rem` to every category including the last one, where the original deliberately used `margin:0` to avoid a double gap before the closing section — fixed with an index comparison (`{{ $last := sub (len ...) 1 }}`) so the last category keeps its original, intentional margin.

**Confirmed with a real headless browser, JavaScript disabled** (this page stays `script-src 'none'`, unchanged by the migration): all eleven categories present with `crisis-lines` the only one open by default; a real click opens a closed section; every cross-reference note (ShelterSafe, field guide, both fault-list pointers) is present and correctly nested inside its own category's `<details>`, checked via `textContent` rather than `innerText` since closed-`<details>` content is excluded from the latter — the same Playwright quirk documented when this page's collapsible disclosures first shipped; print media still forces closed sections to genuine non-zero rendered dimensions, since the print CSS lives in the layout template unchanged.

**One stale accuracy issue caught and fixed while touching `substrate.yaml`'s "Build" row.** It named only "Manifesto, Learn, Archive, this page" as Hugo-generated, already missing Invitation from an earlier migration and now also missing Resources. Corrected to name all six current Hugo pages, and rewrote the trailing sentence about Archive's filter script — the original phrasing implied its convenience came from "Practise's small runtime," which checking `hugo/layouts/archive.html` shows is inaccurate; it's `archive-filter.js`, a separate small script, not shared with Practise's tool runtime. Also added a sentence naming Home, Practise, and Contribute as the three pages keeping the dc-runtime, and why (live interactive logic Hugo's static output can't hold) — stated nowhere on this row before.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean, including a new "Resources.dc.html matches its Hugo-generated output" line in `check-pages.mjs`'s output that didn't exist before this task.

### UX-01 + UX-02 — Resources' collapsible categories get a visible disclosure cue and a working jump menu
**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Resources' eleven collapsible categories (shipped earlier this session) rendered with `summary{display:flex}`, which silently suppresses the browser's native disclosure triangle — closed categories carried no visual sign they could be opened at all, indistinguishable from a heading with nothing under it. The page's own "Choose what you need" jump menu compounded this: clicking a category link scrolled to the section without opening it, so following the site's own shortcut still landed on a wall of shut headings. Separately, `mutual-aid` (genuinely empty by design) and every populated-but-closed category used the identical visual form, so a reader who opened nothing learned the wrong thing about both.~~
now: every `<summary>` carries a visible chevron, drawn with `summary::before{content:"▸"}` and rotated 90° via `details[open]>summary::before{transform:rotate(90deg)}` — pure CSS, no script, since this page stays `script-src 'none'`. The jump menu now actually opens its target: each category's `id` moved off the `<details>` element and onto a wrapping `<div>` around its content instead. This isn't cosmetic — it's what makes the fix work without JavaScript at all: the HTML living standard's fragment-navigation "reveal" algorithm auto-opens an ancestor `<details>` only when the link's target id is on a *descendant* of it, never when the id is on the `<details>` element itself (documented earlier this session, when Resources' collapsible categories first shipped, as expected-not-broken behaviour — it turns out the actual fix was always available, just not yet applied). `crisis-lines` keeps `open` by default, unchanged. Populated categories now show an entry count in the summary badge ("3 entries · Canada, national"). `mutual-aid` — the one category that's genuinely empty, not thin — is no longer a `<details>` at all: rendered as a plain, always-visible, visibly muted row (dashed top border, muted heading colour) so it can never again be mistaken for a closed-but-populated section.

**Found by an independent UX audit, not this session — see `docs/audits/ux-audit-2026-08-08.html`.** Both findings concern work this session shipped earlier (Phase 2.5's collapsible-categories task, later migrated to Hugo). Treated as first priority in `tasks.md`'s Phase 6 for that reason: the underlying bug — ten of eleven categories effectively invisible — is exactly the "never hide safety-critical information" practice this session held to everywhere else. The practice held for the one category deliberately left open; it didn't yet cover the other ten, which is what this fixes.

**One deliberate departure from the audit's own literal recommendation, on record before this was built.** The audit's strongest suggested fix for UX-01 is shipping every category open by default. Not taken as written, since it would reverse a direct, recent author instruction earlier this same session asking specifically for Resources to be collapsible. Fixing the chevron and the jump-menu instead closes both real defects (invisible affordance, broken navigation) without silently overriding that instruction — recorded in `tasks.md`'s Phase 6 header before this task was built, not decided after the fact to justify what shipped.

**One contrast regression caught and fixed before shipping, not after.** The first pass at `mutual-aid`'s "dimmed" treatment used `opacity:.72` on the whole block — `npm run check`'s axe pass caught it immediately (`color-contrast`, 3 nodes, serious). Replaced with explicit, pre-verified colour choices (`#585B4F` for the heading, already used elsewhere on this page and measured at 5.5:1 against the page background) plus a dashed border instead of a solid one, avoiding the opacity approach's blanket contrast cost entirely.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean. Real headless-browser check, JavaScript disabled (unchanged — this page carries none by design): direct navigation to `Resources.dc.html#coercive-control` now opens the target `<details>` (previously stayed closed, confirmed both before and after this fix); clicking the "Choose what you need" jump-menu link for Immigration opens it the same way; the chevron's computed `transform` flips from `none` to a 90°-rotation matrix on open; `crisis-lines` still starts open; entry-count badges render correctly with singular/plural handling (`1 entry` vs `3 entries`); `mutual-aid` confirmed as a `<div>`, not a `<details>`, with its explanatory text always present in the DOM; Learn's existing cross-reference to `Resources.dc.html#coercive-control` still resolves and now also auto-opens, a free improvement from the same fix; print media still forces all content to genuine non-zero rendered dimensions regardless of on-screen open/closed state.

*(Everything else in `tasks.md` Phase 0 / Phase 1+ remains open.)*

### UX-03 — "Clear everything" on the Consent Domains Map now asks twice

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: "Clear everything" sat beside "Print this map" at the top of the tool and wiped `who`, every domain's pick and note, and the filename in a single `setState` call the instant it was clicked. Nothing on this page is saved unless the reader exports or prints it, by design (RS-027) — so one stray tap could end a session that had taken most of the stated twenty minutes, with no confirmation, no undo, and nothing to recover from.~~
now: the button moved off the top action row — it no longer sits next to "Print this map" — to its own row at the true end of the map, after the twenty domains and the "After you fill it in" reflection questions, introduced by a line stating plainly why it asks twice ("nothing above is saved unless you printed or exported it"). It now takes two presses: the first arms it (`resetArmed: true`), swaps its own label to "Click again to clear everything," switches its border/text to the site's existing error red (`#8B3A2F`, already used for the dispatch form's validation error — no new colour introduced), and starts a 5-second window; a second press inside that window clears for real and disarms. Letting the window lapse without a second press — the accidental-click case this exists for — reverts the button to its resting state with the data completely untouched. A `role="status" aria-live="polite"` line beside the button announces the armed state ("Press once more to permanently clear every domain, note, and the name above.") for screen-reader users, who would otherwise have no reason to re-focus the button to notice its label changed.

Notes:
- **Found by the same independent UX audit as UX-01/02** — see `docs/audits/ux-audit-2026-08-08.html`, finding UX-03, the last of Phase 6's three Criticals.
- **No new dependency, no scripted show/hide beyond what the page already uses.** The confirm state is plain component state (`resetArmed`) rendered through the same `{{ }}` interpolation every other dynamic style/label on this page already uses (see the domain options' `border`/`bg`/`fg` pattern) — nothing here is a new pattern for this codebase.
- **The 5-second window is a `setTimeout`, not tied to any other interaction.** The audit's suggested fix also offered reverting "on any other interaction elsewhere on the page" as an alternative; that would mean instrumenting every input's `onChange` across all twenty domains just to cancel one button's state, for no behavioural gain over a plain timeout — the timeout alone already guarantees a stray click costs nothing, which was the actual defect.
- Verified with a real headless browser, JavaScript enabled (this page keeps the runtime by design): first press arms without touching `who`/`picks`/`notes`; letting the window lapse leaves all three fully intact; a second press inside the window clears them and returns the button to its resting label; the top action row no longer contains a "Clear everything" button anywhere in the DOM. `axe-core` (WCAG 2.1 A/AA + best-practice) run against both the armed and unarmed states: zero violations either way. The rendered DOM (post-render, matching how `scripts/check-pages.mjs` validates every other page) passes `html-validate` against this project's own `.htmlvalidate.json` config, and no console or page errors were raised in either state.

*(Phase 6 remaining: UX-04 through UX-22. Phase 7, reconciling an external SEO/AEO spec against this project's ethos, added the same session — see `tasks.md`.)*

### UX-04 + UX-06 + UX-11 + UX-16 — mobile nav collapses, current page gets a real signal, Home's footer and every page's hover contrast fixed

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the header's eight nav links wrapped to three rows on every page at mobile widths — a fixed 247px, 30–43% of a mobile viewport, on every single page, every load. The current page was signalled by recolouring its link and nothing else, with no `aria-current` anywhere on the site. Home's footer nav was `flex-direction:column` while every other page's was already `flex-wrap:wrap`, making Home's mobile footer 931px against 230–336px everywhere else. `a:hover{color:#3F7A4E}` measured 4.05:1 against the light pages' background, below AA — and, unmeasured until this task, 2.95:1 against Manifesto's dark background, considerably worse.~~
now: below 700px, the header collapses behind a 44px `<details>` toggle labelled "Menu," reducing the mobile header to 123px on every page (247px → 123px, verified identically on all nine). The current page carries `aria-current="page"` plus an underline in the page's own existing accent colour — Invitation's `#7D5915` and Manifesto's `#DB9E2A` preserved exactly, not overwritten with a generic value. Home's footer nav now matches the `flex-wrap:wrap` pattern already used everywhere else (931px → 713px; the remainder is Home's own longer footer copy, not a layout defect). Link hover is `#2C5A38` (6.33:1) on the eight light pages and `#509C64` (4.52:1, already used on Home's dark roadmap section) on Manifesto — two different values because one page has a dark background, not one fix copy-pasted nine times.

**The collapsing nav is two mutually-exclusive copies of the same eight links, not one `<details>` shown and hidden — this was not the first design, and the reason matters for anyone touching this again.** The obvious approach — wrap the existing `<nav>` in `<details class="nav-toggle">`, hide the `<summary>` and force the `<nav>` visible at desktop via CSS regardless of the `open` attribute — silently fails in Chromium specifically when the `<details>` element is a flex item (direct or nested, confirmed both ways) of a `display:flex` ancestor, which the header's wordmark+nav row is on every page on this site. `getComputedStyle` reports the override applied (`display:flex`) but the element still renders at zero size — a real, empirically-confirmed browser limitation, not a specificity or `!important` problem (`!important` doesn't fix it either). The failure is asymmetric: forcing a *closed* `<details>` to render as open fails inside a flex ancestor; forcing an *open* one to render as closed, or letting a real click natively toggle it, both work fine. The shipped fix uses only the safe half of that asymmetry: a plain `<nav id="primary-nav">` (today's markup, unchanged) hidden at mobile via `@media(max-width:700px){#primary-nav{display:none!important}}` — `!important` here because the nav's own inline `style="display:flex"` otherwise outranks the media-query rule — and a second, independent `<details class="nav-toggle">` copy of the same eight links, hidden at desktop the same safe way, relying on 100% native (unoverridden) `<details>` behaviour for its own open/close. Two copies of eight links is more markup than a single shared element, but every simpler shape that was tried actually failed in a real browser; this one didn't, verified below.

**The toggle's revealed content had to stop being a second `<nav>` landmark.** The first working version gave the mobile copy its own `<nav aria-label="Primary">`, which passed every `axe-core` check (axe correctly excludes non-rendered content from most rules) but failed this project's own `check-pages.mjs` — `html-validate`'s `unique-landmark` rule reads raw markup, not computed style, so two identically-labelled `<nav>` elements in one document fail it regardless of which one is visible. The mobile copy is a plain `<div>` instead: still fully keyboard- and screen-reader-navigable (link semantics don't depend on a landmark wrapper), reached through the already-labelled "Menu" `<summary>`, and there is exactly one `<nav aria-label="Primary">` in the DOM again.

**Two small pre-existing drifts fixed while these exact headers were open for editing, not left for a future session to rediscover.** `Home.dc.html` and `Contribute.dc.html`'s `<header>` were missing `class="noprint"`, present on the other seven pages since RS-004 — both now hide correctly on print, matching the rest of the site. `Contribute.dc.html`'s wordmark carried `letter-spacing:-.01em` against every other page's `-.02em` — normalised.

**UX-11's scope was smaller than the audit's own table said, checked before fixing rather than after.** The audit filed UX-11 against "All 9" pages; measuring the actual live mobile footer height on every page first showed Home alone at 931px, with the other eight already at 230–336px because their footer nav was already `flex-wrap:wrap` — only Home had drifted to `flex-direction:column`. The fix matches Home to the convention seven other pages already use, rather than inventing a new two-column grid pattern across nine files as the audit's own suggested fix proposed. `tasks.md`'s UX-11 row corrected to record the real scope.

**A Hugo partial for the header was considered and not built, on purpose.** UX-04/06/11/16 together touch nine separate source files (six `hugo/layouts/*.html`, plus `Home.dc.html`/`Practise.dc.html`/`Contribute.dc.html` by hand) with no shared stylesheet or template between them. A `hugo/layouts/partials/header.html` looked like the obvious deduplication — until the `<details>`-in-flex discovery above made clear the fix's *shape* (two copies, not one togglable element) was the real open question, and a partial would not have reached the three non-Hugo pages regardless. Applied by hand nine times instead, each one verified against its own live DOM rather than assumed identical from the first file. Revisiting the partial once Phase 5 (`HUGO2-01`–`03`) brings Home/Practise/Contribute onto Hugo would then cover all nine from one template — not done here, since that migration hasn't happened yet.

**Two unrelated, pre-existing defects surfaced by testing every page at every breakpoint rather than trusting one page's numbers, fixed in neither — logged instead.** A full `axe-core` pass at 375px (the audit's own scan ran at 1280px only) found Archive's "Held in common" diagram — `overflow-x:auto`, already known and disclosed in copy — has no way to focus it via keyboard (`scrollable-region-focusable`, serious), and confirmed this predates every edit in this task by testing the untouched file directly. The same full-matrix pass found Resources' entry-count badge ("1 entry · Canada — thin coverage, stated honestly") overflows its container by ~26px at 375px, also confirmed pre-existing. Neither is UX-04/06/11/16's fault or fix; both are now `tasks.md` SUGGEST-08/09 rather than silently left for the next person to rediscover.

Verified: `npm run build:hugo && npm run build && npm run check` all pass clean across all eleven checked pages (nine site pages, `index.html`, `glyph-check.html`) — HTML validation, `axe-core`, console-error checks, the Hugo-output-sync check, and the `index.html`-matches-`Home.dc.html` byte check. A dedicated full-matrix pass beyond `npm run check` measured, on every one of the nine pages at both 375px and 1280px: mobile header 247px → 123px (uniform); desktop header unchanged at 77px with the toggle's `display:none` confirmed and all eight links present; zero horizontal overflow introduced (the one pre-existing Resources overflow, above, confirmed unchanged from before this task); zero `axe-core` violations introduced (Archive's one pre-existing violation, above, confirmed present before this task's edits too, at the same count). Real interaction, not just markup inspection: mouse click on the mobile toggle opens it and every link becomes genuinely clickable; keyboard Tab reaches the toggle as the third stop on every page, Enter on a focused (not just clicked) toggle opens it exactly as a mouse click does, and Tab from there proceeds through all eight revealed links in order before continuing to the page's own content — traced explicitly rather than inferred, after an initial synthetic-Tab test gave an inconsistent trace that further isolation attributed to the test tooling, not the page (direct `.focus()` + `Enter`, and a DOM-order focusability check, both agreed with each other and with the mouse-driven result).

### UX-05 — Home's hero recomposed so the six doors sit closer to the fold

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: the hero used only the left ~42ch of the 1120px container — the H1, the standfirst paragraph, and the rotating-question box all stacked in a single column, one below the next, with the entire right half of the section empty except for the decorative drift SVG. The rotating-question box was also styled as a bordered, filled button (`background:none;border:1px solid #C9C6BA`) — visually the strongest call-to-action on the page — even though clicking it only swaps a line of text and goes nowhere. The first door card sat at 1,272px on a 900px-tall desktop viewport and 1,366px on mobile.~~
now: the standfirst paragraph and the rotating-question box sit side by side in a two-column `grid-template-columns:repeat(auto-fit,minmax(300px,1fr))` row below the (unchanged) full-width headline, using the horizontal room that previously sat empty; the hero's own vertical padding is reduced (`clamp(3rem,7vw,5.5rem)` → `clamp(2.2rem,5vw,3.5rem)` top). "another question" is now a plain underlined text control (no border, no fill) sized the same 44px tap target as before, so its visual weight now matches that it replaces a line of text rather than taking a real action. The first door card moved to 1,110px on desktop (was 1,272px) and 1,207px on mobile (was 1,366px).

**The first version of this fix wrapped the H1 into the same grid row as the question box, and made the headline visibly worse before it was caught.** Putting the headline in a `minmax(300px,1fr)` column halved its available width at desktop — but its `font-size:clamp(2.1rem,6.4vw,4.4rem)` scales off *viewport* width, not column width, so the same large text now wrapped across six lines instead of four, in a screenshot taken and looked at before this shipped, not assumed correct from the CSS alone. Moved the headline back outside the grid, full-width and completely unchanged, and paired only the standfirst paragraph — ordinary body text, no viewport-scaled sizing — with the question box instead. Same visual outcome the audit asked for (use the empty right side of the container), achieved without the headline ever wrapping differently than it did before this task.

**The first door row is meaningfully closer to the fold, not fully inside it, and that's reported as what it is.** 1,110px on a 900px desktop viewport is 210px into a second screen, down from 372px — a real improvement, not the full "first row visible in one screen" the audit's phrasing could be read as promising. Closing that remaining gap would mean either shrinking the headline itself (not what the audit asked for, and a real cost to the page's own visual identity) or removing the reduced-but-still-present padding entirely (crowding the page). Left as the honest stopping point of "reduce padding + use the empty space," not stretched further to claim a fold-fit that would need a different, uncommissioned change to actually deliver.

Verified: `npm run build && npm run check` pass clean (`index.html` re-copied from `Home.dc.html` after the edit, byte-match check green). Real headless-browser check at 1280px and 375px: the rotating-question button still changes the displayed question on click (`aria-live="polite"` region content confirmed different before/after) and the layout collapses to a single stacked column below the grid's ~600px combined-minimum width, matching every other `auto-fit`/`minmax` grid already on this site rather than introducing a new breakpoint. Zero `axe-core` violations, zero console/page errors, zero horizontal overflow, at either width.

### UX-07 + UX-08 — Learn and Archive get a contents nav; Archive's reading order gets real links

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Learn (13,995px desktop / 22,709px mobile) and Archive (11,896px / 23,900px) had no way in beyond scrolling — no in-page anchors of any kind past the skip link, on the two longest pages on the site. Behind the Scenes had solved this for itself with a nine-entry `<nav aria-label="Contents">`, never applied to the two pages that needed it more. Separately, Archive's own "The fastest honest route in" — ten numbered titles positioned as a curated reading order — were plain text, none of them pointing at the shelf entry each one named.~~
now: both pages carry an `aria-label="Contents"` nav in the same visual pattern as Behind the Scenes' own. Learn's lists its 11 major sections (the thirteen principles are addressed as one entry, `#principles`, rather than listed individually — see below for why); each of the 13 principles additionally carries its own `id="principle-01"`…`id="principle-13"`, generated from `hugo.Data.principles`' own `.n` field, so any one of them is now a link a reader can be sent directly to. Archive's contents nav lists all nine shelf groups (built with `{{ range hugo.Data.archive.groups }}`, so a tenth group added later appears automatically) plus "Held in common," "The fastest honest route in," and the closing "Absence" note. Every one of Archive's 60 individual entries also carries a stable id, and all ten "fastest route" items are now real links to the specific entry each one names.

**Getting the "fastest route" links right meant reading the full 523-line data file, not pattern-matching on author surnames.** The ten entries are display-abbreviated ("Wildcat — Replacing Exclusive Sovereignty") against 60 full entries across nine groups, and three authors — Wildcat, TallBear, Mingus — each have more than one entry on the shelf. Grepping for "Wildcat" alone turns up three different works; a fuzzy or first-match link would have had roughly even odds of misattributing a citation on the one page this site is most exacting about getting attribution right. Read the whole file instead, matched all ten by both title fragment and author against the one full entry that could be the only correct target, and recorded the mapping before writing a line of template — a wrong link here isn't a UX bug, it's putting the wrong name on someone's work.

**The link target is stored as data, not inferred at render time, and a comment says why.** `hugo/data/archive.yaml`'s `sequence` list changed from ten bare display strings to `{ display, title }` pairs, where `title` is the *exact* full title of the shelf entry copied verbatim from `groups` above. The template computes both the shelf entry's id and the sequence link's target from the identical `{{ .title | anchorize }}` expression, so the two can never independently drift into disagreement — but the two `title` strings themselves (one in `groups`, one in `sequence`) are still two copies a human could edit out of sync. Flagged directly in the YAML: renaming an item's title without updating the matching `sequence` entry breaks that one link silently. Not a risk this task could design away entirely — `sequence`'s whole reason to exist is an editorially curated *subset* with its own abbreviated display text, distinct from the master list it draws from — but named on the page it would bite, not left implicit.

**`urlize` was the wrong function, and shipping it once before catching it is worth recording.** IDs were first generated with `{{ .title | urlize }}` — the function used elsewhere in this Hugo install for page-relative URLs — which does not strip `/`, and does produce ids that fail HTML5's `id` syntax on two titles that contain a slash in normal punctuation ("Heterosexualism and the Colonial/Modern Gender System," "Fraught Intimacies: Non/Monogamy in the Public Sphere"). `npm run check`'s `html-validate` pass caught both immediately (`valid-id`, two files) — not eyeballed and missed. Switched to `anchorize`, Hugo's own function for exactly this (HTML anchor/id generation, not URL paths). One further case `anchorize` doesn't handle: a title starting with a digit ("10 Principles of Disability Justice") produces an id starting with a digit, which `html-validate`'s ruleset (stricter than bare HTML5 on this point) still rejects. Rather than special-case that one title, every item id carries a fixed `entry-` prefix — guarantees a letter-first id regardless of what a future title starts with, at the cost of a slightly longer id than the bare anchorized title alone.

**Learn's contents nav lists sections, not all thirteen principles, on purpose.** BehindTheScenes' own nav (the pattern being reused) has nine entries; Archive's ended up with twelve. Listing all thirteen principles individually alongside Learn's other ten sections would have produced a 23-row nav for an 11-section page — worse to scan than the page it's meant to help navigate. The principles get one entry, `#principles`, pointing at the top of the block; each one is still individually addressable by URL (`Learn.dc.html#principle-09`), which is what "send someone a specific principle" actually needs — a contents-nav row for each one is a different feature (a same-page jump list) this task wasn't asked to build.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all eleven pages, including the two `valid-id` failures above caught and fixed before this shipped, not after. Real headless-browser checks: every href in both contents navs resolves to a real element in the live DOM (11 on Learn, 12 on Archive); all 13 `principle-NN` ids present; all 60 Archive entry ids confirmed unique (zero collisions across the full shelf); all ten "fastest route" links independently re-verified post-fix by reading the *rendered* DOM's target `<h3>` and byline back out and confirming each matches its display text's author and title fragment, not just that a target element exists; a real click-driven scroll (not just `location.hash`) confirmed the browser actually moves a clicked group to the top of the viewport. Zero `axe-core` violations, zero console or page errors.

### UX-09 + UX-14 — dispatch form errors mark the field they're about; hints stop hijacking accessible names

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: on both dispatch forms (Home, Contribute), submitting an incomplete email produced a `role="alert"` message that was correctly announced but changed nothing about the email field itself — no `aria-invalid`, no border change, no moved focus — and rendered *after* the submit button, below every other field, rather than near the input it described. Separately, three fields across the two forms (Home's "Name or pseudonym"; Contribute's "Name or pseudonym" and its "Optional contribution note") wrapped their hint text inside the same `<label>` as the field's actual name, so each field's accessible name became the full label sentence plus the entire hint paragraph concatenated together — audible in full, every time, to a screen-reader user focusing the field.~~
now: a failed submit sets `aria-invalid="true"` and switches the email input's border to the site's existing error red (`#8B3A2F`, already used for this exact error state elsewhere — no new colour), moves focus to the input directly (`document.getElementById("...-email")?.focus()`), and renders the error message immediately below that field instead of after the button. The three hint-in-label fields are restructured identically: the label wraps only the field's actual name (with an explicit `for`/`id` pair now that it no longer wraps the input), the hint became its own `<p id="...-hint">` sibling in the same visual position as before, and the input carries `aria-describedby` pointing at it — same reading order for a screen reader (name, then hint, then field), but the field's *accessible name* is now just its name.

**The network-failure path deliberately does not touch the email field's state.** The submit handler has two distinct failure modes: a client-side format check (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`) before anything is sent, and a `fetch` failure or non-OK response after. Only the first sets `emailInvalid: true` — the second's error ("That did not send. Check your connection and try again.") is about the network, not the address, and marking a perfectly valid email as invalid there would be wrong. `emailInvalid` resets to `false` the moment the format check passes and sending starts, so a corrected, valid address never carries a stale invalid state into a later network failure.

**Fixed on all three instances of the hint-in-label pattern found, not only the one field the audit named.** The audit's UX-14 finding cited "Name or pseudonym" on both forms as the example; reading both forms fully before touching either found a third, identical instance — Contribute's "Optional contribution note" textarea has the same hint-wrapped-in-label structure and the same bug. Fixed alongside the other two rather than left for a second pass once found.

**Removing the error message's old location, not duplicating it.** The straightforward version of this fix adds a new `<p role="alert">` near the field and leaves the existing one after the button in place, which would render the same error twice and announce it twice to a screen reader on every failed submit. The old block is deleted, not kept as a fallback — verified by counting `[role=alert]` elements in the live DOM after a failed submit (one, on both forms) rather than assuming the removal was clean.

Verified: `npm run build && npm run check` pass clean (`index.html` re-copied from `Home.dc.html`, byte-match green). Real headless-browser check on both forms: submitting empty moves focus to the email input, sets `aria-invalid="true"`, and renders the border in the error colour, confirmed via `getComputedStyle` rather than reading the CSS source; exactly one `role="alert"` element exists in the DOM afterward on each form; each restructured field's `<label for="...">` text is now the field name alone, separate from its hint's own `id`, and `aria-describedby` on the corresponding input resolves to that hint. Zero `axe-core` violations, zero console or page errors, on either form.

### UX-10 + UX-12 + UX-13 + UX-15 + UX-22 — Home's grids, dispatch repetition, and a Resources route

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Home's roadmap (four stages: Open now / In progress / Next / Someday) and footer (four blocks) both used `repeat(auto-fit,minmax(240px,1fr))`, which wraps to three columns plus one orphaned onto its own row inside the 1,024px usable width of the 1,120px container — breaking the roadmap's own left-to-right progression and stranding the footer's "Reuse" block under "Build." The dispatch section's privacy assurances appeared four times on Home (a door-card summary, four left-column bullets, a form footnote repeating two of those bullets 400px away, and the footer's Build column) and were repeated again on Contribute with no line connecting the two forms — a visitor who joined on Home had no way to know Contribute's form was the same list, not a second one. The two dispatch columns ended at different heights because the left column's four bullets ran longer than the form, leaving trailing whitespace under the submit button. Resources had no link anywhere on Home outside the header and footer nav, despite being named as an option in the dispatch's own interest checkboxes.~~
now: both grids' `minmax` floor is 200px, and both render their full four columns at every desktop width tested — confirmed by reading `gridTemplateColumns` back from the live DOM, not assumed from the CSS value alone. Home's form footnote is deleted (the bullets 400px to its left already say the same thing); Contribute's own footnote is untouched, since Contribute has no separate bullet list of its own to duplicate against — removing it there would leave Contribute's form with zero privacy statement, not zero redundancy. Contribute's intro paragraph gains one clause: "This is the same list as the one on the homepage, not a second one — an address already on it does not need to be entered twice." Home's closing block gains one new sentence — "If what you need is Resources, it doesn't wait for the end of a page." — ahead of the existing "go to the writers themselves… all three are correct" line, which is untouched rather than edited to say "four."

**UX-13 shipped scoped down from the audit's literal suggestion, and the reason is architectural, not a judgment call.** The audit's fix says "extract one shared dispatch component." This codebase has no mechanism for that: each page is a single self-contained file, and each one's `<script data-dc-script>` block defines its own `Component` class with its own `state`, independently — there is no shared JS module, import system, or bundler anywhere in the site (deliberately, per D2's "no supply chain, nothing to rot"). Building one now would mean introducing exactly the kind of shared-dependency architecture this project has avoided everywhere else, for two forms small enough that duplicating twelve lines of interest-checkbox markup is not itself a maintenance burden. Shipped the part of UX-13 that's actually about the user-facing problem — "no stated relationship between them" — with one sentence, and left the component-extraction idea out rather than force a structural change the rest of the codebase argues against.

**UX-22 needed no separate fix, and that was confirmed rather than assumed.** The audit's own note on UX-22 says "largely resolved by UX-12's consolidation" but still lists "align both columns to the top of the form" as a fallback. Measured both dispatch columns after the footnote removal alone: 656px and 656px, exactly equal. No additional alignment rule was added — there was nothing left to fix.

**The existing "all three are correct" sentence was not touched to make room for a fourth option.** Home's closing paragraph names writers/tool/close-the-tab as three deliberately-equal choices, stated as a complete sentence. Adding Resources as a fourth item there would mean rewriting "three" to "four," which is editing existing copy, not adding new copy — outside this task's mandate. The Resources sentence instead stands on its own, immediately before that paragraph, making a fourth kind of point (support you might need *before* finishing the page) rather than a fourth item competing inside a sentence built around exactly three.

Verified: `npm run build && npm run check` pass clean (`index.html` re-copied from `Home.dc.html`). Real headless-browser check: `gridTemplateColumns` on both the roadmap and footer grids confirmed 4 tracks; the form-footnote sentence confirmed absent from the rendered DOM's text content; a real link to `Resources.dc.html` confirmed present in Home's closing section; both dispatch columns' rendered heights read back as identical. Zero `axe-core` violations, zero console or page errors.

### UX-17 + UX-18 — the Consent Domains Map gets a progress count and progressive disclosure; the second tool gets announced

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: all twenty of the Consent Domains Map's condition fields rendered open from the first paint, regardless of what — or whether — anything had been picked for that domain, so the form showed its maximum possible length before a single answer existed. Nothing on the page tracked how many of the twenty had been marked, on a task the page's own eyebrow states takes about twenty minutes. Separately, that eyebrow read "Practise — tool 01 of 09," naming only the Consent Domains Map — the page's second, fully independent tool ("An ending, prepared for," unconditionally rendered further down the same page regardless of what stage the first tool is in) had no mention anywhere above it.~~
now: a condition field appears only once its domain is marked "yes, with conditions" or "not yet — ask me" — the two answers that actually invite one — and, once written, stays visible even if the pick is later changed to something else, so a note is never hidden out from under its own text. A running "*n* of 20 marked" count sits above the domain grid, computed from the same `picks` state the domains themselves render from, and — per the audit's specific ask — confirmed present under Chromium's print-media emulation, not just on screen. The top eyebrow now reads "tools 01–02 of 09 · about 20 + 10 minutes to fill in," and a two-item contents nav beneath the safety/privacy callouts links to both: "01 · The Consent Domains Map" and "02 · An ending, prepared for."

**The condition-field disclosure rule intentionally excludes the other three answers, not just the two it targets.** "Yes, freely," "no," and "mine to withhold" are complete answers on their own — a condition field under "no" would invite qualifying a refusal, which is exactly the kind of soft-walking-back the framework argues against elsewhere. Restricting the reveal to "with conditions" and "not yet" isn't a simplification for its own sake; it matches which answers the framework's own vocabulary treats as conditional in the first place.

**Linking to the first tool needed a stable anchor that didn't exist, because the tool itself doesn't have one stable DOM element.** The Consent Domains Map is three mutually exclusive stages (`atSafety` / `atGrounding` / `atTool`), each its own top-level `<sc-if>` block with no shared wrapper — there was no single element "the map" that a link could point to regardless of which stage a visitor is in. Added one: a plain `<div id="consent-map">` now wraps all three stage blocks without touching their conditional rendering, giving the contents nav (and anyone else) something constant to target no matter where in the flow a visitor currently is.

**UX-18's "floating button" turned out to already have a heading — what it lacked was upstream warning.** The endings tool's "Open this tool" button sits directly under its own `<h2>` and its own "tool 02 of 09" eyebrow; it was never literally headingless. What actually matched the finding is that the whole second-tool section renders unconditionally — a visitor still on the *first* tool's safety screen can already see a full second tool's intro further down the same page, with no warning above that a second tool exists at all. Fixed at the point that's actually true: the top eyebrow and contents nav now say up front that there are two, rather than adding a heading the button already had.

Verified: `npm run build && npm run check` pass clean. Real headless-browser interaction: picking "yes, freely" leaves the condition field hidden and increments the count; picking "conditions" reveals it and increments the count; typing a note and then switching that same domain's pick to "no" leaves the field visible with the typed text intact, not cleared; both contents-nav links resolve to a real element and produce an actual scroll (not just a hash change); `document.getElementById("endings-tool")` and `"consent-map"` both confirmed present regardless of which of the first tool's three stages is active. Print-media emulation (`page.emulateMedia({media:"print"})`) confirmed the marked count and a filled condition field both remain visible while the header correctly disappears, rather than assuming `.noprint`'s scope was safe from a CSS reading alone. Zero `axe-core` violations, zero console or page errors.

### UX-19 + UX-21 — the drift SVG stops fighting the H1 on mobile; the Venn diagram gets a scroll cue

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: Home's decorative drift-circle SVG rendered at full opacity behind the H1 at every viewport width, including below ~700px where the headline is the single most important line on the page and the narrower column gives the circles more relative visual weight against it. Separately, Archive's "Held in common" Venn diagram sits inside an `overflow-x:auto` container that is 700px wide on a viewport as narrow as ~355px of usable width — the page's own monospace caption says outright that it scrolls sideways, but nothing in the diagram itself signalled that off-screen content remained once a reader stopped reading the caption.~~
now: the drift SVG's wrapper carries a new `class="drift-wrap"`, and the existing `@media(max-width:700px)` block (already home to the mobile nav's rules) gains one line, `.drift-wrap{opacity:.3}` — the circles fade to an atmospheric wash rather than competing with the headline's contrast, and desktop is untouched (confirmed `opacity:1` at 1280px via `getComputedStyle`, not assumed from the media query's boundary alone). Archive's scroll container gains `class="venn-scroll"` and one small rule: a 32px-wide right-edge fade, `linear-gradient(to right, rgba(231,229,220,0), #E7E5DC 90%)` — `#E7E5DC` is the page's own background colour, not a new value — with `pointer-events:none` so the fade sits purely as a visual signal and never intercepts the drag/scroll gesture it's there to hint at.

**The Venn fix is the simpler of two real options, chosen for a stated reason rather than by default.** A scroll-position-aware version — two `background-attachment` gradients that appear and disappear as the container actually reaches its scroll boundaries — exists and is the more "correct" behaviour (no fade once there's nothing left to scroll to). It's also fragile to get right from inline styles without a tight visual feedback loop, and this is a `noscript`-safe, no-build-step, hand-authored HTML page with no live-reload workflow to iterate that kind of CSS against. Given UX-21's own "Polish" tier and "XS" effort sizing in the audit, the always-visible fade was shipped instead: one property, testable in a single screenshot, with a known and accepted imperfection — the fade doesn't disappear once a reader has scrolled all the way to the true end of the diagram. The scroll-position-aware version remains available as a future upgrade if the always-on fade ever reads as misleading in practice; nothing about this fix forecloses it.

**The fade needed to be proven harmless at both ends of the width range it touches, not just present.** At mobile widths (390px tested) the container is genuinely scrollable — `scrollWidth` 700 against `clientWidth` 355 — and a real `scrollBy` call confirmed the fade's `pointer-events:none` does not block the drag it overlays. At desktop widths (1280px tested) the same container's content fits without overflowing (`scrollWidth` equals `clientWidth`, `isScrollable: false`), so the fade CSS is present but has nothing to fade against — verified rather than assumed, since a gradient with no overflow beneath it is a null effect, not a bug, but only checking one width would have left that unconfirmed.

**This does not touch SUGGEST-08, and the two shouldn't be conflated.** SUGGEST-08 is the same container's `scrollable-region-focusable` axe finding — the region has no way to be reached by keyboard at all, a functional gap. UX-21 is a purely visual affordance for a mouse/touch reader who can already reach the region but might not know it scrolls. Fixing the fade does nothing for keyboard reachability; SUGGEST-08 remains open, logged, and unchanged by this commit.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all eleven pages. Real headless-browser checks: `.drift-wrap`'s computed opacity reads `0.3` at 390px width and `1` at 1280px width; a mobile screenshot confirms the circles read as a faint wash behind the headline rather than competing with it. `.venn-scroll`'s `::after` confirmed present with the expected gradient, width, and `pointer-events:none` in computed style (not just source CSS); an actual `scrollBy(100,0)` against the container confirmed the scroll position changes despite the overlaid fade; desktop width confirmed non-scrollable so the fade is inert there. Zero `axe-core` violations, zero console or page errors, on either page.

### SEO-01 — WebSite + Article/DefinedTerm JSON-LD on the seven indexable pages

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: none of the site's nine pages carried any structured data — no `WebSite`, `Article`, or `DefinedTerm` JSON-LD anywhere — despite `robots.txt`'s own stated welcome of answer-engine retrieval bots reading these pages "for a person who asked a question." A retrieval system had only the existing `<title>`/`<meta name="description">`/OpenGraph tags to work from, with no machine-readable signal of which page actually defines the site's central term versus which pages merely discuss it.~~
now: `Home.dc.html` (and its byte-identical `index.html`), `Manifesto.dc.html`, `Invitation.dc.html`, `Learn.dc.html`, `Archive.dc.html`, `Resources.dc.html`, and `BehindTheScenes.dc.html` each carry one `<script type="application/ld+json">` block in `<head>`, structured as a two-node `@graph`: a `WebSite` node (`name: "Relational Sovereignty"`, matching the header wordmark used identically on every page) shared verbatim across all seven, plus one page-specific node. Home's second node is a `DefinedTerm` — `name: "Relational Sovereignty"`, `description` copied verbatim from Home's own `<meta name="description">`, which already reads as a one-sentence definition ("Relational sovereignty asks how we can build relationships that protect self-determination while recognizing interdependence, care, responsibility, and the living worlds to which we belong."). The other six pages' second node is an `Article`, with `headline`/`name`/`description`/`url` copied verbatim from that page's own existing `<title>`, `<meta name="description">`, and `<link rel="canonical">` — no field invented, no copy trimmed or reworded to fit the schema. `Practise.dc.html` and `Contribute.dc.html` deliberately get nothing, matching the task's own scope note: both are already `noindex` and disallowed in `robots.txt`, and structured data is specifically designed to be found.

**Home got `DefinedTerm`, not `Article`, because it is structurally the one page that actually defines the term rather than discussing it.** The other six pages' meta descriptions describe *what the page contains* ("Sixteen theses and a refusal…", "Thirty-odd texts on…"); Home's meta description is the only one on the site phrased as a definition of the concept itself. Marking it up as a `DefinedTerm` rather than another `Article` gives a retrieval system a single, unambiguous anchor for "what is relational sovereignty" distinct from the six pages that elaborate on it — which is what `about` linking (below) then connects back to it.

**Four of the six `Article` nodes carry `"about": {"@id": ".../#term"}`; two deliberately don't.** Manifesto, Invitation, Learn, and Archive are each substantively about the concept itself — their own descriptions say so directly ("a manifesto on relationships without ownership," "a second, warmer door into relational sovereignty," "what relational sovereignty is," "texts on relational sovereignty"). Resources and BehindTheScenes are not: Resources' subject is crisis and safety services, BehindTheScenes' is this site's own infrastructure and mechanics. Linking either of those to the `DefinedTerm` as "about" that concept would overclaim a topical relationship their actual content doesn't have — on a site this exacting about not misattributing what a source is about (see UX-08's citation-matching work above), the same standard applies to a claim this project makes about its own pages.

**No `author` or `publisher` field appears anywhere, and no `datePublished`/`dateModified` was invented.** `FLAG-04` (Phase 0) already declined to invent an author/credential schema — this site names no individual and no registered organisation anywhere, stated as a deliberate property of the project rather than a gap (`BehindTheScenes.dc.html`: "At v0.2: one unpaid person, writing and building. Every future contributor, translator, reviewer, and maintainer will be named here individually, not aggregated into a credit line."). Google's Article guidelines treat `author` as recommended, not required, for basic structured-data validity, and nothing in SEO-01's own scope asked for rich-result eligibility at the cost of inventing an identity. Publication/modification dates would have needed a real per-page dated record this project doesn't currently keep; rather than fabricate one, both properties are simply absent from every node.

**Verified, not assumed, that a `script-src 'none'` CSP does not block an inline JSON-LD block.** `Resources.dc.html` is the one page on the site whose CSP sets `script-src 'none'` rather than `'self'` (it ships zero executable JavaScript by design). A `<script type="application/ld+json">` element is a "data block" under the HTML parsing spec, not a script the browser ever attempts to execute — so CSP's script-blocking check never applies to it, on any `script-src` value, including `'none'`. Confirmed empirically rather than taken on spec-reading alone: a real headless load of `Resources.dc.html` shows the block present, its content valid JSON, and zero CSP violations reported to the console — same result on every other page's `'self'` policy.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all eleven pages, `html-validate` included. Real headless-browser checks on all ten pages: exactly one `application/ld+json` block on each of the seven target pages (eight counting `index.html`) and zero on `Practise.dc.html`/`Contribute.dc.html`; every block's `textContent` parses via `JSON.parse` without error; each parsed graph's node types, `@id`s, and names read back as expected (`WebSite` + `DefinedTerm` on Home, `WebSite` + `Article` on the other six); zero Content-Security-Policy violations reported on any page, including `Resources.dc.html`'s `script-src 'none'`. `index.html` re-copied from `Home.dc.html` and confirmed byte-identical.

### BUG-01 + BUG-02 — Invitation's live 404, and the skip-link/nav-toggle first-paint flash

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: `Invitation.dc.html` returned a genuine `404` on the live site (confirmed with `curl` directly against `relationalsovereignty.com`, not just read from the repo), despite `sitemap.xml` listing it at priority `0.9` and `robots.txt` explicitly allowing it — the site's own crawler policy pointed at a dead link. Separately, on first paint of `Home.dc.html`, `Practise.dc.html`, and `Contribute.dc.html` — the three pages still on the `x-dc` runtime — the skip-link rendered as plain visible text at the top-left of the viewport for a real, measurable stretch of parse time before the stylesheet rule that hides it took effect, because that rule lived inside a `<style>` block the runtime renders roughly 230 lines after the skip-link itself, regardless of the two elements' actual order in the page's own source.~~
now: `scripts/prerender.mjs`'s `COPY_AS_IS` list includes `Invitation.dc.html`, so it's copied into every `_site/` build going forward — confirmed by rebuilding `_site/` locally and finding it present, where before this fix it was silently absent. Each of `Home.dc.html`, `Practise.dc.html`, and `Contribute.dc.html` gained one small `<style>` tag in their genuinely static `<head>` — `.skip-link{position:absolute;left:-9999px;top:0}.nav-toggle{display:none}` — placed right before the existing `support.js` `<script>` tag. Because this sits in `<head>`, the browser's parser applies it before `<body>` begins at all, which is a structural guarantee, not a timing race: the skip-link and the mobile-nav toggle are correct from the very first parsed byte of `<body>`, independent of whenever the runtime gets around to rendering the page's main `<style>` block.

**`Invitation.dc.html`'s file was never the problem — only its presence in the deploy artifact was.** `scripts/build-hugo.mjs`'s `HUGO_PAGES` list has included `Invitation.dc.html` since RS-036 added the page, so the committed root-level file has been correct and in sync with its Hugo source the entire time. `scripts/prerender.mjs` is a second, independent list of what actually gets copied into `_site/` — the folder GitHub Pages serves — and RS-036 updated the first list without updating the second. Two lists doing conceptually the same job (`HUGO_PAGES` vs the Hugo-generated portion of `COPY_AS_IS`) is exactly the kind of duplication that drifts silently; noted here rather than merged into one, since merging them is a real refactor of two scripts with different responsibilities (Hugo regeneration vs. deploy assembly), not a one-line bug fix.

**The skip-link fix doesn't touch why the runtime reorders `<style>` — it routes around the one consequence found so far.** Reading `Home.dc.html`'s own source confirmed the `<style>` block is written *before* the skip-link in the file (line 44 vs. line 69) — the `x-dc` runtime itself moves it later at render time, for reasons not investigated here (see `SUGGEST-10` in `tasks.md`). Rather than change how the runtime orders its output — a bigger, riskier change to code shared by three pages — this fix adds a small, deliberately redundant rule earlier, in a location (the static `<head>`) the runtime's reordering can't reach. The main `<style>` block's `.skip-link`/`.nav-toggle` rules are untouched and still own everything the early rule doesn't: `:focus`'s reveal position, the mobile media query's `display:block` override, hover states.

**A second instance of the same root cause was found and fixed alongside the one reported.** The author's report named only the skip-link. Investigating the mechanism (why a class-based rule would ever apply "late") turned up that `.nav-toggle` — the mobile hamburger `<details>` element, meant to be invisible by default at desktop widths — sits in the exact same late-rendered `<style>` block, and a `<details>` element is visible by default with no CSS at all. Fixed both in the same early rule rather than shipping a fix scoped only to the literally-reported symptom and leaving a matching, not-yet-noticed one live.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all eleven pages. `_site/Invitation.dc.html` confirmed present after a clean rebuild. Real headless-browser checks on `Home.dc.html`, `Practise.dc.html`, and `Contribute.dc.html` at both 1280px and 375px: `.skip-link`'s default computed `left` is `-9999px` (not the unstyled default) on every page/width combination; `.nav-toggle`'s computed `display` is `none` at 1280px and `block` at 375px, matching the intended responsive behaviour exactly; focusing the skip-link still moves it to `left:16px` (the `:focus` rule in the main style block still fires correctly, confirming the early rule doesn't shadow or conflict with it). Zero `axe-core` violations, zero console or page errors, on any of the eleven pages.

### BUG-04 — in-page and contents-nav links get a distinct look from citation links

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: every same-page or cross-page anchor link on the site — contents-nav items (`aria-label="Contents"` lists on BehindTheScenes, Learn, Archive, Practise) and inline in-prose cross-references (Archive's "fastest honest route in," BehindTheScenes' "The substrate below," and others) — rendered with only the site's global default `a{color:#2B4C9B;...}` rule, identical to an ordinary citation link pointing at someone else's work. Nothing distinguished "this jumps you somewhere on this project" from "this cites an external source."~~
now: two CSS rules, added to all nine page sources, cover every qualifying link with zero markup changes anywhere. `nav[aria-label="Contents"] a` gets the bordered-chip look Archive's own filter buttons already used (`border:1px solid #C9C6BA;border-radius:2px`, monospace label, `#585B4F`/hover-green `#2C5A38`) — Manifesto's copy of the same rule uses its own dark-theme palette instead (`#5C7A72` border, `#8FA9A2` text), ready for the day it gets a contents-nav of its own even though it doesn't have one yet. Every other link whose `href` contains `#` gets a small leading arrow via `::before{content:"→ "}`, matching the arrow already established by Home's own "→ another question" convention — inheriting `currentColor` rather than a hardcoded value, so it reads correctly whether the link sits in a page's light main content or (as on Archive's "fastest route" list) a dark section with its own inline text colour.

**The two rules are attribute selectors, not per-instance classes, because the site has zero external links containing a `#`.** Checked before committing to this approach, not assumed: `grep` across every page and `hugo/data/*.yaml` found no citation link anywhere on the site with a fragment in its `href`. That means `a[href*="#"]` alone, with no markup changes, correctly and completely separates "in-project navigation link" from "citation link" — the property being selected on *is* the distinction the task asked for, not a proxy for it.

**Two links needed an explicit exemption, and the exemption is itself a selector, not a list of two names.** Manifesto's and Practise's "Tell me when…" dispatch-interest links are already fully bespoke CTA buttons — bordered, padded, coloured — built long before this task. Adding an arrow on top would have cluttered an already-finished design. Rather than hardcode "skip these two specific links," the rule excludes anything already carrying `border-radius` in its inline `style` attribute (`:not([style*="border-radius"])`) — the actual tell that a link has already been designed as a button, wherever it appears, including on any future page that reuses the same CTA pattern.

**Fixed a specificity mistake before shipping, not after.** The first attempt gave contents-nav links both the chip style *and* an arrow (a `nav[aria-label="Contents"] a::before{content:none}` reset that should have suppressed it). It didn't — `a[href*="#"]:not(.skip-link):not([style*="border-radius"])::before`'s two `:not()` clauses each carry the specificity weight of a class, giving that selector *higher* specificity than the two-part nav-scoped reset despite reading as "more specific" to a human eye. Caught by screenshotting the actual rendered chips (`→What this is not`) before considering this done, not by reasoning about the CSS alone. Fixed with `!important` on the reset — the same narrow, targeted use already established in this codebase for beating a competing rule's specificity (`#primary-nav{display:none!important}` from UX-04), not a first resort.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all eleven pages. Real headless-browser checks: contents-nav links on BehindTheScenes, Archive, Learn, and Practise all confirmed `content:none` on `::before` (no arrow) via computed style, screenshotted at both 1280px and 390px; a plain in-prose link (BehindTheScenes' "The substrate below," verified as the *second* of two DOM matches for `href="#substrate"` — the first being the unrelated contents-nav item of the same name) confirmed `content:"→ "`; Archive's "fastest route" links confirmed the same; both CTA-button exceptions (Manifesto and Practise) confirmed `content:none`, screenshotted to show their existing design is untouched. Zero `axe-core` violations, zero console or page errors, on any of the eleven pages.

### BUG-03 + BUG-05 — pretty URLs site-wide, with redirects at every old path; a second missing-from-deploy file found along the way

**Shipped:** 2026-08-08 · **Commit/PR:** (pending)

~~was: every page's URL ended in `.dc.html` — `/Manifesto.dc.html`, `/Archive.dc.html`, and so on — including the six pages Hugo generates as plain static HTML with no runtime left to name. `hugo/hugo.toml` pinned this deliberately (`uglyURLs = true` plus an explicit flat `url:` per page), specifically so the Hugo migration wouldn't have to touch any existing link. Given a choice between leaving it, dropping just the `.dc` infix, or full pretty URLs, the author chose full pretty URLs. Separately, found while rebuilding and testing the new structure — not part of the original report — `print.js` (Manifesto/Invitation/Learn's print button) was already 404ing live, for the exact same reason `Invitation.dc.html` was (BUG-01): missing from `scripts/prerender.mjs`'s `COPY_AS_IS` list.~~
now: Home stays at `/`; the other eight pages moved to `/manifesto/`, `/invitation/`, `/learn/`, `/archive/`, `/resources/`, `/behind-the-scenes/`, `/practise/`, `/contribute/`. Each of the eight old flat paths is now a small, `noindex`'d redirect page (`<meta http-equiv="refresh" content="0; url=...">` plus a canonical pointing at the new URL) rather than gone outright — GitHub Pages can't issue a real HTTP redirect, so this is the closest available substitute, and it means an already-indexed or bookmarked `.dc.html` link still lands on real content instead of a dead end. Every internal reference site-wide was repointed: header/footer nav (all 9 sources, both the desktop and mobile-menu copy of each), every in-body cross-reference, `robots.txt`, `sitemap.xml`, and the canonical/OG/JSON-LD fields SEO-01 added the same day. `print.js` is now in `COPY_AS_IS` alongside `archive-filter.js`.

**The six Hugo pages' new paths come from one front-matter field, not a naming convention this build step has to guess at.** Each `hugo/content/*.md` file's `url:` front matter (`"/manifesto/"`, previously `"/Manifesto.dc.html"`) is Hugo's own mechanism for pinning a page's output path exactly, overriding whatever `uglyURLs` would otherwise compute — so `uglyURLs` is now `false` (Hugo's default; it was never actually doing anything on its own here, since every page already had an explicit override) and `hugo.toml`'s comment was rewritten to say so, rather than left describing a rationale that no longer applied. `scripts/build-hugo.mjs`'s `HUGO_PAGES` list changed shape from flat filenames to `{ name, path }` pairs (`path: "manifesto/index.html"`), used identically for both reading Hugo's output and writing the committed copy — the two locations are now the same relative path for the first time, where they used to differ (nested in `hugo/public/`, flat at the repo root).

**Hugo doesn't clean stale output between runs, and that would have shipped both URLs for every page.** The first rebuild after changing `url:` left `hugo/public/Manifesto.dc.html` (Hugo's old output, now orphaned) sitting right next to the new `hugo/public/manifesto/index.html` — Hugo's build stats even reported "Cleaned: 0." `build-hugo.mjs` now does `fs.rmSync(publicDir, {recursive:true,force:true})` before every `hugo` invocation. Caught by listing `hugo/public/` after a build and finding eight files where six were expected, not assumed safe.

**Practise and Contribute physically moved (`git mv`), which meant finding every place their old directory depth was assumed rather than stated.** Both files' only relative reference was `<script src="./support.js">` — safe as a flat root file, wrong one directory down (`/practise/support.js` doesn't exist). Fixed by switching to an absolute `/support.js` on all three `x-dc` pages (Home included, for consistency, though Home's own depth didn't change). That led to a deeper version of the same problem: `support.js` itself — the one script shared by Home, Practise, and Contribute — loads React from a *relative* `./vendor/react.production.min.js`, hardcoded as part of a 2026 hand-patch (the file is otherwise generated from TypeScript). A relative path inside a shared script resolves against whichever page loaded it, not against the script's own location — correct for Home at the root, wrong for Practise and Contribute one level down. Fixed at the source: both React URLs are now root-absolute, with the patch comment updated to explain why, so the next person who has to reapply this hand-patch after a regeneration doesn't reintroduce a relative path without knowing it'll break two of three pages that load it.

**A full sweep for relative references — not just the ones already known about — found four more, on pages that don't obviously look related to Practise or Contribute.** `archive-filter.js` (Archive) and `print.js` (Manifesto, Invitation, Learn) were each referenced by a bare relative filename; so were two separate `href="robots.txt"` links (BehindTheScenes, Learn, Resources, Home, Practise, Contribute — six instances total) and one `href="glyph-check.html"` (BehindTheScenes). None of these were broken *before* this migration (everything lived at the repo root, so a relative reference and a root-absolute one behaved identically) — the migration is exactly what turns a merely-untidy relative path into a real 404, for any page that moves even one directory deep. Found by grepping every `src=`/`href=`/`action=` attribute across all nine page sources for anything that wasn't already absolute, external, a fragment, `mailto:`, or a template placeholder — a systematic sweep, not a page-by-page guess — and re-run after each fix until it returned nothing. This is also how `print.js`'s missing-from-`COPY_AS_IS` problem (BUG-05) was found: chasing down where its 404 during a headless-browser check of the new `/manifesto/` page actually came from led straight to the same class of bug BUG-01 had already named.

**The redirect stubs are deliberately thin, and deliberately not identical to a 404.** Each is `noindex`, so a crawler that does fetch one before following the refresh won't index it as separate content; each carries the real canonical link to the new URL, for the crawlers that read canonicals without executing the refresh at all; and each has visible fallback text and a real link, for the small number of clients that don't act on `<meta http-equiv="refresh">`. `robots.txt` deliberately does **not** disallow the old paths — a disallowed URL is invisible to a crawler trying to learn that it moved, which is the opposite of what a redirect-in-progress needs.

**What this migration could not verify locally, and said so rather than assumed.** GitHub Pages' actual behavior for a bare `/manifesto` (no trailing slash) — whether it 301s to `/manifesto/` the way most static hosts do — depends on GitHub's own serving layer, not anything in this repository, and there is no local equivalent to test it against with full fidelity. Every internal link was written in the canonical trailing-slash form specifically so this question doesn't need to be answered for the site to work correctly; it's only a concern for an external link built by hand without the slash. Noted here rather than claimed as verified.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean — 19 pages now checked by `check-origins.mjs` (up from 11, reflecting the six new nested paths) with zero cross-origin references, and `check-pages.mjs` gained a dedicated `checkRedirectStubs()` pass (not the normal browser-navigation check, since a real browser immediately follows a stub's own refresh — confirmed by fetching each stub's raw HTML instead) confirming all eight stubs 200, contain the correct `meta refresh` target, the correct canonical, and `noindex`. Real headless-browser checks beyond the automated suite: a stub loaded in an actual browser context lands on the correct final URL with the correct page title; `/practise/`'s React app boots with zero page errors and renders its real heading from its new nested path; a CTA link written as `href="/#dispatch"` (previously `Home.dc.html#dispatch`) correctly scrolls to the dispatch section — checked specifically against `_site/`'s prerendered output, since the *raw* `Home.dc.html` source doesn't contain `#dispatch` in its initial HTML at all (it's client-rendered), which produced a false alarm on the first pass until the test target was corrected to the actual deployed artifact; the mobile nav toggle still opens and its links still resolve at a nested URL (`/archive/` tested at 375px).

### SUGGEST-08 + SUGGEST-09 — two mobile-width defects, and the gap in the test suite that let both of them through

**Shipped:** 2026-08-09 · **Commit/PR:** (pending)

~~was: Archive's "Held in common" Venn diagram is a horizontally scrollable box (`overflow-x:auto` around `min-width:700px` content) with no way to reach it from the keyboard — `axe-core`'s `scrollable-region-focusable` (serious). Below about 700px that diagram is the only place four of the entries appear, so a keyboard-only reader could see the scroll cue UX-21 added and had no way to act on it. Separately, Resources' entry-count badge held the count and the scope note under a single `white-space:nowrap`, running the longest one 26px past a 375px viewport (81px at 320px) where it was clipped rather than wrapped — the reader lost the end of the sentence with nothing to indicate it had been cut.~~
now: the Venn container takes `tabindex="0"`, plus `role="group"` and `aria-labelledby` pointing at the section's own "Held in common" heading, so the box has a name when focus lands on it. The badge splits into a nowrap count and a wrapping scope note, with `min-width:0` on the flex item — a flex item defaults to `min-width:auto` and refuses to shrink below its content's intrinsic width, so removing the `nowrap` alone would not have made it wrap.

**The more useful fix is the third one: `check-pages.mjs` now runs a second `axe` pass at 375px on every page.** Both defects were pre-existing, both were serious enough to matter, and both survived a full accessibility suite that reports "all 11 pages passed" — because that suite ran at Playwright's default 1280px and nothing else. A scrollable region that isn't keyboard-accessible cannot be flagged at a width where nothing scrolls; the violation does not exist until the layout is narrow enough to create it. SUGGEST-08 had to be found by hand, afterwards, which is precisely the failure mode a check suite exists to prevent. The narrow pass reports only violation ids that didn't already fire at desktop width, so a single problem isn't counted twice.

**Verified by breaking it on purpose.** After adding the narrow pass, the Archive fix was temporarily reverted and `npm run check` re-run: it failed with `axe @375px [serious] scrollable-region-focusable`, then passed again once restored. A guard that has never been observed failing is not yet known to be a guard.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all eleven pages at both widths, plus all eight redirect stubs. Direct browser measurement before and after on both fixes — the Venn container confirmed focusable (`document.activeElement`), correctly labelled, and actually scrollable once focused; the Resources badges confirmed within the viewport at 320px, 360px, and 375px with every category expanded, where they previously overflowed at all three. A full axe + overflow sweep of all nine content pages at 375px, 390px, and 768px reported no remaining serious or critical violations and no page-level horizontal scroll.

### WD-26 + WD-01 — collapse the Home duplication, then fix the typo it was hiding twice

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: the homepage's real content lived in two byte-identical files, `index.html` and `Home.dc.html`, maintained by hand in parallel — `check-pages.mjs`'s `checkIndexMatchesHome()` diffed them and failed the suite if they drifted, and the local dev server mapped a bare `/` request to `Home.dc.html` rather than `index.html` to test against what it treated as the source of truth. The homepage hero read "FREDOM THROUGH RELATIONSHIP, NOT FROM IT. NO OWNERS. NO OBJECTS." in both files — the spelling as supplied when the hero was rewritten earlier this session, now confirmed as a typo by the author-supplied web design spec (WD-01).~~
now: `Home.dc.html` is a soft-redirect stub to `/`, reusing the exact pattern BUG-03 already built for the other eight flat `.dc.html` paths — `<meta http-equiv="refresh" content="0; url=/">`, a canonical link, `noindex`, and a fallback link, with `script-src 'none'` since a redirect stub needs no JavaScript. `index.html` is now the only file holding the homepage's actual content. The hero reads "FREEDOM THROUGH RELATIONSHIP, NOT FROM IT." with "NO OWNERS." and "NO OBJECTS." each broken onto their own line via explicit `<br>`, per the author's follow-up request — screenshotted at 1280px and 390px to confirm.

**Promoted from a Phase 9 follow-up to a Phase 9 prerequisite, and worked first for a concrete reason.** The spec's own §1b names the duplication as a live bug source and suggests collapsing it as a later cleanup; planning found that backwards — six other Phase 9 items touch Home (WD-02, WD-03, WD-05, WD-07, WD-17, and now WD-01 itself), each a double edit until the duplication is gone. Landing WD-26 first halves the remaining phase's Home-touching edits from twelve to six, and this task is itself the proof: WD-01 became a one-file, one-line change instead of two.

**Three files changed to make the collapse real, not just cosmetic.** `scripts/prerender.mjs`'s `PAGES` array (the x-dc runtime pages that get rendered in a real browser) dropped `Home.dc.html`; `COPY_AS_IS` (the already-static files copied verbatim) gained it, alongside the other eight stubs. `scripts/check-pages.mjs` moved `Home.dc.html` from the browser-tested `pages` list to the `redirectStubs` list checked by raw fetch — the same reasoning already documented there for the other eight: navigating to a stub in a real browser context immediately follows its own refresh, so a `page.goto()` would silently test the *target* a second time rather than the stub itself. `checkIndexMatchesHome()` was deleted outright rather than left dormant, along with its call in `main()` and the local dev server's `/` → `/Home.dc.html` mapping, now `/` → `/index.html` — matching what GitHub Pages actually does at the bare domain, which the byte-identical-copy workaround existed to approximate before this.

**`check-origins.mjs` needed no changes and correctly picked up the new stub anyway.** Its `PAGES` list is built by globbing every `*.dc.html` file at the repo root rather than naming files individually, so `Home.dc.html` is scanned automatically now that it's a plain static file — one fewer place this collapse had to be taught about by hand.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean — 10 pages (down from 11; Home.dc.html is no longer counted as a separate browser-tested page) and **9** redirect stubs (up from 8), with `check-pages.mjs` reporting `✓ Home.dc.html redirects to /` alongside the original eight. Real headless-browser check beyond the automated suite: navigating directly to `/Home.dc.html` lands on `/` with a `200`, confirming the meta-refresh actually fires end to end, not just that the stub's markup is well-formed. `grep -c FREDOM index.html Home.dc.html` returns 0 for both.

### WD-02 + WD-03 + WD-04 — motion token, balanced headlines, matrix row hover

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: every hover state on the site — nav links, buttons, disclosure summaries, Archive's filter chips, contents-nav links, Learn's card titles — snapped instantly between colours with no transition. Headlines wrapped however the browser's default line-breaking landed, occasionally ragging badly (Learn's "Four things named plainly, using **the vocabulary already here.**"). Tracing one situation across the holds/fails matrix's thirteen columns required a finger on the screen, since nothing marked which row was under a reader's eye.~~
now: one motion token — `transition:color .14s ease,background-color .14s ease,border-color .14s ease` on `a,button,summary,[data-filter],nav[aria-label="Contents"] a,.card-title` — added immediately after each file's `a:hover` rule in all 9 files carrying page CSS (the ten-file count in the web design spec that proposed this was already stale by the time it was worked; WD-26, landed immediately before this, had just collapsed `Home.dc.html` out of that set). Manifesto gets the shorter selector list the spec specifies for it — no `[data-filter]` or `.card-title` exist on that page. `h1,h2,h3{text-wrap:balance}` added to the same 9 files, next to the existing `*{box-sizing:border-box}` line. Learn's holds/fails matrix gained `table.matrix tbody tr{transition:background-color .14s ease}` and `:hover{background:rgba(15,42,46,.05)}`, placed before the `td.c-holds`/`td.c-fails` fill rules — a `<td>`'s own background always paints over its parent `<tr>`'s, so the fills stay exactly as legible on the tint as off it.

**All three are paint-only or layout-inert, and all three are already covered by the site's existing `prefers-reduced-motion` rule** — no new media query was needed; `*{animation:none!important;transition:none!important}` already zeroes every transition added here. `outline-color` was deliberately left out of the transition's property list, matching the spec's own reasoning: keyboard focus must land instantly, not ease in.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages. Real headless-browser checks beyond the automated suite — a first attempt to hover-test the matrix row read `background-color: transparent` before *and* after a synthetic hover, which turned out to be `page.mouse.move()` targeting a stale `getBoundingClientRect()` captured before a `scrollIntoView` had settled (the row was below the viewport, so `elementFromPoint` at that coordinate returned `null`); switching to `locator.hover()`, which scrolls the target into view itself before dispatching events, confirmed the row actually matches `:hover` and its background changes as specified. A first full-page screenshot also appeared to show the section heading's two lines overlapping — isolated to a sticky-bar shadow issue, not a `text-wrap:balance` bug: the heading had been scrolled to sit partly behind the sticky jump bar's `z-index:30` and semi-opaque box-shadow, an artifact of which element the test happened to scroll to, not of the CSS shipped. A clean screenshot with the heading properly clear of the bar confirms two evenly balanced lines with no overlap. Separately confirmed: nav-link `transition-duration` reads `0.14s` under normal conditions and `0s` under `reducedMotion: "reduce"`, and `getComputedStyle(h1).textWrap` reads `"balance"`.

### WD-07 — focus visibility on dark grounds, plus a second instance found while doing it

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: the site-wide focus ring is `#2B4C9B` (blue), which measures 1.88:1 against the `#0F2A2E` teal ground used for dark bands inside otherwise-light pages — Home's roadmap band and footer, Learn's opacity section and footer, Archive's "fastest route" band and footer, Behind the Scenes' crawler and changelog sections and footer, Invitation's "On goodbyes" band, Resources' footer, and Practise's and Contribute's own dark sections and footers. A keyboard user tabbing through any of these could not see where focus was. Manifesto, being dark-ground throughout, already used an ochre ring and was unaffected.~~
now: every genuine dark-ground container — a section or footer with content sitting inside it, as opposed to a small element that merely happens to set the same background colour — carries `class="dark"`, and each of the 8 affected files (Manifesto excluded, already correct) gained `.dark :focus-visible{outline-color:#DB9E2A}` right after its own base `:focus-visible` rule. Ochre at `#DB9E2A` measures 6.42:1 on `#0F2A2E`.

**Scoped the hook to real bands, not every literal occurrence of the colour, and verified the distinction rather than assuming it.** A grep for `background:#0F2A2E` returns the skip-link and roughly a dozen small submit/tool buttons across Home, Contribute, and Practise in addition to the genuine dark sections and footers — 34 total matches across 8 files, of which 15 are real bands. The buttons and the skip-link were deliberately left untouched: each carries the site's global `outline-offset:3px`, which places the ring *outside* the element's own border, landing on the surrounding light page rather than the button's own dark interior. Checked directly rather than assumed — focusing a live "Continue" button on Practise and sampling the pixel 5px outside its edge (inside the ring's landing zone) returned the page's light rust-tinted background, not the button's `rgb(15, 42, 46)`. Tagging a leaf button `class="dark"` would in any case have been inert: `.dark :focus-visible` is a descendant combinator, and a button has no focusable descendants to match — only real container bands, where other links or buttons sit *inside* a dark ancestor, are the actual bug. Behind the Scenes' `#crawler` section also contains a nested `<div style="background:#0F2A2E">` per opacity-state card; it needed no class of its own, since it's already a descendant of `#crawler`'s `.dark`.

**Found a second, previously-undocumented instance of the same bug while doing this, on a page the spec didn't flag.** Invitation doesn't use the site's `#2B4C9B` ring at all — it carries its own page-level identity, `#7D5915` (see `docs/design-palette.md` and this session's FLAG-07 on why that page is an exception to the site's ochre standardisation). That colour was never checked against Invitation's one dark band: measured at 2.38:1 against `#0F2A2E`, worse than the 1.88:1 the spec was written to fix. `#6B4C12` — the general text-safe ochre FLAG-07 already has under consideration for this page's light-ground base colour — would have been a still-worse choice there, being darker still. Fixed with the identical `.dark :focus-visible{outline-color:#DB9E2A}` pattern, scoped so Invitation's light-ground ring is untouched pending FLAG-07's separate, still-open decision about that base colour.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages. Real headless-browser checks beyond the automated suite — for each of the 8 files' dark bands, the first focusable link or button inside was located, `.focus()`ed, and its computed `outlineColor` read back: six of eight bands contain a link (Archive's fastest-route, both Behind the Scenes crawler and changelog... — correction, changelog itself is prose with no interactive content, confirmed by a direct DOM range check, not assumed — Invitation's one band, Learn's opacity section, Home's roadmap band, Practise's dark section) and all six confirmed `rgb(219, 158, 42)` (`#DB9E2A`). The two link-free bands (Behind the Scenes' changelog, Contribute's "What the commons refuses") were confirmed to genuinely contain zero `<a>`/`<button>` elements via a precise section-boundary scan — an earlier looser check appeared to find a link in Contribute's section but that was a boundary error catching the footer below it, not a real find. Screenshots of two focused links (Home's "change log," Invitation's "Practise") confirm the ring is visibly ochre against the teal ground in both cases. Manifesto confirmed untouched — zero occurrences of `class="dark"` or a `WD-07` comment in that file.

### WD-09 + WD-06 — per-group entry counts, and a filter that finally announces itself

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: Archive's nine shelf headings carried no count, on a page 60 entries and roughly 12,000px tall — a reader had no sense of a group's size before scrolling into it. Worse, pressing a filter button narrowed the visible list (60 entries down to 36 for "books," confirmed) with no `aria-live` region anywhere on the page: a screen-reader user got total silence and no way to know the list had changed, while a sighted user had no visible count either — nothing said whether narrowing was even worth it.~~
now: each group heading carries a small monospace count (`<h2>{{ .title }} <span data-count>{{ len .items }}</span></h2>`), and a `role="status" aria-live="polite"` paragraph above the filter — server-rendered with the honest, always-true no-JS value ("Showing all 60 entries," summed from `hugo.Data.archive.groups` at build time) — updates to "Showing N of 60 entries" as `archive-filter.js`'s `apply()` counts what it leaves visible. The same function now also rewrites each group's `[data-count]` to the filtered count, so the per-group numbers stay truthful while narrowed rather than always showing the unfiltered total, and restore correctly when "everything" is pressed again.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Real headless-browser checks beyond the automated suite, matching the spec's own worked example exactly — initial state reads "Showing all 60 entries" with `role="status"`/`aria-live="polite"` present; clicking "books" reads "Showing 36 of 60 entries," with 36 entries actually visible in the DOM and the nine per-group counts (`[3,7,3,4,6,1,3,4,5]`) summing to 36; clicking "everything" restores "Showing all 60 entries" and the group counts back to `[8,11,4,5,11,1,9,6,5]`, summing to 60 — the same nine-group total verified while planning this phase. With JavaScript disabled entirely, the status still reads "Showing all 60 entries" and all 60 entries render with none `[hidden]`, confirming the server-rendered fallback is correct on its own, not merely inert.

### WD-08a — Archive's entry links nudge their arrow on hover and focus

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: Archive's "read it free, [journal] →" links (and their outbound-link equivalents) sat static on hover and focus — the arrow was plain trailing text inside the `<a>`, no different from a citation link elsewhere on the site.~~
now: the arrow is wrapped `<span class="arr" aria-hidden="true">→</span>`, with `.arr{transition:transform .15s ease}` and `a:hover .arr,a:focus-visible .arr{transform:translateX(3px)}`. `aria-hidden` keeps the glyph out of the accessible name — the link still announces as "read it free, Borderlands 19(2)," not "…→" — and `transform` is a composited property, so nothing else in the row reflows or repaints. Only Archive's entry links use this hand-authored `{{ .linkLabel }} →` pattern; a check of Home's outro and Learn's "→ roadmap" phrasing (both named in the design spec's prose alongside Archive) found neither actually writes a literal arrow character into link text — both rely on the site's separate generic `a[href*="#"]::before` rule instead, which is WD-08b, deliberately deferred because changing that pseudo-element from `inline` to `inline-block` risks altering line-wrap behaviour in tight columns like Learn's jump menu. Scope stayed at Archive alone rather than the three-page reading the spec's prose could support.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Real headless-browser check beyond the automated suite: measured the arrow's x-position before and after both `.hover()` and `.focus()` — both moved it exactly 3px, under `reducedMotion: "reduce"` so the measurement reflects the settled end state rather than mid-transition. Confirmed `aria-hidden="true"` present on the span. Screenshot confirms the link's own focus ring and the nudged arrow render correctly together.

### WD-27 — the page-weight claim drifted stale for a third time, so this one is now enforced, not just corrected

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: `hugo/data/substrate.yaml`'s "Page weight" entry claimed "about 15 KB (the Invitation) to about 118 KB (the Archive)... a shared ~70 KB script." Measured against the actual shipped `_site/`: Invitation was 17.1 KB and Archive was 133.7 KB — the Archive figure alone nearly 16 KB past what the colophon published. This is the same claim's third drift. SUGGEST-01 first replaced a flat "under 60 KB" outgrown by the site's own content; this session's own Phase 9 planning caught a second drift (118 KB claimed vs. 128 KB actual) and corrected the numbers — and by the time WD-02 through WD-09 finished landing, the corrected numbers had *already* drifted again, because nothing was checking them.~~
now: the numbers are corrected — "about 17 KB (the Invitation) to about 134 KB (the Archive)... a shared ~69 KB script" — and, more importantly, `scripts/check-pages.mjs` gained `checkPageWeight()`, wired into `main()` alongside the redirect-stub and Hugo-sync checks. It regex-extracts the claim from the *rendered* Behind the Scenes page (not the YAML source — this project has no YAML-parsing dependency, matching D2's stated preference for zero dependency tree, and the rendered output is also the closer match to what a reader actually receives), measures every shipped file's real size (the three x-dc pages from `_site/`'s prerendered output, the six Hugo pages from their committed root-level file, `support.js` for the shared script), and fails the build if either named page drifts more than 1.5 KB from its claim or — a check the number alone can't make — if the page actually named as lightest or heaviest is no longer the page holding that title.

**The claim's own copy now names its own history rather than repeating a promise that has already been broken twice.** Where the entry previously said a stale number was "corrected... rather than left to quietly stop being true" (a promise this same entry had already failed to keep once by the time WD-27 was worked), it now says the figure "has drifted stale twice before" and names the check that now enforces it — accurate rather than aspirational.

**Verified by breaking it on purpose, the same way SUGGEST-08/09 (Phase 8) verified their own guard.** Temporarily reverted the colophon to its previous stale numbers (15 KB / 118 KB) and re-ran the check: it failed with `Invitation is 17.1 KB, more than 1.5 KB off the claimed 15 KB` and the equivalent for Archive, then passed again once restored to `✓ Page weight (Invitation 17.1 KB – Archive 133.7 KB, script 68.9 KB, claim within 1.5 KB)`.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages, with `checkPageWeight()`'s output now a visible line in every run rather than a fact nobody is checking. Confirmed the regex extraction directly against the shipped file: `['17', 'Invitation', '134', 'Archive', '69']`, exactly matching the published sentence.

### WD-10a — dead grid slabs, fixed on the two grids that actually have them

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: two hairline grids drew their dividers by giving the grid container the rule colour and each cell the page colour, with `gap:1px` — the container colour showing through every gap, including gaps with no cell behind them. Learn's field guide (10 items in a 3-column layout) left 2 empty cells in its last row; Behind the Scenes' crawler-policy grid (5 items, 4 columns at desktop widths) left 3. Both rendered as a solid rectangular block of divider colour where no content existed — unambiguously a rendering bug, not a design choice.~~
now: both grids draw their hairlines on the cells instead of the container — `border-top`/`border-left` on the grid, `border-right`/`border-bottom` on each cell, `gap:0` replacing `gap:1px` plus the shared background. A cell that doesn't exist has no border and no background of its own, so an incomplete row shows plain page ground.

**Confirmed before touching anything which grids actually have the bug, and left the others alone.** Learn's `.opq` (4 items) and `.senses` (4 items) grids both resolve to exactly 4 columns and are exactly full — re-verified against the current data files (`opacity.yaml`: 4 entries; the `.senses` block: 4 `<div>`s) before this change, not assumed from an earlier reading. Neither was touched.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Real headless-browser checks beyond the automated suite, at all four breakpoints the site's own QA checklist names (1440, 1024, 768, 390 — 1024 is where a grid's column count actually changes, which the spec's own note calls the case that matters most): measured each grid's resolved column count, row count, and the background colour sampled at a point inside any empty final-row cells. At every width on both grids, the sampled colour came back fully transparent (`rgba(0, 0, 0, 0)`) rather than the divider colour — confirmed at 4 columns (1440px, 3 empty cells), 3 columns (1024px, 1 empty cell), 2 columns (768px), and 1 column (390px, no empty cells possible). One correction made mid-verification: an early check script's selector matched the wrong grid on Behind the Scenes (that page has three grids sharing the same `minmax(250px` column spec, only one of them a hairline grid) and undercounted at 4 cells instead of 5 — caught by cross-checking against the known item count from `crawler.yaml` rather than trusting the first result, then fixed by scoping the selector to `#crawler` specifically. Screenshots at 1440px confirm both grids render with visible cell dividers and plain page background where the final row runs short, no slab in either case.

### WD-13 — Archive's filter bar goes sticky, carrying the WD-06 count

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: Archive's filter controls (the seven filter buttons, and WD-06's "Showing N of 60" status line) lived in normal document flow near the top of a page roughly 12,000px tall. A reader partway through the shelf who wanted to narrow or reset the filter had to scroll back to the top first.~~
now: the filter controls sit in a `.filterbar` div styled `position:sticky;top:0`, pinned to the viewport for the entire scroll — reusing Learn's proven `.jump` bar pattern (sticky, `z-index:30`, the same drop-shadow) rather than inventing a second one. The two explanatory paragraphs below the controls ("★ a first route marks…", "Every entry below is shown by default…") stay in normal flow, deliberately not pinned — they're one-time context a reader needs once, not chrome needed at every scroll depth.

**Two real bugs surfaced during verification, neither visible from reading the CSS, both confirmed and fixed with an actual browser rather than assumed correct from the rule alone.**

First: the bar did not actually stay pinned. Measuring its position at 50% scroll depth returned `top: -4969`, not `0` — it had scrolled away with the page. `position:sticky` only holds an element in place within its own containing block's height; nesting `.filterbar` inside the same short `<section>` that also held the two explanatory paragraphs meant that section's bottom edge — a few hundred pixels into a 12,000px page — was also the end of the bar's sticky range. Fixed by moving `.filterbar` out to be a direct sibling of the groups `<section>`, both children of `<main>`, matching exactly how Learn's `.jump` bar is placed (a direct child of `<main>`, sibling to every section below it) — confirmed by reading Learn's actual DOM structure rather than assuming the pattern transferred by copying the CSS alone.

Second, found only by testing mobile specifically: the filter buttons kept wrapping onto multiple lines at narrow widths despite a `@media (max-width:700px){.filterbar .chips{flex-wrap:nowrap}}` rule meant to prevent it, ballooning the bar to 193px tall — enough to eat a quarter of a phone screen. The `.chips` div carries an inline `style="…flex-wrap:wrap…"`, and an inline style attribute always outranks an external stylesheet rule at equal or lower specificity; the media-query override was being silently ignored. Fixed with `!important` on the mobile override — the same narrow, targeted use of `!important` this codebase already established for exactly this situation (see BUG-04 in this file). Chips now scroll horizontally on mobile instead of wrapping, and the bar drops to 108px.

That second fix exposed a third, smaller issue: `[data-group]`/`article[data-tags]`'s `scroll-margin-top`, sized for the shorter desktop bar, wasn't enough to clear the taller mobile one — an in-page anchor landed 12px behind the bar. Given a mobile-only override, same `!important` reasoning, `scroll-margin-top:7.5rem` at narrow widths.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean, including WD-27's page-weight check confirming Archive's slight growth from this change (134.3 KB) stays within its 1.5 KB tolerance of the published 134 KB claim. Real headless-browser checks beyond the automated suite — the bar's `top` measured `0` (truly pinned) at 10%, 50%, and 99% scroll depth on both desktop and mobile, not just "looks sticky in a screenshot"; the WD-06 status line confirmed live-updating to "Showing 36 of 60 entries" while scrolled deep into the shelf and filtered from that position; an in-page contents-nav anchor link lands with its target's top edge clearing the bar's bottom edge with margin to spare, on both breakpoints; and the page-level horizontal scroll check (`window.scrollTo(9999,0)` then reading `scrollX`) confirmed `0` on mobile despite the newly scrollable chip row.

### WD-14 — Behind the Scenes gets Learn's sticky section nav; `learn.js` becomes `sections.js`

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: at roughly 39 minutes and 18,600px, Behind the Scenes is the longest page on the site and the only long one with no persistent orientation. Its Contents nav (nine chip links to each section) exists once, in the hero, and scrolls away with everything else — a reader partway through the page had no way to jump elsewhere without scrolling back to the top.~~
now: a sticky `.jump` bar, ported from Learn, sits as a direct child of `<main>` right after the hero section — pinned to the viewport, naming the current section, and expandable into the same nine-link list the hero's Contents nav already offers. `learn.js` is renamed `sections.js`, since it now serves two pages rather than one; every reference (`<script src>` on both pages, `scripts/prerender.mjs`'s `COPY_AS_IS` list, an internal comment on Learn) was updated in the same commit, and the file's own header comment now describes both consumers rather than just Learn.

**Confirmed rather than assumed that `learn.js`'s selectors really are generic enough to reuse unchanged.** They are — `nav[aria-label="Sections"] a[href^="#"]` and `#jump-current` — but only because the new Behind the Scenes markup was written to use the exact same names, not because the selectors were inherently page-agnostic. `BAR = 76` (the sticky-bar clearance constant) needed no change either: both pages' bars render at an identical 50px, and 76 already carries deliberate slack over that literal height, confirmed by direct measurement rather than assumed from the shared CSS alone.

**No `[data-open-all]` button was ported, on purpose.** Learn's button toggles every `details[data-collapsible]` open or closed at once; Behind the Scenes has no `data-collapsible` sections at all — its one `<details>` (the "Show the N substrate fields" toggle) is a single, unrelated disclosure, not part of this navigation model. Porting the button anyway would have shipped a control that toggles nothing. `sections.js`'s existing `if (toggle && collapsible.length)` guard means this isn't a special case in the script — the button and print-restore logic simply find nothing to act on and no-op, the same as they would on any page without collapsible sections.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean, with zero console or page errors on Behind the Scenes specifically (would have caught a broken script reference from the rename). Real headless-browser checks beyond the automated suite: the bar measured `top: 0` (genuinely pinned, not just present) at 5%, 30%, 60%, 90%, and 99% scroll depth on an 18,600px page; the current-section label updated correctly scrolling past `#crawler` and `#labour` (the last section); a direct deep link to `/behind-the-scenes/#roadmap` landed with the heading clearing the bar; the bar is `display:none` under print media, matching Learn's own behaviour; the pre-existing, unrelated substrate `<details>` confirmed still closed by default and untouched; and with JavaScript disabled entirely, the nine section links render and work as a plain list, the collapsed `<details>` defaults to closed (native browser behaviour, no script required), and the page remains complete — the same progressive-enhancement bar every other script on this site is held to.

### WD-11 (first half) — the token block, promoted everywhere it belongs, touching nothing else

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: Learn defined the full four-register colour system as CSS custom properties on `:root`; the other eight files carrying page CSS repeated the same values as raw hex literals wherever they needed them. This is why WD-05 ("one ochre") was a multi-file hunt across `hugo/data/substrate.yaml`, `practise/index.html`, and seven `#7D5915` occurrences on Invitation alone, rather than a one-line edit — and it is why the next palette change will be the same hunt again.~~
now: the identical `:root` token block — every value unchanged from Learn's own definition — is promoted to the 7 other files that carry the shared light-ground base CSS (archive, behindthescenes, invitation, resources, index.html, practise, contribute). **Manifesto does not get it.** Its register is rhetorical rather than analytical, and `docs/design-palette.md` already excludes it from the four-register system on that basis; adding tokens to a page that will never consume them would be dead weight this codebase's own conventions argue against.

**This is deliberately only the first half of WD-11, and says so where it's tracked.** The spec's own instruction is explicit: "do this as its own commit, changing nothing else... then migrate literals to tokens in follow-up commits, page by page, so any visual regression is bisectable." What shipped here is exactly the first half — the tokens exist and are readable via `getComputedStyle`, and nothing yet reads them. Migrating any file's literal hex values to `var(--token)` references is separate, future work.

**Proved the change is inert rather than merely arguing it should be.** A CSS custom property has zero rendering effect until something references it with `var(--name)`. Grepped all 7 newly-touched files for that pattern and confirmed zero occurrences in every one — not a screenshot-diff within some visual tolerance, but a deterministic guarantee that nothing on any of these pages could possibly have changed. Confirmed the tokens are nonetheless real and correctly scoped: `getComputedStyle(document.documentElement).getPropertyValue('--holds')` returns `#2C5A38` on all 7 promoted pages and returns empty on Manifesto, while each page's actual rendered `body` background colour is unchanged — `#E7E5DC` on the 7 (set by a literal rule, as before) and `#0F2A2E` on Manifesto.

**Shipped ahead of WD-25 (the shared-CSS-into-a-partial extraction), and that was a deliberate call, not an oversight.** The plan that scoped this phase recommended sequencing the extraction before this promotion, reasoning that extracting first would make WD-11 a one-file edit instead of a seven-file paste. That reasoning no longer changes the outcome: Tier A already pasted seven other site-wide rules (the motion token, `text-wrap:balance`, the dark-ground focus override, and more) across all 9 files ahead of any extraction decision, so this token block is one more line in a pile that already exists — not a new category of cost the sequencing note was trying to avoid. The genuinely separate, still-open part — migrating literal values to reference these tokens — remains worth sequencing after WD-25 if that extraction happens, so each migration commit bisects against one shared source rather than nine independent copies.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages, with zero new axe, HTML-validation, or console-error findings — expected, since unused custom properties have no accessibility or rendering surface to break.

### WD-20 + WD-24, and a site-wide reduced-motion gap found while verifying them

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: Learn's sticky jump bar named which section a reader was in but not how far through the page they were. The thirteen principle cards' question bands sat at whatever height their own text needed, so a one-line question ("What would happen if someone said no?") rendered as a thin sliver directly beside a three-line one (122px, principle 03) in the same grid row — the bottom edges already lined up exactly (`.p-b`'s `flex:1 1 auto` already guaranteed that), but the top edges didn't, and nothing evened them.~~
now: **WD-20** — a 2px ochre hairline along the jump bar's bottom edge, driven entirely by `animation-timeline: scroll()`: no scroll listener, no `requestAnimationFrame` loop, nothing running on the main thread while the reader scrolls, because the browser itself ties the animation's progress to scroll position. Wrapped in `@supports (animation-timeline: scroll())` so it's fully absent — not a broken or partial version of it — anywhere the feature doesn't exist. **WD-24** — `.p-q{min-height:4.1rem;display:flex;align-items:center}`, which floors every question band at two lines' worth of height without touching the tallest one, so a one-line question no longer reads as an afterthought beside its neighbours.

**Verifying WD-20 surfaced a real, previously undiscovered, site-wide accessibility gap — not local to the new feature.** The site's global rule for `prefers-reduced-motion: reduce` is `*{animation:none!important;transition:none!important}`, present identically in all 9 files carrying page CSS. Measured directly: under reduced motion, `document.body`'s `animation-name` correctly reset to `none` — but `.jump::after`'s did not, still reporting `jump-progress` and still visibly tracking scroll. The reason is a CSS fundamental, not a typo: the universal selector `*` matches real elements only: it does not implicitly reach `::before`/`::after` generated content, which needs to be addressed explicitly. That meant every `summary::before{transition:transform .15s ease}` rule already on the site — the disclosure-triangle rotation used by every `<details>` on every page — had been silently exempt from reduced motion since it was written, undetected because nothing had directly measured a pseudo-element's computed style against this specific media query before. Fixed at the source: all 9 files' rule now reads `*,*::before,*::after{animation:none!important;transition:none!important}`.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages, including WD-27's own page-weight check. Real headless-browser checks beyond the automated suite — for WD-20: `CSS.supports("animation-timeline: scroll()")` confirmed `true` in the test environment, and the bar's `transform` was sampled at five scroll depths (0%, 25%, 50%, 75%, 99%), reading `scaleX` values of 0, 0.269, 0.538, 0.808, and 1 — an almost perfectly linear track of scroll position with no JS involved. For the reduced-motion fix: before the fix, `.jump::after`'s `animationName` under `reducedMotion: "reduce"` read `"jump-progress"` (still animating) despite `document.body`'s own animation correctly reading `"none"`, proving the gap was real and specific to pseudo-elements, not a broader failure of the media query itself (`window.matchMedia("(prefers-reduced-motion: reduce)").matches` was confirmed `true` throughout); after the fix, both `.jump::after`'s `animationName` and `summary::before`'s `transitionDuration` correctly read `"none"`/`"0s"`, while normal-motion behaviour for both was re-confirmed unchanged (the scroll-tracking values above, and `summary::before`'s transition still measuring `0.15s`). For WD-24: measured all 14 `.p-q` bands (13 principles plus the visually-matching continuity-of-care band) at 1440/1024/768/390px — every short band now floors at exactly 66px, the 122px band is unaffected at every width, and no band's text is clipped (`scrollHeight` never exceeds `clientHeight` on any of them). A before/after screenshot of the full principles grid confirms the intended effect: short single-line questions no longer read as slivers beside their taller neighbours.

### WD-21 + WD-23, and WD-22 turning out to already exist

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: Learn's collapsible cards snapped open and closed instantly, with no easing available even on browsers that could support one. The site had no signature mark of its own — every section break used the same plain hairline, with nothing distinguishing the one break that is actually about the site's own central figure (two vessels on one river, neither steering the other) from any other.~~
now: **WD-21** — `<details class="card">` eases open and closed via `interpolate-size: allow-keywords` and a `block-size` transition on `::details-content`, feature-detected behind `@supports` rather than polyfilled: browsers without the support keep the plain, correct, instant native behaviour they already had, at zero cost. **WD-23** — a two-row divider (`.rule-two`), used exactly once on the entire site, at the single break that is about the concept itself: right where `#sovereignty-senses` ends and Learn's Treaty section — "two vessels on one river, neither steering the other" — begins. Two thin parallel lines, teal then ochre, enact the figure the prose is about rather than illustrating it after the fact.

**WD-22 needed no new development, because the feature it proposed already existed.** The design spec described adding an optional `was:` field to `hugo/data/changelog.yaml` and rendering struck prior wording beside its replacement, "sparingly — two or three entries where the revision is meaningful." Checking the actual data file before writing anything found this pattern already present on every one of its roughly forty entries, site-wide, well before this phase — the same `~~was~~ now` convention this very completed-tasks log already uses for its own history. The one genuine gap, found by reading the template rather than assuming the data matched it: the render was unconditional (`{{ .was }}`, not `{{ with .was }}`), so the v0.1 entry's deliberately empty `was: ""` produced a pointless `<del style="color:#73968D"></del>` with nothing inside it. Fixed with `{{ with .was }}...{{ end }}`, so an entry with nothing to strike renders nothing extra.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages. Real headless-browser checks beyond the automated suite — for WD-21: `CSS.supports("interpolate-size", "allow-keywords")` confirmed `true` in the test environment, and a card confirmed to still open and close correctly (`open` toggled `false → true` on a summary click) with the new transition present. For WD-23: confirmed exactly one `.rule-two` element exists on the page, confirmed its document position (7475.7px) sits immediately before `#treaty`'s (7524.7px) with nothing else between them, and a precise element-targeted screenshot confirms both lines render distinctly — not merged into one thicker line, not missing either colour. For WD-22: confirmed the empty-`was` entry (v0.1) now renders with no `<del>` element at all, where it previously rendered one with empty content; spot-checked a populated entry to confirm the struck/replacement pair is unaffected by the conditional.

### Direct instruction — Home's hero title reset to its exact requested composition

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: the hero read as one run-on sentence across two lines by however the viewport happened to wrap it — "FREEDOM THROUGH RELATIONSHIP, NOT FROM IT." at `max-width:18ch`, followed by "NO OWNERS." and "NO OBJECTS." at the same size as the rest.~~
now: five explicit lines, matching the exact composition given directly by the author — "FREEDOM THROUGH" / "RELATIONSHIP, NOT" / "FROM IT." at full size, then "NO OWNERS." / "NO OBJECTS." at `.72em` inside their own `<span>`. Hard `<br>` tags replace natural wrapping throughout, since the break points themselves are now the specification, not an incidental result of viewport width.

**Caught a real layout bug while verifying it, not just re-typing the requested text.** The old `max-width:18ch` and `clamp(2.1rem,…)` size floor were both sized for the previous unbroken sentence; against the new hard breaks, they fought each other at narrow widths — "FREEDOM THROUGH" alone was wrapping into a fourth line well before `max-width` became the binding constraint. Re-floored to `clamp(1.55rem,6.4vw,4.4rem)` and `max-width:20ch`.

**The bisection method itself needed a correction mid-verification.** `element.innerText.split("\n")` repeatedly reported "5 lines, correct" at widths where a screenshot showed 7 rendered lines — a false positive from an `innerText`-layout quirk in this rendering path, not a real result. Re-verified using `Range.getClientRects().length` per text node instead — ground-truth geometry rather than a higher-level text API — and only then confirmed genuinely clean.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Real headless-browser checks beyond the automated suite: per-text-node `getClientRects()` count confirmed exactly 5 rendered lines at 320px, 375px, 768px, 1024px, 1440px, and 1920px — the full width range from the narrowest real device in circulation up through desktop — with no wrap inside any of the five intended lines at any of them.

### WD-19 — section arrival reveal, and a verification-script bug that looked like a product bug

**Shipped:** 2026-08-10 · **Commit/PR:** (pending)

~~was: every section on Learn was fully present at first paint, with no distinction between the section a reader has already scrolled to and the twelve still below the fold.~~
now: `/reveal.js` adds a `.rv` class to every `.kick` (section kicker) on load, then uses one `IntersectionObserver` to add `.rv-in` the first time each one scrolls into view, unobserving it immediately after — a one-shot fade-and-rise (`opacity`/`translateY`, `.25s ease`) rather than a repeating scroll effect. Shipped on Learn only; Archive has zero `.kick`-classed elements, so it was checked and deliberately left alone rather than force-fit.

**The hiding class is added by JS and only by JS, never present in the stylesheet — the spec's own explicit requirement, checked directly rather than assumed from the diff.** With scripting disabled entirely, all 13 kickers render at full opacity from first paint with no `rv`/`rv-in` class present anywhere: a reader with JavaScript off, or a crawler that never executes `reveal.js`, sees a complete page, not one that starts hidden and hopes a script arrives to reveal it. Under `prefers-reduced-motion: reduce`, the script bails out before touching the DOM at all — checked directly, not inferred from the guard clause: zero `.rv` classes anywhere and every kicker at full opacity throughout.

**A verification run mid-session appeared to show the reveal not firing on scroll, and very nearly got logged as an unresolved bug in the shipped code.** The check scrolled a below-the-fold kicker into view with `el.scrollIntoView({block:"center"})` and read back its class list 500ms later — still `opacity:0`, no `.rv-in`. The actual cause was the check itself: Learn sets `html{scroll-behavior:smooth}` under normal motion (only forced back to `auto` under reduced motion), and `scrollIntoView()` without an explicit `behavior` inherits that — so the check was very likely reading the element's state mid-animation, before the browser had settled, rather than reading a genuine failure to fire. Re-run with `scrollIntoView({block:"center behavior:"instant"})` to remove the animation-timing variable entirely, then polled the class list every 200ms instead of reading it once: `.rv-in` was present within 200ms of the scroll, and computed opacity reached `1` by 400ms, exactly matching the CSS transition's own duration. Same category of false reading as the hero-title `innerText` issue above — the fix was to the test, not the product.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean across all pages. Real headless-browser checks beyond the automated suite: all 13 `.kick` elements enumerated with their live class list and computed opacity both before and after scrolling; the above-the-fold kicker confirmed already `rv-in`/opacity `1` on load with no scroll needed; each of the 12 below-the-fold kickers confirmed starting at `rv` only/opacity `0`; the no-JS and reduced-motion paths each re-confirmed with a direct DOM query rather than trusting the script's own guard clauses.

### Direct instruction — Two Row Wampum content pulled from the live site, preserved in `docs/parked/`

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: the Two Row Wampum appeared in two places on the live site — Learn's `#treaty` section ("Two sovereigns, one shared condition...") and its `.rule-two` divider, and Archive's `#held-in-common` section, a two-track Venn diagram naming the wampum explicitly as its own layout figure. A fault entry ("The layout still parcels space") described the Archive half as a known, published condition.~~
now: both sections are off the site, in full — not deleted. `docs/parked/two-row-wampum/` holds the exact markup, CSS, and YAML data removed (`learn-treaty-section.html`, `archive-held-in-common.html`, `archive-data.yaml`, plus a `README.md` describing what came from where and how to reinstate any of it), so nothing has to be reconstructed from git history if this content comes back later.

**Removing a section cleanly meant finding everything that assumed it still existed, not just deleting the two `<section>` blocks.** Checked systematically rather than trusting a single grep: Learn's Contents nav and sticky Sections-bar nav both carried a `#treaty` link (removed from both); Archive's own Contents nav carried a `#held-in-common` link (removed); the fault entry naming the Archive section was retired (folded into the same fault-list cleanup as the Contribute-note work below, since the site author's instruction covered both); `hugo/data/topics.yaml`'s "Treaty and protocol" entry flipped from `drafted: true` back to `false` — the topic is still nameable, it just has no live section behind it now; Behind the Scenes' roadmap count ("Thirteen have a drafted outline") corrected to twelve to match; a citation in `archive.yaml` (Coulthard's *Red Skin, White Masks*) had a trailing clause pointing at "the diagram below," trimmed since the diagram is gone — the rest of that citation, a real standalone entry, was untouched.

**Reading-time estimates on both pages were re-measured rather than left stale or guessed at.** Both kickers state a number ("about 15 minutes," "about 11 minutes") that this codebase has a documented history of letting drift (see WD-27, and the changelog's own record of correcting it twice before). Measured word counts before and after removal — for Learn, the removed section's ~391 words against the page's own ~2,972 remaining; for Archive, where most content is `hugo.Data.archive`-driven rather than literal template text, both the template prose and the YAML string values had to be counted to get a real total (~354 words removed against ~3,252 remaining) — and scaled each page's prior estimate (17 → 15 minutes; 12 → 11 minutes) proportionally rather than by feel.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean, including WD-27's page-weight check, which caught a real consequence of this change on its own — Archive's actual size dropped from 134.9 KB to 127.8 KB, more than its 1.5 KB tolerance, and failed the build until `hugo/data/substrate.yaml`'s page-weight claim was corrected to match. Grepped the full live-file set (every `hugo/layouts/*.html`, every `hugo/data/*.yaml`, every root-level `*.html`) case-insensitively for "wampum" after the change: zero remaining occurrences outside `docs/parked/` and the untouched, append-only `changelog.yaml` history.

### Direct instruction — Learn gets a visible hero title; Forms & Labels moves to the top

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: Learn's real `<h1>` ("Learn: the thirteen principles") carried class `.vh` — visually hidden, screen-reader-only. A sighted reader saw the kicker line and then straight into body prose, with nothing that read as a page title. `#forms-and-labels` (relationship-anarchy and amatonormativity vocabulary, the "field, not a ladder" list of relationship forms) sat near the end of the page, between Opacity and the field guide.~~
now: the hidden h1 is a real, visible one — "Relationships, Principles, and Limits," styled to match the site's own hero pattern (kicker → h1 → lede) rather than inventing a new one. `#forms-and-labels` is now the first section after the hero, ahead of the thirteen principles, so the site's own vocabulary and its "no ranking of forms" stance are established before the principles that build on them.

**Moving a section meant moving its spacing logic with it, not just its markup.** `#principles` carried an inline `padding-top` override that existed specifically because it used to sit directly under the sticky jump bar; no other `.band` section on the page has one, because they all rely on the *preceding* section's own bottom padding instead. Left in place, `#principles` would have doubled up on top spacing now that `#forms-and-labels` sits between it and the jump bar. Removed the override from `#principles` and gave `#forms-and-labels` the smaller top padding instead, matching how every other "first section after the jump bar" on this site is actually spaced, rather than leaving a visibly larger gap where the move happened to land.

Both the hero's Contents nav and the sticky jump bar's Sections nav were reordered to match, so nav order and document order agree — checked directly in a real browser rather than assumed from the edit, comparing `[...document.querySelectorAll("main > section[id]")].map(s => s.id)` against both nav lists' own link order.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean, including axe accessibility at both breakpoints the suite runs. Real headless-browser + screenshot checks beyond the automated suite: the h1 confirmed visible (non-zero `offsetHeight`/`offsetWidth`) with the exact requested text, at desktop and 375px mobile; section order, Contents-nav order, and jump-bar order all confirmed identical, ten items each.

### Direct instruction — Invitation gets a subtitle under its hero

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: Invitation's hero was a kicker and an h1 ("An invitation.") with nothing between the title and the prose that follows.~~
now: a subtitle — "What relational sovereignty sounds like:" — sits directly under the h1, styled at the same lede weight the rest of the site uses for hero subtitles, framing the personal-register prose that follows as a demonstration of the concept rather than an unexplained tonal shift.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Confirmed in a real browser that the new paragraph is the h1's immediate next sibling, with the expected text.

### Direct instruction — Behind the Scenes drops its duplicate Contents nav

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: the hero carried a plain `nav[aria-label="Contents"]` — nine links, one per section — and the sticky jump bar added by WD-14 carried a second, collapsible `nav[aria-label="Sections"]` naming the exact same nine sections. Both existed at once.~~
now: the hero's plain nav is gone; the sticky, collapsible "Sections" nav is the only one. Its own CSS rules (`nav[aria-label="Contents"] a{...}` border/hover/arrow-suppression, and that selector's place in the shared transition rule) were removed too rather than left as dead weight now that nothing on the page matches them. The WD-14 comment explaining why the jump bar was added no longer describes a since-removed nav as its reason; reworded to say plainly that the duplicate was removed once the bar shipped.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Confirmed in a real browser: `nav[aria-label="Contents"]` count is `0`, `nav[aria-label="Sections"]` count is `1`.

### Direct instruction — Behind the Scenes' change log links to GitHub instead of rendering inline

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: `#changelog` rendered every entry in `hugo/data/changelog.yaml` in full, in the site's own struck/replacement palimpsest style — by this point roughly forty entries deep.~~
now: the section keeps its heading and framing ("Corrections are shown, not overwritten") but the rendered list is replaced with a single link to the project's GitHub repository, where the real commit history lives.

**`hugo/data/changelog.yaml` itself was not touched, and gets no new entries going forward.** Its own header comment establishes it as append-only, written for a page that rendered it — this change retires that renderer, so continuing to append to a file with no live consumer would just be maintaining a second, parallel history alongside this one. The reuse-terms section's own cross-reference ("changes are published as a dated entry in the log below") was reworded to point at the project's version history generally rather than claim a dated inline list that no longer renders.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Confirmed in a real browser: the changelog section no longer contains any of its old entry text (checked for the "v0.2 — current" version marker specifically), and its one link resolves to `https://github.com/jbloewencolon/RS-website`.

### Direct instruction — Contribute's note gets a real destination; the fault list catches up

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: the Contribute page's optional note field was captured in component state and sent to the dispatch Worker alongside the mailing-list signup, but nothing on the Worker read, stored, or forwarded it — a fault the site named itself ("no one sees what's typed there"). Fixing it, per that fault's own text, needed "a real destination... that does not exist yet."~~
now: a link under the note field — "Email this note to relationalsovereignty@gmail.com →" — opens a `mailto:` draft pre-filled with the note, name, and email, recomputed on every keystroke so it always reflects the current field values rather than whatever was typed when the page loaded. The dispatch-signup submit flow (the "Send" button, the confirmation email, the Worker POST) is untouched; this is an additional, explicitly separate path the reader triggers themselves, not a change to the existing one.

**Chose `mailto:` deliberately, not as a placeholder.** The Worker the form already posts to is external infrastructure this repository doesn't contain and this session has no access to — fixing it server-side, which the code's own prior comment assumed as the eventual path, isn't available here. A `mailto:` link needs no new backend, no account, no CSP change (anchor-tag navigation isn't governed by `connect-src`/`form-action`), and fails in the same well-understood way an ordinary `mailto:` link always has if a reader's device has no mail client configured — a small, honest tradeoff against the alternative of a third-party form-relay service this session couldn't provision credentials for. The note is still included in the existing Worker POST too, unchanged, in case the Worker is ever updated to read it directly.

**The fault list was trimmed by three, not just the one this resolves.** The site author's instruction named three entries for removal: the note fault (now genuinely resolved, above), "the archive notes are unreviewed" (removed at the author's direction, no further reasoning given), and "the layout still parcels space" (named two things — Archive's held-in-common section, gone in the change above, and the Invitation/Manifesto pairing, unrelated to it; rather than reword the fault down to the second half alone, it was retired outright, per the author's instruction covering it explicitly). Remaining faults renumbered 01–04, contiguous, matching this file's own established convention for a prior retirement (see its header comment). Two live cross-references to the old numbers were caught and fixed: Contribute's own "per fault 05" (now fault 03) and the stale "fault 07" mentioned in a code comment (rewritten to describe the actual current mechanism instead of a fault number that no longer exists). `changelog.yaml`'s historical entries mentioning old fault numbers were left untouched, as always — they describe what was true when they were written.

Verified: `npm run build:hugo && npm run build && npm run check` pass clean. Real headless-browser checks beyond the automated suite: typed a note containing an ampersand and angle brackets specifically to check encoding, and confirmed the link's `href` attribute percent-encodes the note correctly (`%26`, `%3C`, `%3E`) rather than breaking the query string or leaking raw markup; confirmed the link is absent no `body` param when the note is empty, and gains one live, without a page reload, as soon as text is entered; zero page errors thrown in either state. Fault numbers confirmed rendering as `01, 02, 03, 04` in document order.

### SEC-01.1 – SEC-01.4 — rate limiting, Turnstile, and retry backoff on the dispatch endpoint

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: `/api/subscribe` had no rate limiting anywhere in `worker/src/` — an external security audit and an independent review both found it could be used to send unwanted confirmation email to a stranger's address, repeatedly, from anywhere. `updateWithRetry`'s conflict retries had no backoff, hammering the GitHub API in a tight loop under contention.~~
now: `worker/src/ratelimit.js` (new) enforces two independent limits on every subscribe attempt — five per hour per IP, one per hour per address — plus a 300/day circuit breaker, all backed by a Workers KV binding. `worker/src/turnstile.js` (new) verifies a Cloudflare Turnstile token server-side before any email sends; both forms now carry the widget. `worker/src/github.js`'s `updateWithRetry` backs off (exponential + jitter) between conflict retries instead of retrying immediately. 8 new tests in `worker/test/abuse.test.mjs` cover per-IP/per-address/daily limits, Turnstile pass/fail/missing-secret, honeypot bypass, and fail-open behaviour with no KV bound.

Notes:
- **A real design tension, put to the site author rather than assumed:** shipping Turnstile means loading a script from `challenges.cloudflare.com` on Home and Contribute — the site's first third-party request, directly contradicting the standing "no trackers, cookies, analytics, or third-party requests" footer claim. Asked via `AskUserQuestion` rather than picked unilaterally; answer was **add Turnstile now, and update the claim** rather than skip it or build a self-hosted alternative. Footer copy on both pages and `hugo/data/substrate.yaml`'s "Third-party requests" row updated accordingly, not just disclosed in this file.
- **`scripts/check-origins.mjs` correctly caught the new cross-origin script** on first run — exactly the check's stated job ("a decision for a human, not something to silence here"). Added `https://challenges.cloudflare.com/turnstile/v0/api.js` to its allowlist explicitly, following the file's existing `KNOWN_CROSS_ORIGIN_FETCH` pattern, rather than loosening the check generally.
- **A real bug caught before shipping, not after:** the first draft wired the Turnstile widget's `ref` callback as a fresh arrow function created inline in `renderVals()` — the same pattern the codebase already uses for `onChange`/`onClick` handlers. React re-invokes a `ref` callback (with `null`, then the new value) whenever its *identity* changes between renders, unlike an ordinary event-handler prop — so that draft would have torn down and rebuilt the widget on every keystroke elsewhere in the form, silently discarding a solved token the moment someone typed their name. Fixed by making `turnstileRef` a stable class field, assigned once, in both `index.html` and `contribute/index.html`; left as a comment at both sites so the pattern isn't reintroduced by someone copying the surrounding style.
- **Absorbs SUGGEST-04** (link-prefetch hardening on `/api/confirm`/`/api/unsubscribe`, raised during initial codebase familiarization) by re-filing it into Phase 10.2/SEC-02.3 rather than doing it here — same underlying finding, independently re-found by this security review, better sequenced with the rest of the Worker hardening.
- **A new fault was published, not silently carried:** the same review found the confirm token itself leaks the chosen name and interests to anything the link passes through — a separate, more serious gap than rate limiting. Filed as fault 05 on Behind the Scenes rather than fixed in this pass (scoped to Phase 10.1's endpoint-abuse findings); closed two commits later, see the SEC-02.1 entry below.
- **Not live in production from this session — cannot be.** Two Cloudflare account actions gate deployment: creating the KV namespace and registering a Turnstile widget, neither of which this session holds credentials for. `worker/README.md` Steps 6–9 document both. The Worker fails closed on subscribe with no Turnstile secret configured (deliberately — see the abuse-test suite), so this is a **coordinated deploy**: don't ship the Worker without the site's real Turnstile site key, or vice versa.
- Verified: full `worker/npm test` (21 tests at the time), `scripts/check-origins.mjs`, `scripts/check-pages.mjs`, and a real `npm run build:hugo` rebuild of Behind the Scenes against the pinned Hugo v0.164.0 binary — all pass. `check-pages.mjs` shows one expected, non-regression console-error on `index.html`/`contribute/index.html` in this sandbox specifically (its browser cannot reach any external host at all, proven identical to `example.com` failing the same way) — pre-existing environment limitation, not caused by this change.

### SEC-02.1 + SEC-02.3 (partial) — opaque confirm tokens; interstitial confirm/unsubscribe

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: the confirm link sent at signup carried the chosen name and interests directly in a signed-but-unencrypted token — readable by anything the link passed through before it was clicked (mail-provider logs, link-scanning security products, Resend's own logs), quietly defeating the aggregate-only interest data `store.js` is built to preserve. Separately, both `/api/confirm` and `/api/unsubscribe` acted immediately on a bare `GET` — a link-prefetching mail scanner or antivirus product (which issues only `GET` requests, never submits a form) could silently confirm a signup or remove a subscriber nobody had actually clicked anything for.~~
now: `worker/src/pending.js` (new) stores the pending signup — address, chosen name, interests — in Workers KV under a random opaque id with a 48-hour TTL; the signed confirm token carries only that id and a timestamp. `/api/confirm` and `/api/unsubscribe` are both GET-to-show / POST-to-act: the link opens a small self-contained HTML page with a single button, and clicking that button — a real form submission a prefetcher never performs — is what actually confirms or removes. Still one link, one click on the button it opens; no account, survey, or added delay.

Notes:
- **Closes fault 05** (retired on Behind the Scenes, same commit) — the confirm-link privacy leak found during the SEC-01 review, above.
- **Renamed the `RATE_LIMIT` KV binding to `WORKER_KV`** (`wrangler.toml`, `ratelimit.js`, README, tests) since it now holds two things, not one. This makes it load-bearing rather than a nice-to-have: `handleSubscribe` now fails closed (503) with no `WORKER_KV` bound, since there's no fallback left that puts the signup payload back in the link — a deliberate posture change from SEC-01's fail-open default, documented as such in both the Worker README and the commit message so nobody mistakes it for an inconsistency. `ratelimit.js`'s own functions still fail open in isolation when called directly (worth keeping — defense in depth, and independently useful if this module is ever reused elsewhere); `worker/test/abuse.test.mjs` now proves that at the unit level, since the full request flow no longer reaches it without KV bound.
- **Deliberately not deleted on read:** the pending-signup KV record persists until its TTL expires rather than being removed on first confirm, preserving the existing "re-confirming is idempotent" behaviour (`confirmSubscriber` itself is already idempotent) rather than introducing a new "link already used" failure mode a double-click or browser retry could trigger.
- **Email copy updated for accuracy:** the confirmation email used to say "nothing happens until the link above is clicked" — no longer true once clicking only opens a button. Reworded to "nothing happens until that page's confirm button is clicked; opening the link alone does nothing."
- **SEC-02.3 partial, not full:** the task as filed named both endpoints; both got the GET/POST split. What's *not* done from Phase 10.2 is SEC-02.2 (the git-history question — old encrypted store versions persisting after a subscriber is removed) and SEC-02.4's remaining scope (this changelog/fault update is itself SEC-02.4's deliverable, done alongside). SEC-02.2 is `[DECISION]`-tagged and was put to the site author rather than picked; unanswered as of this entry, so a new fault 05 (renumbered from the retired one) names it explicitly rather than leaving the gap unstated.
- Verified: `worker/npm test` — rewrote `flow.test.mjs`'s confirm/unsubscribe steps to prove the GET-doesn't-mutate property directly (no commit, no email, on a bare GET to either endpoint) and that the confirm token decodes to exactly `{id, t}`, nothing else; `abuse.test.mjs` updated for the binding rename and the new required-KV failure mode. Full site `check-origins`/`check-pages`/Hugo-sync suite re-run clean after the Behind the Scenes regeneration.

### SEC-04.1 + SEC-04.2 — GitHub Actions pinned to commit SHAs, Dependabot enabled

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: both workflows called every action by a mutable major-version tag (`actions/checkout@v4`, etc.) — `deploy.yml` runs with `pages: write` and `id-token: write`, so whoever could repoint one of those tags could publish arbitrary content to the live domain. Nothing kept dependency or action versions current either way; `npm ci` already ran from a committed lockfile, but the loop of noticing when something in it needed a bump wasn't closed.~~
now: every `uses:` in `deploy.yml` and `check-pages.yml` (six distinct action references) is pinned to the 40-character commit SHA its tag currently resolves to, with the version kept as a trailing comment for readability. `.github/dependabot.yml` (new) watches three ecosystems — root `npm`, `worker/`'s own separate `npm` project (it has its own `package.json`, easy to miss with a root-only entry), and `github-actions` — the last of which is what keeps the new SHA pins from going stale: Dependabot opens a PR bumping both the SHA and its comment together whenever a pinned action ships a new release.

**The SHAs were resolved live, not recalled or guessed.** A major-version tag like `v4` is a moving pointer maintainers repoint at each new patch release, so a memorized or plausible-looking SHA would very likely be stale or simply wrong — either silently pins to an older, potentially-vulnerable release, or references a commit that doesn't correspond to what `v4` actually means today. Each of the six was resolved with `git ls-remote --tags --refs https://github.com/<owner>/<repo>.git refs/tags/<version>` against the action's own public repository — `--refs` specifically, so an annotated tag resolves to the commit it points at rather than the tag object's own SHA, which would silently pin to the wrong kind of object.

Verified: both workflow files parse as valid YAML (`yaml.safe_load`) and `.github/dependabot.yml` parses with the expected three ecosystem entries. `npm run check` still passes clean after the edit — these files aren't part of that suite's own checks, but confirms nothing else was disturbed in the same pass.

### SEC-04.4 — post-deploy check that the live domain is serving the commit that was just deployed

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: `deploy.yml` treated `actions/deploy-pages` reporting success as the end of the story. The external audit's F-02 named the real gap: `concurrency: cancel-in-progress: true` means a superseded run is killed mid-deploy, and a run that fails partway through can leave the previous artifact live with no signal anything's wrong — "deployed" and "the domain is actually serving what was just built" are two different claims, and only the first was ever checked.~~
now: `scripts/prerender.mjs` stamps every build with `_site/deployed-commit.txt`, containing the commit SHA it was built from (`$GITHUB_SHA` in Actions; a `git rev-parse HEAD` fallback keeps a local `npm run build` producing a real file too, rather than an empty or missing one). A new step in `deploy.yml`, after `deploy-pages`, fetches that file back through the URL Pages just reported and fails the run — a visible, red signal — if the two don't match, retrying up to 8 times over ~40 seconds first to absorb ordinary CDN propagation lag rather than racing it.

**Tested the actual retry/comparison logic against a local server before trusting it unexamined in CI**, since the real workflow can't be run from here: served the real `deployed-commit.txt` from a local Python HTTP server and ran the exact bash from the new step against it three ways — a matching SHA (succeeds on the first attempt), a deliberately wrong SHA (correctly exhausts all retries and reaches the failure path), and a base URL with no trailing slash (the `${base%/}` guard produces the same correct single-slash URL either way, confirming no double-slash bug if `page_url`'s trailing-slash convention ever varies).

Verified: `npm run build` run twice locally — once with no `GITHUB_SHA` set (wrote the real local `HEAD`, confirmed matching `git rev-parse HEAD` exactly) and once with a fake `GITHUB_SHA` exported (wrote that exact fake value instead), proving both branches of the source-selection logic work before either is exercised for real by Actions. `npm run check` re-run clean afterward — the new file doesn't trip `check-origins.mjs`'s per-page audit or `check-pages.mjs`'s page walk, both of which iterate explicit page lists rather than scanning `_site/` generically. `deploy.yml` re-validated as parseable YAML after the addition.

### SEC-02.2 — the git-history question, decided

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: fault 05 stated the problem — a removed subscriber's encrypted record persists in the store repo's git history indefinitely — and named three defensible answers (periodic history rewrite, migrate to a datastore where deletion is real, or keep this and disclose it) without picking one. Asked via `AskUserQuestion` with a recommended default; unanswered as of the last entry.~~
now: answered — keep the current git-commit-as-datastore approach, disclosed plainly rather than fixed. No code change: `worker/src/store.js` is untouched. Fault 05's title changed from "Nobody has decided..." to "A removed subscriber's encrypted history outlives their removal," and its body now states the decision directly — the other two options were considered and set aside, not left unexamined.

**Checked the removal confirmation page's own copy against the decision before assuming it needed a change.** `worker/src/index.js`'s post-unsubscribe page says "You've been removed. Nothing further will be sent." — accurate under this decision as written: it's a claim about future email, not about historical data, so it needed no rewording once the decision was to keep and disclose rather than to actually delete.

Verified: `npm run build:hugo` regenerated Behind the Scenes; a real browser confirms `#faults` renders fault 05 with the new title and body, at position 5 of 5.

### FLAG-08 / SEC-03.0 — the hosting/header decision, resolved

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: `tasks.md` presented three costed options (proxy through Cloudflare, migrate hosts, stay as-is) with a recommendation on file but no author decision — every row in Phase 10.3 (the clickjacking/HSTS/security-header work) was blocked on it.~~
now: option (A) chosen — proxy the site through Cloudflare. `tasks.md`'s FLAG-08 and SEC-03.0 rows both updated to record the decision. Fault 04 reworded from "there is currently no way to close this gap without changing host" (no longer true — there's a decided, if not-yet-live, fix) to naming the fix and its status plainly: decided, not yet live.

**What's left is account setup, not more decision or more code from this session.** The domain has to actually be added to a Cloudflare account and proxied — a DNS/nameserver change only the account holder can make. SEC-03.1–03.4 (the actual header values: `frame-ancestors`/`X-Frame-Options`, HSTS, `nosniff`/`Referrer-Policy`/`Permissions-Policy`, and the per-page CSP port) have nowhere to apply until that's live. Nothing in this entry claims those are done — only that the decision blocking them is no longer open.

Verified: `npm run build:hugo` regenerated Behind the Scenes for fault 04's new wording; real-browser check confirms it renders correctly. `tasks.md` cross-checked for every other place FLAG-08 or SEC-03.0 was referenced (the Phase 10.3 section header, the "Deliberately not doing" note) to avoid leaving a contradicting "blocked" claim next to the resolved one.

### SEC-03.1–03.4 (preparation only) — the exact Cloudflare header configuration, ready to apply

**Shipped:** 2026-08-11 · **Commit/PR:** (pending)

~~was: FLAG-08/SEC-03.0 named "proxy through Cloudflare" as the chosen path, but nothing existed yet describing what to actually configure once that's live — SEC-03.1–03.4 were four rows of intent with no values behind them.~~
now: `docs/spec/cloudflare-headers.md` (new) — one universal Transform Rule (HSTS starting at `max-age=300` with a staged raise schedule to a year, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, `X-Frame-Options: DENY`) plus four path-scoped CSP rules, in catch-all-first order so an unmatched future path falls back to the strictest policy rather than getting no CSP. A verification checklist closes the file, gated explicitly on real header inspection (`curl -sI`) rather than assuming intent equals result — and SEC-03.5 (removing the now-duplicate meta tags) is written to wait on every row of that checklist passing, not on this file existing.

**Every CSP value is read directly from the ten shipped `<meta>` tags, not re-derived from the external audit or from memory.** `grep`'d each page's live `Content-Security-Policy` meta content, compared them, and found they collapse into exactly four distinct strings — not ten — which is what makes four Transform Rules sufficient: Home+Contribute share one (the only two reaching the dispatch Worker and loading Turnstile), Practise is its own (needs `unsafe-eval`, nothing external), six pages share a third (`script-src 'self'`, no eval, no third party), and Resources plus all nine `*.dc.html` redirect stubs turned out to already be byte-for-byte identical to each other — confirmed by diffing them, not assumed from their both being "simple" pages — which is the fourth bucket and the catch-all default. `frame-ancestors 'none'` is the one directive added to every bucket: the single thing SEC-03 exists to deliver that a `<meta>` tag structurally cannot carry, everything else ported unchanged.

**Deliberately preparation only — nothing in this entry is live.** The domain isn't proxied through Cloudflare yet; that's an account-level DNS/nameserver change only the operator can make, described as this file's own first section rather than skipped over. No header from this file has been applied anywhere, and the file says so in its own opening paragraph so it can't be mistaken later for a record of something already done.

Verified: cross-checked all four CSP bucket strings, directive-by-directive, against the exact `grep` output pulled from the ten live page sources plus all nine redirect stubs before writing them into the file — not retyped from memory. Confirmed the Resources/redirect-stub bucket really is one identical string across all ten of those files, not nine-similar-but-different ones that happened to look alike at a glance.

### FLAG-06 — the `.dc.html` URL scheme, decided
**Shipped:** 2026-08-08 · **Commit/PR:** see BUG-03

~~was: every URL on the live site ended in `.dc.html`, an artifact of the authoring format leaking into the address bar~~
now: author chose option (C) — full pretty URLs. Each page's path is set by an explicit `url:` field in its `hugo/content/*.md` front matter, with redirect stubs left at the nine old flat paths so bookmarked and already-indexed links still land somewhere real.

Notes: recorded here 2026-08-13 during the Phase 11 cleanup — the row had been struck through in `tasks.md` since it shipped but never got an entry in this file. Reconstructed from the `tasks.md` row and the BUG-03 write-up; treat the BUG-03 entry as the fuller record.

### WD-10b — the "no fourteenth principle yet" cell, and a real grid defect found while shipping it
**Shipped:** 2026-08-11 · **Commit/PR:** PR #26

~~was: `.principles` (13 items, 3 columns) left a filled hairline slab where the fourteenth cell would be~~
now: the gap carries an authored "no fourteenth principle yet" card, author copy approved as drafted; `/behind-the-scenes/#roadmap` confirmed to resolve.

Notes:
- **The originally-planned fix was wrong and was caught before it shipped.** `grid-column:span 2` on the filler works at 3 columns and breaks at 2 — the filler can't fit beside principle 13 in the remaining single-column space, so it wraps to its own row and leaves 13 with a genuinely empty cell beside it. A hard-coded breakpoint would have papered over that and gone stale the moment the grid's column minimum or gutters changed.
- Fixed properly instead: `.principles` now draws hairlines per-cell (`border-top`/`border-left` on the container, `border-right`/`border-bottom` on each `<li>`) — the same technique WD-10a already shipped for `.fg` and the Behind the Scenes crawler grid — so a genuinely empty trailing cell shows page ground and the filler stays a plain, unspanned grid item.
- Verified by binary-searching the 2↔3 column transition (982/983px) plus visual checks at 1920/1280/1024/768/500/390/320px. No dead cell at any width.

### WD-15 — palette rollout to Behind the Scenes, Practise, Resources, Archive
**Shipped:** 2026-08-11 · **Commit/PR:** PR #26 (Resources half) + `bb94c0d`

~~was: the four semantic registers were applied systematically only on Learn; four other pages carried the colours without the meaning~~
now: all four pages carry the register system, each decision recorded rather than applied uniformly.

Notes:
- **Resources** — the one genuinely empty category (`mutual-aid`) takes ochre on its dashed rule and `Not yet built` scope label. No author judgement was needed: `resources.yaml` already carried `scope: "Not yet built"` and the category already had an explanatory paragraph, so this only added the colour.
- **Behind the Scenes** — author's call: all five faults take ochre even though that renders them uniformly, because every one is currently unfixed. The numeral moved from rust to ochre in the process — rust means "where the framework fails or runs out," but these are things *named and not yet built*, which is ochre's job. If faults ever carry mixed states, a per-fault status field is where the register would start doing finer work.
- **Practise** — the seven unbuilt tools' status labels are ochre for both `in progress` and `not started` (author's call): both are "named, not built" in register terms, and the status *words* carry the finer distinction, so nothing is encoded by hue alone. `#DB9E2A` is text-safe on that section's `#0F2A2E` ground (6.42:1).
- **Archive** — see WD-16.
- **The Practise rust carve-out, settled 2026-08-11:** rust means *safety surface*, and every current use on that page qualifies, so no rust was changed.
- Recorded here 2026-08-13 during the Phase 11 cleanup.

### WD-16 — Archive's seven access-state chips take the "named, not built" register
**Shipped:** 2026-08-11 · **Commit/PR:** PR #26

~~was: the seven access-state chips were styled like the clickable filter chips higher up the page~~
now: ochre text (`#6B4C12`, 6.24:1) on a **dashed** `#DB9E2A` edge.

Notes:
- Dashed specifically so they cannot be mistaken for the solid, clickable filter chips above them — the spec's own stated failure mode.
- Not carried by colour: the section's paragraph already reads "None of it is built yet," and dashed-vs-solid survives greyscale (verified with achromatopsia emulation).
- **The chips were already in gradient order in `archive.yaml`** (public → contributor-controlled → community-specific → temporarily restricted → excerpt only → metadata only → exists-but-unavailable), so no reordering was needed — checked rather than assumed.
- Recorded here 2026-08-13 during the Phase 11 cleanup.

### WD-28 — the orphaned "second way in" framing
**Shipped:** 2026-08-11 · **Commit/PR:** PR #26

~~was: Invitation's kicker referenced being the second of two ways in, after the pairing it referred to had been removed from Home~~
now: "A warmer register · about 4 minutes" — author chose to drop the pairing rather than restore it, tying the kicker to the existing subtitle ("What relational sovereignty sounds like:") instead of to a first-of-two that no longer exists.

Notes: recorded here 2026-08-13 during the Phase 11 cleanup.

### WD-12 — Archive's register system: a rust `counter` class, and teal as the default
**Shipped:** 2026-08-11 · **Commit/PR:** `307fa5f`, `d61f40f`

Two halves of one change, tracked as separate rows in `tasks.md` and recorded together here.

**Rust `counter` register.**

~~was: the Archive had no way to mark a text that cuts against the framework's own argument~~
now: a `counter` tag in `archive.yaml`, rendered in the rust register under the label `⚑ we have no answer to this`.

Notes:
- Label approved by the author; all four candidates approved — Barker/Alfred, Freeman (*The Tyranny of Structurelessness*), Roberts (*Torn Apart*), Bridges (*The Poverty of Privacy Rights*).
- Rust takes priority over `start`/`free` in both the rule colour and the kicker prefix. **Checked and stated honestly:** no entry currently carries both `counter` and `start`, so that priority order is untested against real data — correct if such an entry ever arrives, but not currently exercised.
- Filter chip added alongside.

**Teal as the default register.**

~~was: link-blue `#2B4C9B` doubled as the default category colour for ordinary Archive entries, so blue meant both "this is a link" and "this is an entry"~~
now: the default register is teal `#0F2A2E`. On Archive, `#2B4C9B` appears exactly twice — the base `a{}` rule and the `:focus-visible` outline — so blue means "this is a link" and nothing else.

Notes:
- Verified register distribution across all 60 entries: 34 teal (default), 14 ochre (`start`), 8 green (`free`), 4 rust (`counter`).
- **An earlier pass through the `tasks.md` table wrongly recorded this half as already shipped; that was caught and corrected before the work was actually done.** Kept here because a status field that was once wrong is worth knowing about.
- Recorded in this file 2026-08-13 during the Phase 11 cleanup — both rows had been struck through in `tasks.md` since they shipped but never got an entry here.

---

## Phase 11.1 — The shared layer

### IA-10a / IA-10b — one base CSS block instead of nine
**Shipped:** 2026-08-14 · **Commit:** `b76a656`

~~was: every page carried its own inline copy of the same ~60 lines of base CSS — nine hand-maintained copies, which is why `docs/web-design.md` §1c called every site-wide rule "a ten-file edit," and which is what produced the drift that section exists to warn about~~
now: the block lives in `hugo/layouts/partials/head-base.html` and nowhere else. A site-wide base rule is one edit plus `npm run build:hugo`.

Notes:
- The partial takes a dict of palette overrides, so the two page-level exceptions stopped being forks. Manifesto passes the dark ground plus `"tokens" false`; Invitation passes its ochre link and focus ring (FLAG-07). Structural rules ship to every page even where a page has no element to match — Behind the Scenes has no Contents nav, Manifesto has no filters — because inert selectors cost a few bytes and unifying them is what keeps this one block rather than three.
- The three hand-authored pages can't call a partial. `scripts/sync-base.mjs` lifts the block Hugo just rendered and splices it between markers in each; `npm run build:hugo` syncs, `npm run check` fails on drift. One source, one renderer — reimplementing the template in JavaScript would have been a fourth copy.
- **IA-10b came out in the wash.** The `:root` token block is defined once rather than in eight files, and Learn's duplicate is gone. Migrating literal hex to `var()` is still open — that is WD-11's second half.
- **Three traps, all now guarded.** (1) Hugo's CSS sanitiser rewrites unrecognised values in CSS context to the literal string `ZgotmplZ`; an unfiltered `rgba()` became a silently dead hover rule, not a build error. Every interpolated value goes through `safeCSS`. (2) `html/template` strips HTML comments *and* comments inside `<style>`, so no marker survives into Hugo output — the sync locates the block structurally instead. (3) A marker comment that ran onto a second line lost its closing `*/` on the first sync and commented out the rest of the stylesheet. Three pages rendered unstyled and the entire existing suite passed: html-validate, axe and the console are all blind to a stylesheet the browser gave up parsing. `sync-base.mjs` now rejects a multi-line marker, and `check-pages.mjs` asserts every page's body actually paints its intended ground colour.
- Verified: all nine pages pixel-identical at 390/768/1440 against pre-extraction screenshots. Home at 768px differed by 0.11% until the SMIL drift animation was frozen, after which byte-identical — animation phase, not layout.
- `docs/web-design.md` §1c rewritten (original struck, not deleted, since §3's per-file instructions were written against it), §3 given a pointer, and guardrail 10b added: do not edit base CSS inside a page file.

### IA-08 / IA-C3 — a 14px floor on interactive microtype
**Shipped:** 2026-08-14 · **Commit:** `17d0832`

~~was: the audit named three sub-14px controls; measuring every interactive element on all nine pages at 390 and 1440 found 97. The whole nav and utility chrome sat at 12–13.5px~~
now: 77 declarations raised to 14px — both nav copies, skip link, jump bars, filter chips, the open-all button, print and submit buttons, disclosure summaries, form labels, and the export filename/file inputs.

Notes:
- Scoped to controls. Decorative microtype is untouched, and so are the 55 sub-14px links inside sentences of prose, which WCAG 2.5.8 exempts — the measurement classifies those separately rather than by eye.
- **Two deliberate exclusions.** The stress-matrix headers (20 links at 12.5px) were left to IA-05, since that table already overflowed and raising its type first would have widened the overflow and then been redone; settled there. And the dispatch honeypot input, whose 13.33px is the browser default on a field deliberately hidden from people and password managers.
- **IA-C3 folded in**, being the same markup: the mobile nav's `aria-label="Menu"` duplicated the summary's own visible text. The audit's claim that open/closed state was missing did not survive checking — native `<summary>` exposes it — but the redundant label was noise, and removing it leaves the visible text as the accessible name.
- Verified: no horizontal body overflow at 320/390/768/1440 on any page, which is the real risk in a type change since the desktop nav is a single wrapping row. Desktop nav still fits one line at 1440; mobile nav wraps to four rows at 390 and stays inside the viewport.

### IA-10c / IA-10d — four components and three form states
**Shipped:** 2026-08-14 · **Commit:** `14cf2dd`

~~was: every control's treatment was written per instance, inline, so a reader could not tell what a control would do before clicking it and a change meant finding every copy~~
now: `.action`, `.action-utility`, `.nav-link`, `.disclosure`, plus `.form-error`, `.form-status` and `.field-invalid`, written once in the shared base from the treatments already shipped.

Notes:
- **Deliberately inert on arrival**, the same pattern WD-11 used for the token block. Applying them is IA-11/IA-12 in Phase 11.3, page by page and checkable one page at a time; defining them first is what makes that a swap instead of nine more rewrites. Verified inert: all nine pages pixel-identical at 390/768/1440 against the previous commit.
- Colours come from the palette dict rather than the register tokens, so the components work on Manifesto, which ships `"tokens" false` and has no `--teal` to reference. Manifesto and Invitation each pass their own action ground, so a primary button keeps its page's identity without forking the rule.
- The type stacks moved out of the token block and are now defined for every page including Manifesto. `design-palette.md` excludes Manifesto from the semantic *colour* register, which is a claim about what a hue means; a font stack makes no such claim.
- **Page weight rose ~2 KB per page and the build's own check caught it**, which is what that check is for. The colophon figure is updated to 19–130 KB and now says plainly that pages currently ship slightly more CSS than they use, why, and that 11.3 should return it below where it started. Grouping declarations rather than repeating them per class cut about a third of the addition.

## Phase 11.2 — Learn, in the cheap order

### IA-16 / IA-04 — principles first, and an honestly scoped bulk control
**Shipped:** 2026-08-14 · **Commit:** `7469a28`

~~was: the page is titled "Relationships, Principles, and Limits" and its hero promises thirteen principles, then opened with a terminology section~~
now: "Forms and labels" sits after the principles it defines vocabulary for.

Notes:
- Pure source reorder: no copy changed, every anchor kept, body text length identical before and after (19,092 chars), which is the check that it was a move and not an edit.
- **Measured at 390px:** the first principle section starts at 1396px instead of 2946px — 3.49 viewports of scrolling down to 1.65. Every fragment still resolves; the page renders identically with scripting off.
- **Both indexes had to move too, and not for tidiness.** `sections.js` builds its scroll-spy list from the DOM order of `nav[aria-label="Sections"]` links and then walks it assuming that order matches the document. Leaving the navs alone would have left the sticky bar naming the wrong section.
- **The spacing override moved with the section.** `#forms-and-labels` carried a top-padding override for being the first section under the sticky jump bar; every other `.band` relies on the preceding section's bottom padding. This file records that the same override was moved once before, when the section moved up. It moved back: `#principles` takes it, `#forms-and-labels` becomes a plain `.band`, bottom padding unchanged because `.band`'s value is the one it had inline.
- **IA-04:** the bulk control carries `aria-controls` naming the nine regions it governs, generated from the same data the cards are so the list cannot drift. All nine ids resolve and are exactly the nine `[data-collapsible]` elements `sections.js` sweeps. It deliberately does not name the four sexual-content disclosures — IA-01 removed those from the sweep because the page promises "opening one is the only thing that changes."

### IA-05 — the stress chart and the full rows as labelled alternate views
**Shipped:** 2026-08-14 · **Commit:** `a31be6a`

~~was: the chart and the seven full rows are the same seven situations, and the page showed both to everyone while the chart quietly needed sideways scrolling on anything narrower than a laptop~~
now: two labelled views of one dataset with an explicit switch, and the narrow-width overflow is gone rather than annotated.

Notes:
- **The breakpoint is 860px, not the audit's 820px.** 820px is where the overflow was observed, not where it stops. `table.matrix` carries `min-width:760px` and the wrap pads 4vw a side, so the container first clears 760px between 820px (752px, still scrolls) and 860px (789px, fits). Measured across 768/820/860/900/940/980/1024/1100.
- **Scripting off:** the rows always render, because they hold the content, and the chart renders only at 860px and up. That is what removes the defect rather than warning about it. Confirmed map hidden at 390/820, shown at 860/1440, rows shown at all four, switch not drawn.
- **Scripting on:** the switch decides at any width. The chart stays reachable on a phone inside its labelled scroller — the documented exception to this site's no-horizontal-scroll rule — and the scroll hint appears only when the chart is both current and narrow.
- **Deep links work across the switch**, which is the part that needed care: the chart's row headers point at ids inside the rows view, so following one must change view before the browser resolves the fragment or it scrolls to a `display:none` element. Handled in the capture phase, plus on load and on `hashchange`. Verified `#stress-map` lands visibly at 320/390/820 where it is not the default, and card ids land at 76px — exactly the sticky-bar offset — with the right card open.
- **Printing gets both views**, the same reason `sections.js` already opens every `<details>` before printing.
- The legend and the ★ note moved outside both views: those marks appear on the chart's cells and the rows' chips alike, so the key belongs to neither view.
- **Settles IA-08's deferral.** The chart keeps its 12.5px row headers rather than taking the control floor: it is a summary index whose every row and column links to the same content at full reading size in the rows view, and widening the table to satisfy a type floor would push the fitting threshold back up and reintroduce the overflow this change removes.
- First real consumer of IA-10c's `.action-utility`, including its `aria-pressed` state.

## Phase 11 addendum — Deploy pipeline fixes, and the Learn pocketed-navigation redesign

### The deploy pipeline: two independent CI breaks, both from real network access
**Shipped:** 2026-08-14 · **PR:** #39, merged

~~was: merging PR #38 put the real Turnstile site key into production for the first time — and broke both CI workflows on the next run, neither failure caused by that PR's own content~~
now: both fixed; the merge onto `main` that carried the fix produced the first fully green `Deploy to GitHub Pages` and `Check pages` runs since PR #38 landed.

Notes:
- **`prerender.mjs`, `check-pages.mjs`, and `responsive-audit.mjs`** all used `page.goto(..., { waitUntil: "networkidle" })`. With a real Turnstile key, the widget keeps background network activity going indefinitely, so `networkidle` never resolves — the CHANGE_ME placeholder had failed the challenge instantly, with nothing left to wait out, and this sandbox's own network policy blocks `challenges.cloudflare.com` outright, so neither environment could have caught this before a real CI run against a real key did. Fixed by switching all three to `waitUntil: "load"`; the two that run in CI already gate real completion on a DOM selector or an explicit settle wait, not on network silence.
- **`check-pages.yml`'s Hugo install** separately broke: `go install github.com/gohugoio/hugo@v0.164.0` started requiring Go ≥ 1.26 sometime after the pin was written, on an ordinary Hugo-side release unrelated to this repo. Fixed by switching to the prebuilt-binary install method `hugo/README.md` already documented as the supported alternative — removes the second version number to track rather than bumping the Go pin to match today's Hugo and leaving the same drift to recur on Hugo's next `go.mod` bump. `actions/setup-go` is no longer needed and was removed.
- **A third failure surfaced once the first two were fixed and CI actually reached real assertions:** Turnstile's own API correctly rejects a render request from `http://127.0.0.1` (the local test server), since the widget is scoped to `relationalsovereignty.com` — that's the whole security property (SEC-01.3), not a bug. `check-pages.mjs` now tracks real Turnstile HTTP failures via a `response` listener and excuses exactly that many matching console messages, so a genuine new console error on Home/Contribute still fails the build.
- Turnstile itself confirmed working end-to-end on the live domain by the author after this shipped.

### IA-20 — Learn's ten sections become closed-by-default pockets, keyed to the hero grid
**Shipped:** 2026-08-14 · direct author instruction, not from the audit

~~was: all ten sections rendered fully, always, making Learn a ~15-minute, ~4,500-word scroll regardless of what a reader came for~~
now: every section is a closed `<details>`. The hero grid — the same ten boxes IA-06 found duplicated against the sticky bar — opens exactly one and closes the rest; a second click on the box for the section that's open closes it. "Open every section" now opens all ten pockets too (nine of them; sexual content is still excluded, IA-01), not only the stress-test/adjudication cards nested inside as before.

Notes:
- **The sticky "Sections" bar is gone from Learn, not the hero grid.** IA-06 proposed the opposite direction (delete the grid, keep the bar as the single index) — reasonable when the page was one long scroll and the bar was the only thing that still worked once you'd scrolled past the hero. Once sections themselves are pocketed, that reasoning inverts: the grid is the fixed, always-findable reference point, and a bar naming "which of ten short, independently-openable sections you're in" has nothing left to answer. Confirmed with the author before removing the bar rather than the grid, given it reversed a documented recommendation. `sections.js` is shared with Behind the Scenes, which keeps its own sticky bar untouched — verified directly (`.jump` still present, `position:sticky`, scroll-spy still naming the current section) after every change to the shared file below.
- **The anchor ids had to move**, not just get wrapped. This codebase's own established pattern (already used for the stress-test/adjudication cards, and Resources' categories) puts a fragment id on a *descendant* of the `<details>` it should open, never on an ancestor — the reveal algorithm only opens ancestor details of the target, and an id on the `<details>` itself has no ancestor details to open. All ten ids moved from the outer `<section>` onto a new `.pocket-body[id]` wrapper inside each `<details>`. Checked for external references first: only `#field-guide` is linked from elsewhere (Invitation, Contribute, Resources, Practise, all pointing to `/learn/#field-guide`) — moving the id doesn't break those, since a browser resolves an id wherever it now lives.
- **`#field-guide`'s print-isolate CSS broke the same way**, less obviously: `body.print-isolate>main>section:not(#field-guide)` stopped matching anything once the id left the `<section>` tag, which would have hidden the field guide too under its own "print only this" button. Fixed with a `data-pocket="field-guide"` attribute on the section instead, and the same attribute was added to all ten for consistency (used again by the opacity section's kicker-colour rule, which had the same problem).
- **`<summary>` cannot contain a `<p>`** — html-validate caught this immediately on the first build: each pocket's kicker (previously a `<p class="kick">`) had to become a `<span>` with the same CSS, since `.kick`'s own rules (`display:flex` etc.) don't care what tag carries them.
- **Sexual content is a pocket like the other nine, but not a `data-collapsible` one.** IA-01's promise — "opening one is the only thing that changes" — now composes with an extra layer rather than being loosened by it: reaching one of the four cards takes opening the section *and then* a card, stricter than before. The bulk sweep and the no-JS print CSS fallback (below) both stay scoped away from it, matching what the JS-driven print handler already did.
- **A real latent bug in the pre-existing `reveal()` mechanism, exposed by pockets' much larger height swings.** Two separate, genuine problems, found by testing the same navigation repeatedly rather than once:
  1. Clicking a same-page anchor fires both a `click` listener and the `hashchange` event that follows it, and both called `reveal()` — a pre-existing overlap (for two different real triggers: a click; Back/Forward or a typed URL) that only doubles up for the click case. Each call scheduled its own independent `requestAnimationFrame`-deferred scroll. Fixed with a `lastClickedHash` guard so the `hashchange` a click causes recognises it and skips re-running.
  2. Separately, and more consequentially: this site sets `html{scroll-behavior:smooth}` sitewide, and the browser's own native fragment-scroll (toward the id'd element's own `scroll-margin-top`, not the ancestor `<details>` reveal() needs to clear) runs as an internal animation that never goes through `window.scrollTo` and can't be detected or pre-empted — only outlasted. `reveal()`'s single corrective scroll would win the instant it ran, then get quietly overwritten a few frames later as that native animation kept creeping toward its own, different target. Confirmed by instrumenting every `scroll` event: scrollY kept climbing for ~150–250ms with no further JS calls in that window. Never caught on the small cards this code was written for — a card barely moves the two targets apart — until a pocket that can resize the page by thousands of pixels made both the gap and the ongoing drift obvious. An early-exit "two stable frames" heuristic was tried first and failed: the native animation pauses irregularly rather than creeping at a steady one-tick-per-frame rate, so a brief pause was mistaken for settling. Fixed by re-snapping unconditionally, every frame, `behavior:"instant"` (bypassing the smooth CSS setting rather than starting a second competing animation), for a flat 500ms window — comfortably past every native-scroll duration measured here.
- **Verified exhaustively, not spot-checked**, after both fixes: all ten pockets' summaries land at exactly the same offset (matching the computed `BAR`) at 390px and 1440px, both via a fresh page load with the fragment already in the URL and via clicking the hero grid live — 21 landing checks, all passing, run repeatedly to confirm the result was no longer non-deterministic (it had reproduced two visibly different wrong positions across otherwise-identical runs before the fix).
- **The bulk sweep and IA-05's view-switch now coordinate.** Opening the stress-test cards via "Open every section" does nothing visible if the chart/rows switch is still on "map" — the cards live only in "rows." The bulk-open handler now calls into IA-05's `showView` (exposed via a small shared variable) when it opens, not when it closes, so what it just opened is actually visible.
- **Print, verified two ways.** With scripting, `sections.js`'s existing `beforeprint` handler correctly force-opens all nine `data-collapsible` pockets and leaves sexual content closed — confirmed by dispatching the event directly. Without scripting, the established `details:not([open])>*{display:block!important}` fallback pattern (already used on Resources and Behind the Scenes) was added, deliberately scoped with `main>section:not([data-pocket="sexual-content"])` so a no-JS reader printing the page gets the same exclusion a scripted one already gets — verified via `.matches()` that the scoping selector correctly excludes the sexual-content section. Full paginated-print rendering inside a closed `<details>` isn't reliably verifiable via headless DOM queries in this environment (`getComputedStyle` and `checkVisibility()` both proved unable to reflect the browser's internal `<details>` suppression) — same class of limitation already recorded elsewhere in this file for screen readers and cross-platform type.
- **`main [id]{scroll-margin-top}`** dropped from `5.4rem` (calibrated for the old sticky bar) to `1.5rem` — there's nothing fixed left at the top of the page to clear. `.pocket-body[id]` carries its own `9rem` on top of that, matching `.card-body[id]`'s existing value, for the same reason: overshoot enough to clear a summary sitting above the id'd element.
- **`BAR` (the sticky-bar clearance constant in `sections.js`) is now measured, not hardcoded.** `document.querySelector(".jump")`'s real height if present, else a flat 24px default. Behind the Scenes measures out at 50px now — more accurate than the stale 76px both pages shared before, not just preserved.
- Full `npm run check` clean (html-validate, axe, page weight, redirect stubs) apart from the two pre-existing sandbox-only failures to reach `challenges.cloudflare.com`, which reproduce identically on `main`.

### IA-21 — The Learn hero becomes the whole navigation, and Home's six doors carry their register at rest
**Shipped:** 2026-08-15 · direct author instruction, not from the audit · **follows** IA-20

Four changes the author raised together. The first three are one idea taken to its conclusion; the fourth is the same reasoning applied to Home.

**~~was:~~ Learn's hero carried three tally boxes — `13 principles` / `7 places they break` / `1 question each`.**
now: removed. Every figure in them was already in the sentence directly above ("Thirteen principles follow. Each carries a question you can actually ask") and in the grid's own "Seven stress tests" — they restated the hero rather than adding to it. The colour legend they sat above is kept and now sits directly on top of the ten swatches that are its first real use. `.tally`, `.t-teal`, `.t-fails`, `.t-ask` deleted; `.swatch`/`.s-*`/`.legend-key` all still in use and untouched.

**~~was:~~ ten closed section headers stacked below the grid — the same ten items, twice.**
now: a closed section takes no space on screen at all. This is the duplication IA-06 was originally about, recreated one layer down by IA-20 itself: a compact ten-box grid followed immediately by ~1,000px of ten large kicker + `<h2>` headers saying the same ten things. It also meant IA-20's own brief — "the info appears below the boxes" — was only literally true for the first box; choosing the tenth opened its content a thousand pixels further down. With the other nine absent, every box opens its content directly beneath the grid.

Notes:
- **The whole `<section>` is hidden, not just its `<details>`.** Every `.band` carries its own bottom padding and the opacity band carries a full-bleed dark background, so hiding only the disclosure would have left nine strips of empty paper and one empty dark stripe.
- **Two guards, both load-bearing.** The rule lives inside `@media screen`, so paper is untouched — printing still lays out every section regardless of what was open, which is the same reason `sections.js` opens the collapsibles on `beforeprint`. And it is keyed on a `.js-pockets` class added by `sections.js`, so with scripting off all ten headers stay: they are that reader's only way in, since the grid's links can *open* a pocket (the HTML reveal algorithm walks a fragment target's ancestors) but have no way to close one. Both verified — no-JS shows ten section headers and still opens one from a grid link; `emulateMedia({media:"print"})` lays out all ten with nothing open.
- **The accordion mechanism itself stays.** The question asked was whether the section titles still needed to be an accordion; the answer is that the *closed* headers are redundant with the grid, but the open one's summary is not — it is that section's heading and its close control, and it is what no-JS depends on entirely.
- **"Open every section" had to start reaching the sexual-content pocket.** It was excluded from the `data-collapsible` sweep (IA-01), which was invisible while a closed section still showed its header — but with closed sections hidden, the button would have *deleted a section from the page*. `sections.js` now composes an explicit `sweep` list (the collapsibles, plus every pocket, deduped) for the button, `allOpen()` and the label sync. IA-01 is not loosened by this and arguably tightened: opening that pocket reveals an intro and four still-closed card summaries, nothing more. `beforeprint` deliberately keeps using `collapsible`, not `sweep`, so the printed page's exclusion is exactly what it was.
- **The grid now says which section is open**, which it did not need to when ten headers were visible. `aria-current="true"` is what assistive technology reads; a visible tinted state, register-coloured edge and an `Open ▾` badge are the sighted equivalent. Both are set from the pockets' real state by a single `toggle` listener, so a section opened from its own summary, a pasted deep link, or the bulk control marks the grid exactly as clicking a box does — verified for each path, including that closing from the summary clears the mark.

**~~was:~~ the ten choice boxes were 44px-tall pills, `inline-flex` so they didn't fill their grid cells, in a grid with a `0` row gap.**
now: full-cell cards at ~81–120px, with a real gap on both axes, each carrying the section's own kicker verbatim as a description line — so the choice is made on what a section says rather than on a two-word label. Separate bordered cards on the paper ground rather than the hairline slab Home's doors use: ten is enough weight already, and the open one has to stand out from its neighbours. Register now reads on each card's top edge at rest, matching Home.

**~~was:~~ Home's six doors showed their register only on hover (WD-17).**
now: every door wears it at rest — a 3px top edge plus a shallow wash of the same hue — so the six read as three pairs before a word is read, and the scheme is visible to a reader who never points at anything and on a touch screen at all. Hover deepens the same wash rather than switching to the old neutral `#DDDAD0`, so a door never loses its register at the moment of contact. A legend below the grid names all three registers in words; per the site's standing constraint, colour is not the sole carrier and each door's own kicker still says what it is.

Notes:
- **The washes are opaque, pre-composited over paper, not `rgba()` of the register hue — and that is the finding here, caught by axe rather than by eye.** The doors' grid paints `#C9C6BA` behind them and shows it through a 1px gap to draw the hairlines between cards. The doors' original opaque `#E7E5DC` inline background covered it; replacing that with a translucent wash composited over the *grey*, not over paper, and dropped every card's body copy to 3.68–3.92:1. Ten serious `color-contrast` violations on a page that was clean before. Fixed by using the same washes pre-composited over `#E7E5DC`.
- **Contrast measured across all 18 combinations** (three registers × rest/hover × kicker/body/title), not just the ones axe happens to sample in the resting state: worst case 4.66:1, against the 4.5:1 floor. Learn's new `aria-current` state was scanned the same way, with a section open and with all ten open, at 1280px and 390px — axe clean at every one.
- The doors' `background` moved out of six inline `style` attributes into the stylesheet, and the `style-hover="background:#DDDAD0"` attributes went with it — the register-aware hover is a real CSS rule now.

Verification: 47 behaviour assertions across eleven scenarios (no-JS, nothing-open, single-select, toggle-close, deep link, close-from-summary, bulk open/close, print media, repeated landing position, box geometry, 320px reflow, Home's doors, and Behind the Scenes' untouched sticky bar and scroll-spy), green on three consecutive runs. `npm run check` and `npm run check:responsive` both back to their exact pre-change baselines — the only remaining failures are the two pre-existing sandbox-only `challenges.cloudflare.com` connection resets and six pre-existing sub-44px inline prose links on Home, all of which reproduce identically on `main`.

Notes on what did *not* change:
- **IA-17 is still open and still untouched.** The thirteen principles render exactly as before once their own pocket is opened.
- **Behind the Scenes**, the other consumer of `sections.js`, is unaffected: it has no pockets and no `[data-open-all]`, so `sweep` is empty and the new section 1.6 no-ops. Verified live — sticky bar present, scroll-spy still naming the current section, no page errors.

### COPY-01 — Author revision of all sixteen manifesto theses
**Shipped:** 2026-08-15 · direct author instruction · words only, no markup changed

The author supplied a revised full text of the manifesto and asked for the wording to be brought into line with it. 23 replacements across `hugo/layouts/manifesto.html`, each asserted to match exactly once before anything was written; the rendered page was then extracted from a live DOM and diffed against the supplied copy, which came back identical line for line. No element, attribute, style, id or link was touched — thesis 16's `<a href="/archive/">a movement that cannot survive its own kitchen</a>` is intact, and `html-validate` is clean.

What changed, in short: the standfirst loses its third sentence; theses 01, 02, 07, 09, 11 and 16 get substantive rewrites; 03, 04, 05, 06, 08, 13 and 14 get smaller ones (mostly em-dash asides becoming parentheses or separate sentences); thesis 10's title becomes *Liberation is material*; kickers change on 03, 04, 06, 08, 10, 11, 12 and 13. Theses 12 (body) and 15 are unchanged.

Notes, and two things the author should know were affected:
- **Thesis 01 no longer names two genealogies of possession — this rewrites what RS-006 shipped.** ~~was: "There is more than one road into that grammar. Settler sovereignty made land into property and then made people into holdings. Chattel slavery made the person into property directly, and made kinship legally void — a different mechanism, a different afterlife… Both are here. Neither stands in for the other."~~ now: "The ideology of settler colonialism made land and people into property and rendered kinship legally void." The distinction is the thing RS-006 existed to insert, placed at that exact point on `base-work-order.md` §5's instruction, and kinship rendered legally void is specifically the chattel-slavery mechanism (Patterson's natal alienation, by way of Spillers) rather than a settler-colonial one — so the new sentence attributes it to the road the old text was written to distinguish it from. **Nothing broke:** the archive group "Possession, the person, and kinship made void" (Spillers, Hartman, Roberts, Bridges) is untouched and still live, and the Archive's reading-ten standfirst still promises "two genealogies of possession." But the shelf now supports a distinction the manifesto does not make, and RS-006's open residual — adding Patterson and Collins once FLAG-03 answers which books — was to add them *for* that distinction. Flagged on RS-006's row in `tasks.md` as a reconciliation question, not silently absorbed.
- **Thesis 11 loses its own guardrail sentence.** ~~was: "…not the state, not a crowd with an opinion. Collapse that distinction and this thesis becomes a rationale for the surveillance already aimed at the families it should be protecting."~~ now: "We are accountable to those that absorb the consequences (not the state or crowds with an opinion)." The core distinction survives; the named consequence of collapsing it does not, and the kicker's qualifier ("and only the people who absorb it get to ask") is gone with it. Worth a second look given the site's standing rule that a qualification protecting a specific group stays declarative and unhedged, but the replacement is not itself wrong — recorded rather than argued.
- **One correction to the supplied copy:** thesis 05 read "Relationships recquire authority to be named"; shipped as *require*. Nothing else was altered.
- Two smaller usage points left exactly as written rather than quietly edited, since the instruction was to change the words to these words: thesis 11's "those that absorb the consequences" (of people, "who" is the usual choice), and thesis 06's "(trust, care, history, risk, labour) not by the rank", where the parentheses no longer supply the pause the em-dashes did.
- `npm run check` back to baseline — the only failures are the two pre-existing sandbox-only `challenges.cloudflare.com` connection resets, identical on `main`.

### IA-22 — The ⌖ marginal notes become pop-open disclosures, site-wide
**Shipped:** 2026-08-15 · direct author instruction, not from the audit

~~was: eighteen ⌖ notes across nine pages (Home, Manifesto, Invitation, Archive, Resources, Behind the Scenes, Learn ×6, Practise ×2, Contribute ×2) — always-visible bordered paragraphs, each carrying a caveat, safety warning, or provenance note.~~
now: every one is a `details.note`, closed by default. A click opens and holds one via native `<details>` — no script required, so this works on mobile and with scripting off exactly as well as it did before this shipped. `notes.js` layers a second path on top for a mouse or a keyboard: hovering, or tabbing to, the summary previews the note without a click.

The component (`.note` / `.note-fails` / `.note-holds`, register colours matching what each note already used — rust for the two genuine safety warnings, green for the two privacy/procedural ones, ochre for the rest) is promoted to `hugo/layouts/partials/head-base.html`, the same "define once, apply everywhere" move IA-10a made for the four interactive components. Learn's pre-existing `.note`/`.note-fails` (the only page that already had this pattern, unstyled as a disclosure) was deleted from its own `<style>` in favour of the shared definition.

**No copy was written.** Every collapsed label is a verbatim substring of the note's own leading clause, split only at punctuation the original text already had (an em dash, a colon, or a short sentence's own full stop) — e.g. "Safety —" becomes the label "Safety"; "No unilateral amendment. These terms carry…" becomes label "No unilateral amendment." and body "These terms carry…". Two notes had no clean short lead-in and kept their full first sentence as the label rather than have one invented (Archive's "Indigenous relationality…are not a generic radical style", Contribute's "Moderation prioritizes safety…"). One note (Learn, field-guide section, "The Repair Protocol…") has a hyperlink inside what would otherwise be its lead clause; the label was cut back to "The Repair Protocol" and the link stayed in the body, since a link nested inside a `<summary>` is a nested-interactive-control accessibility anti-pattern (the click bubbles to both).

Notes:
- **A genuine colour-contrast bug, caught by axe, not by eye.** Five of the eighteen notes sit on dark ground and need a lighter summary colour than the shared component's light-ground default. The first attempt set `color` as an inline style on the `<details>` wrapper, expecting it to reach the `<summary>` by inheritance — it doesn't, because the base stylesheet has an explicit rule targeting `details.note>summary` directly, and an explicit rule for an element always wins over an inherited value from its parent regardless of specificity. The five summaries rendered at `#585B4F` (the light-ground default) on `#0F2A2E` backgrounds — five new serious violations. Fixed by setting the dark colour explicitly on the `<summary>` itself as well as the `<details>` wrapper (the wrapper's copy is still needed, for the body `<p>`, which has no such explicit rule and correctly inherits).
- **Resources' CSP blocked its own new script.** The page had `script-src 'none'` — accurate when it carried no scripts at all — and silently dropped `notes.js` with a CSP console error once one was added. Changed to `script-src 'self'`.
- **A real bug in `notes.js` itself, found by testing rather than assumed away: listeners attached directly to each `details.note` at load time went dead on Home, Practise and Contribute.** Those three pages run on the dc-runtime (`support.js`): `boot()` parses the static, server-rendered `<x-dc>...</x-dc>` markup, then replaces the whole subtree with a freshly React-rendered `#dc-root` a moment after first paint. The static nodes notes.js originally attached `mouseenter`/`focusin` listeners to are the ones that get discarded; the visually-identical nodes a reader actually hovers moments later are new DOM objects that never had a listener attached. Playwright's `hover()` and `evaluate()` calls confirmed this precisely: `matchMedia` true, the element found, hover reported successful by Playwright's own actionability checks, `.open` still false — while a listener attached fresh via `page.evaluate()` after the swap fired correctly. Fixed by delegating on `document` instead of on each element: `mouseover`/`mouseout` (bubbling) and `focusin`/`focusout` (bubbling) resolve their target via `event.target.closest("details.note")` at dispatch time, which is always after whatever DOM swap has already happened, rather than caching a node reference at attach time. This also simplified the six static pages' behaviour for free — one code path instead of two.
- **`click` is prevented on the summary, on hover-capable devices only.** Without it, a mouse click while already hovering (the normal case — you hover before you click) would fight the hover state: native `<details>` toggles on click, and since hover already forced `open` true, the click would immediately flip it back to false. Gated behind the same `(hover:hover) and (pointer:fine)` media check as the rest of the file, so a touchscreen never has this handler attached and keeps plain native click-to-toggle.
- **`focusin`/`focusout` on the whole `<details>`, not `focus`/`blur` on the `<summary>`.** Several notes contain a link in their body (Learn's Repair Protocol and Contribute-routed notes, Invitation's Practise link). `focus`/`blur` fire per-element and would close the note the instant focus moved from the summary onto that link — hiding the very element a keyboard user just tabbed onto. `focusin`/`focusout` bubble and fire once for the subtree, so the note stays open while focus is anywhere inside it and only closes once focus actually leaves. Verified directly: tab to a note's summary (opens), tab again onto the link inside its now-open body (stays open, and the link itself has focus), tab a third time past it (closes).
- **Print unaffected.** `@media print{details.note:not([open])>*{display:block!important}}` added to the shared base, scoped to the class so it can't interact with Learn's existing, deliberately narrower `main>section:not([data-pocket="sexual-content"]) details:not([open])>*` rule (IA-01) — including for the one note that happens to sit inside the sexual-content pocket (Learn, "These four are closed rather than open…", explaining why the pocket's four cards are closed): unlike the pocket's actual sensitive contents, that explanatory note was never gated before, and paper has no hover or click, so it stays print-visible like all seventeen others rather than newly becoming unreachable on a printout.
- Verified with Playwright against a local static server (not `file://`, which silently breaks every root-absolute script path including `/notes.js` — the first test run's false "hover does nothing" result, on every page, was this rather than a real bug): closed by default, opens on hover, closes on mouse-leave, opens on keyboard focus, stays open while tabbing through a link in the body and only then closes, and native click-to-toggle on a touch/no-hover emulated device with no involvement from `notes.js` at all.
- `npm run check` clean apart from the two pre-existing sandbox-only `challenges.cloudflare.com` connection resets (confirmed identical via `git stash` against the prior commit before concluding they weren't a regression).

### COPY-02 — Every em dash removed from reader-visible copy
**Shipped:** 2026-08-15 · direct author instruction, not from the audit

~~was: 306 em dashes across the nine shipped pages, carrying four distinct grammatical jobs and one typographic one.~~
now: none. The count is verified two ways: a tag-stripped scan of all nine committed files, and a real headless browser reading `document.body.innerText` with every pocket and `<details>` forced open, which also covers the three dc-runtime pages whose DOM is rebuilt by React after first paint.

Replaced by role rather than by a single global swap, since one character was doing five different things:

| job | replacement | example |
|---|---|---|
| appositive / explanation | colon | "Not legal advice: a starting point for finding who can give it." |
| paired interruptive aside | parentheses | "because GitHub Pages, this site's current host, cannot set response headers" |
| two independent clauses | full stop | "It also stands on its own. You don't need a reason to be here." |
| light coordination | comma | "no watershed to name yet, not because nobody looked" |
| bullet / table marker | middle dot | "· When you disagree, what happens afterwards?" |

Notes:
- **Fifty of the 306 were never prose.** Forty-nine were bullet markers (`<li>— …` and the `<br>`-separated safety questions on Resources), and one was `td.c-none::before{content:"—"}`, the stress matrix's "not engaged" cell mark. All became `·`, which the site already uses as a separator in every footer and kicker line and which is already covered by the glyph-coverage table and `glyph-check.js`, so no new character was introduced. The matrix legend entry was changed in the same commit as the CSS that draws the cell, so the key and the chart cannot drift.
- **Applied through an anchor map, not sed.** Each entry is a `[file, old, new]` triple; the applier refuses to write anything unless every `old` matches **exactly once** in its file, and additionally rejects any replacement that still contains an em dash. Two ambiguous anchors were caught this way and lengthened. Nothing was replaced by regex.
- **`hugo/data/changelog.yaml` was deliberately skipped** — 140 em dashes, and no layout references it. It is dead data, not copy. Left for whoever decides whether the file should exist at all.
- **One specimen removed rather than converted.** `hugo/data/glyphs.yaml` listed `★ ⌖ ▪ □ —  interface marks` as a font-coverage row. The em dash was itself the specimen there, and the site no longer uses the character, so it was dropped from the displayed list. `glyph-check.js` still tests U+2014 and was left alone: it is a diagnostic tool, its output is not prose, and removing a character from a coverage test is a decision worth making deliberately rather than as a side effect.
- **Two sign-off dashes in generated output**, not page copy, also changed: Practise's consent-scale option label `"not yet — ask me"` (a control the reader picks) became `"not yet, ask me"`, and Contribute's mailto body builder no longer prefixes the signature line with a dash.
- **This roughly doubled mid-sentence colon density**, from about 2.8 to 6.35 per 1,000 words, because ~60 dashes became colons. Behind the Scenes (32) and Learn (20) carry the most. Flagged as item 8 of the voice audit below rather than silently corrected — rebalancing means a second editorial pass over copy the author has not reviewed yet.
- `npm run check` and `npm run check:origins` both back to baseline: the only failures are the two pre-existing sandbox-only `challenges.cloudflare.com` connection resets, confirmed identical on the prior commit.

### COPY-03 — Voice audit: what still reads as machine-written
**Shipped:** 2026-08-15 · `docs/audits/voice-audit-2026-08-15.md` · **audit only, nothing applied**

Run immediately after COPY-02, on the theory that removing the loudest tell makes the next ones audible. Corpus is 16,706 words of rendered `innerText` from all nine pages with everything expanded, measured by pattern counts normalised per 1,000 words and then hand-checked against source to separate deliberate voice from mechanical repetition.

Headline: **the site's default sentence engine is definition by negation** — 195 explicit negative constructions (11.7 per 1,000 words), and the `X, not Y.` terminal antithesis closing paragraphs on all nine pages, in registers as far apart as the manifesto's polemic and the colophon's engineering notes. A house style does not do that; a generator does.

Counter-finding worth recording: the site is **entirely clean of the LLM lexicon**. Zero instances of *delve, tapestry, testament, crucial, robust, leverage, landscape, realm, navigate, underscore, pivotal, multifaceted, nuanced, holistic, seamless, myriad, moreover, furthermore, in conclusion, it's worth noting, that said*. The fingerprint here is structural, not verbal, which is why it survived a copy revision by the author (COPY-01) that rewrote 23 passages without touching it.

Eight ranked candidates with counts and `file:line` locations are in the audit. Also recorded there, against this session's own work: COPY-02's colon-density side effect. Nothing has been applied — the audit proposes a four-step order in which steps 1–3 are mechanical and step 4 touches the site's honesty posture and should be the author's own pass.
